### hilavitkutin-str

Interned string primitives for the hilavitkutin ecosystem. `no_std`,
zero external dependencies. Provides `Str(u32)` handle type, `str_const!`
macro for compile-time registration via linker sections, `ArenaInterner`
trait for host-provided backends, and `StringInterner` wrapper.
