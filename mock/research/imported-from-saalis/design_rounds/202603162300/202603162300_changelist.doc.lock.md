# Doc Changelist — Implementation Round

**Date:** 2026-03-16
**Covers topics:**
- `202603162200_topic.implementation-roadmap.md`

This changelist covers ALL phases from the roadmap. Each phase's
doc changes are listed. Source changes follow in the src changelist
after docs are locked.

---

## Phase 1: Registration infrastructure

### `crates/saalis-derive/DESIGN.md.tmpl`
Rewrite to cover real `#[register]` implementation:
- Descriptor struct generation (field layout, function pointers)
- `inventory::submit!` integration
- `HasDescriptor<D>` compile-time check generation
- Error reporting (span-based, never panic)
- How `#[derive(Settings)]` generates field schema

### `crates/saalis-sdk/DESIGN.md.tmpl`
Add implementation sections for:
- `Registry` default methods (iter via inventory, get by name, validate)
- `Descriptor` field layout (name as const pointer to Named::NAME)
- Registration validation sequence at startup

---

## Phase 2: Schema and persistence

### `crates/saalis-sdk/DESIGN.md.tmpl`
Add ColumnValue implementation details:
- Serde serialization path for each affinity
- `Option<V>` null handling
- How column descriptors map to DDL

### `crates/saalis-persistence/DESIGN.md.tmpl`
Expand with implementation sections:
- rusqlite connection management (static writer + reader)
- DDL generation from descriptors
- Serde-to-rusqlite parameter binding
- `_saalis_schema` table bootstrapping
- Migration execution from OpDescriptors
- FTS5 external content table setup
- v1: direct SQLite reads (no hot store layer)

---

## Phase 3: Context and scheduler

### `crates/saalis-sdk/DESIGN.md.tmpl`
Add context implementation sections:
- `ConnectorContext<C>` method signatures with real return types
- `QueryBuilder<M>` API: `of()`, `filter()`, `execute()`
- `WriteBuilder<M>` API: `set()` → dirty queue
- HTTP delegation pattern (ureq behind trait)

### `crates/saalis-scheduler/DESIGN.md.tmpl`
Add implementation sections:
- DAG construction algorithm
- Sequential dispatch (topological order)
- Cadence timer integration
- Epoch flush trigger

---

## Phase 4: Core composition

### `crates/saalis-core/DESIGN.md.tmpl`
Expand with implementation sections:
- Boot sequence (main.rs flow)
- Inventory collection at startup
- Settings engine (7-layer precedence)
- Config file format (TOML)
- Basic file management (Roms, Temp, Cache locations)

---

## Phase 5: Web UI

### `crates/saalis-web/DESIGN.md.tmpl`
Add implementation sections:
- Axum router layout (nested routers, state)
- Maud component structure (layout, gallery card, detail page)
- DataView to Maud rendering pipeline
- SSE endpoint for progress
- Static asset strategy

---

## Phase 6: Extensions

### `crates/saalis-connector-steam/DESIGN.md.tmpl` (NEW)
Create with:
- Steam Web API endpoints used
- Settings struct (API key)
- Cataloguer implementation (library fetch)
- Enricher implementation (store page metadata)

### `crates/saalis-connector-igdb/DESIGN.md.tmpl` (NEW)
Create with:
- IGDB/Twitch OAuth flow
- Apicalypse query patterns
- Multi-query batch enrichment strategy
- Field mapping to metadata tables

### `crates/saalis-connector-screenscraper/DESIGN.md.tmpl` (NEW)
Create with:
- ROM hash identification flow
- Media type mapping (box art, screenshots)
- Rate limit handling (tier-based)
- Two-pass enrichment strategy

### `crates/saalis-format-nointro/DESIGN.md.tmpl` (NEW)
Create with:
- DAT file parsing (listinfo crate)
- CRC32/MD5/SHA1 matching pipeline
- Verification status as metadata column

---

## Root template updates

### `mock/DESIGN.md.tmpl`
No changes — `{{crate_summaries}}` auto-populates.

### `mock/PRINCIPLES.md.tmpl`
No changes.

### `mock/WORKFLOW.md.tmpl`
No changes.

---
