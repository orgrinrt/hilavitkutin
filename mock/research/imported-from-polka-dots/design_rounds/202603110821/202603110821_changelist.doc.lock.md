# Doc Changelist: Architecture Redesign

**Round:** 202603110821
**Phase:** DOC

## 1. Remove stale crates

Delete these crate directories entirely (they no longer exist in the new architecture):

- [ ] `mock/crates/polka-schema/` — absorbed into SDK traits + data store
- [ ] `mock/crates/polka-dsl/` — becomes `polka-lang-polka` extension (not a core crate)
- [ ] `mock/crates/polka-keybinds/` — becomes an extension (not a core crate)
- [ ] `mock/crates/polka-resolve/` — absorbed into `polka-data`
- [ ] `mock/crates/polka-manifest/` — absorbed into `polka-pipeline`
- [ ] `mock/crates/polka-platform/` — dissolves into SDK trait (`polka-sdk`) + extension dylibs (`polka-platform-*`)

## 2. Rename crates

- [ ] `mock/crates/polka-registry/` → `mock/crates/polka-catalogue/`
  - Rename dir, update Cargo.toml `[package].name`
  - Update all README.md.tmpl and DESIGN.md.tmpl content

## 3. Create new SDK crates

Each needs: directory, Cargo.toml, src/lib.rs (stub), README.md.tmpl, DESIGN.md.tmpl.

- [ ] `mock/crates/polka-primitives/`
  - Cargo.toml: no polka deps, `no_std` compatible
  - DESIGN.md.tmpl: `repr(C)` atoms, newtypes, `Named`, `Registrable`, `Descriptor`, `Registry` base trait
  - README.md.tmpl: one-liner summary

- [ ] `mock/crates/polka-derive/`
  - Cargo.toml: proc-macro crate, depends on polka-primitives
  - DESIGN.md.tmpl: `#[register]`, `#[derive(Settings)]`
  - README.md.tmpl: one-liner summary

- [ ] `mock/crates/polka-sdk/`
  - Cargo.toml: depends on polka-primitives, polka-derive
  - DESIGN.md.tmpl: re-exports + domain trait contracts (Language, Source, Transform, Platform, PipelineStage, DataValue, SettingsValue, SettingsDomain)
  - README.md.tmpl: one-liner summary

## 4. Update existing core crate templates

### polka-data
- [ ] DESIGN.md.tmpl: rewrite for new role (data store, settings resolution, file glob resolution, merge semantics)
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-sdk

### polka-catalogue (renamed from polka-registry)
- [ ] DESIGN.md.tmpl: rewrite for catalogue role (app/package/service database, loading, validation, querying)
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-sdk

### polka-project
- [ ] DESIGN.md.tmpl: update for new data store coordination role
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-sdk, polka-data, polka-catalogue

### polka-pipeline
- [ ] DESIGN.md.tmpl: rewrite to include manifest tracking, trait-based stage orchestration
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-sdk, polka-project

### polka-scheduler
- [ ] DESIGN.md.tmpl: update for new architecture context
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-sdk

### polka-audit
- [ ] DESIGN.md.tmpl: update for new architecture context
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-sdk, polka-catalogue

### polka-cli
- [ ] DESIGN.md.tmpl: update for SDK/plugin model (thin shell over core crates)
- [ ] README.md.tmpl: update summary
- [ ] Cargo.toml: depends on polka-pipeline, polka-audit, polka-scheduler, polka-project

## 5. Create first-party extension crates

Each needs: directory, Cargo.toml (depends on polka-sdk, cdylib crate type), src/lib.rs (stub), README.md.tmpl, DESIGN.md.tmpl.

- [ ] `mock/crates/polka-lang-polka/`
  - The `.polka` DSL: pest parser, AST, expander, builtins
  - Impls `Language` trait from SDK

- [ ] `mock/crates/polka-source-brew/`
  - Homebrew/Cask/Tap source provider
  - Impls `Source` trait from SDK

- [ ] `mock/crates/polka-source-mas/`
  - Mac App Store source provider
  - Impls `Source` trait from SDK

- [ ] `mock/crates/polka-platform-macos/`
  - macOS platform detection + platform-specific behavior
  - Impls `Platform` trait from SDK

- [ ] `mock/crates/polka-platform-linux/`
  - Linux platform detection + distro-specific behavior
  - Impls `Platform` trait from SDK

- [ ] `mock/crates/polka-keybinds/`
  - Keybind resolution with inventory-registered formats
  - Impls keybind-related SDK traits

Additional format/source extensions can be added later as needed.

## 6. Update workspace configuration

- [ ] `mock/Cargo.toml`: update `[workspace].members` to new crate list (10 core + 6 extensions = 16 crates), update `[workspace.dependencies]`
- [ ] `mock/mockspace.toml`: update `crate_prefix`, `layers`, remove references to deleted crates

## 7. Update root templates

- [ ] `mock/DESIGN.md.tmpl`: review template variables still work with new crate set
- [ ] `mock/PRINCIPLES.md.tmpl`: add type taxonomy section (trait / newtype / repr(C) enum / primitive), add plugin boundary paradigm section

## 8. Update agent templates

### mock/agent/MAIN.md.tmpl
- [ ] Update crate table to new 16-crate list (10 core + 6 extensions)
- [ ] Update architecture rules for SDK boundary
- [ ] Update layer descriptions (SDK / Core:Data / Core:Execution / CLI / Extensions)
- [ ] Add catalogue rename note
- [ ] Add plugin extension model description
- [ ] Update "what not to do" section for new constraints

### mock/agent/rules/schema.md.tmpl
- [ ] Delete (polka-schema crate no longer exists)

### mock/agent/rules/dsl.md.tmpl
- [ ] Delete or repurpose (polka-dsl is now polka-lang-polka extension, not core)

### mock/agent/rules/implementation.md.tmpl
- [ ] Update for SDK boundary rules
- [ ] Add type taxonomy rules
- [ ] Add plugin registration pattern
- [ ] Update layer references

### mock/agent/rules/cargo.md.tmpl
- [ ] Update workspace members list
- [ ] Update dependency direction rules for SDK/core/extension layers
- [ ] Add proc-macro crate conventions (polka-derive)
- [ ] Add cdylib extension crate conventions

### mock/agent/rules/mock-workspace.md.tmpl
- [ ] Update crate list and naming
- [ ] Update validation pipeline description if needed

### mock/agent/rules/generated.md.tmpl
- [ ] No content changes expected (meta-rule about not editing generated files)

## 9. Clean generated docs

Running `cargo mock` will regenerate all docs, but stale files from deleted crates
will remain. Explicitly clean:

- [ ] Delete `docs/POLKA_SCHEMA_OVERVIEW.md`
- [ ] Delete `docs/POLKA_DSL_OVERVIEW.md`
- [ ] Delete `docs/POLKA_KEYBINDS_OVERVIEW.md`
- [ ] Delete `docs/POLKA_RESOLVE_OVERVIEW.md`
- [ ] Delete `docs/POLKA_MANIFEST_OVERVIEW.md`
- [ ] Delete `docs/POLKA_PLATFORM_OVERVIEW.md`
- [ ] Delete `docs/POLKA_REGISTRY_OVERVIEW.md` (will be regenerated as `POLKA_CATALOGUE_OVERVIEW.md`)

## 10. Validation

- [ ] `cargo check` passes on the restructured mock workspace
- [ ] `cargo mock` runs successfully and generates updated docs/agent files
- [ ] No stale references to old crate names in any template or generated file
- [ ] Dependency graph reflects new architecture (SDK → Core → CLI, extensions → SDK only, no upward deps)
