# Seed Verification Redo: Four Audits Against the Original Sources

**Date:** 2026-07-20
**Status:** op-ordered redo of the seed fitness verification (op did not
accept the pass-1 judgment as sufficient on its own); pass 1's record is
`202607210010_seed-verification-pass-1.md`. Four independent agents ran
against the original sources with neutral briefs carrying no prior verdict:
one deep audit (fable tier) verifying every MANIFEST drain claim against the
actual sources, plus three parallel sweeps (sonnet tier): founding-spec
coverage, amendment-chain coverage, and currency plus self-containment.

## Verdicts

Deep audit: **FIT AFTER LISTED FIXES**. The chapters were found faithful and
precedence-correct on essentially every number, ruling, and mechanism
checked (the report's sound-list spans all eleven chapters and every
governance-cited record), with four MAJOR and seven MINOR findings, all
fixable in place. Spec-coverage sweep: full coverage of all 22 domains,
R1-R9, the constraints and principle list, and the missing-items inventory;
two omissions. Amendment-chain sweep: **clean**; every registered ruling
across A1, A2, the unified-engine amendment, the storage addendum, the
deviation ledger, the fairness and designated-core amendments, the
rechart/r3/r4 chain, and the round-level amendments traced to a correctly
stated surviving consequence; one advisory phrasing note. Currency sweep:
three stale statements and three unresolvable terms.

## Findings and their fixes (all applied in this batch)

1. MANIFEST staleness (deep MAJOR): the State line still called the
   resolution batch outstanding, the Post-A2 section still called
   `replace_value` mark-dirty-only, and the drain records did not account
   for the resolution-batch content now in the chapters. Fixed: State line
   updated, Post-A2 corrected to the implemented-awaiting-ratification
   S1-S7 state, and a "Resolution-batch additions" accounting section
   added mapping each post-collection chapter change to its evidence
   record.
2. Wake-strategy self-contradiction (deep MAJOR + currency 1/2): the pool
   paragraph stated the hybrid 128-iteration spin as the design and the
   barrier section stated the predictive three-tier parking as shipped,
   both contradicting deviation 3's benched park-immediately ruling. Fixed:
   both paragraphs rewritten to the ruling, with the predictive tiers named
   as the adapt-phase `pick_tier` follow-up whose bar is park-immediately.
3. Contracts trait-shape provenance (deep MAJOR): the WorkUnit shape's
   `NAME` drop, hint-default drop, and `COMMUTATIVE` default rest on tier-4
   GATE-2 round material with only the `Ctx` spelling registered (A1-6).
   Fixed: stated honestly in place; the tier-2 registration is owed as a
   registry row at drain time.
4. Template tier mislabel (deep MAJOR): MANIFEST called the repo design
   templates "tier-2 design of record" against A2-1's own tier table.
   Fixed: relabelled shipped-structure-of-record with registration owed;
   the eleven-versus-twelve crate count corrected in both places.
5. Spectral currency (currency 3): plan.md's deviation note still framed
   the fiber bench as a proposed oracle; the bench ran (`202607201200`) and
   the evidence proposes restoring canon's Step 7/8 split. Fixed: the note
   now states the delivered evidence and the awaited bless.
6. A2-4 misattribution (deep MINOR): execution's numbering note claimed
   A2-4 places the ownership mask outside the batch; A2-4's list instead
   carries the superseded waist barrier. Fixed with the accurate
   substitution rationale.
7. Cross-chapter figure disagreement (deep MINOR): dispatch stated the
   1.28-1.40x resource-reload figure flatly while storage demotes it.
   Fixed: dispatch carries the qualification and points at storage.
8. Storage S5 gap and settled-versus-unratified tension (deep MINOR):
   the cost-asymmetry caller contract (S5) was unstated and the
   Replaceability section read as settled. Fixed: S5 added to the
   enumeration; the section now points at the awaiting-ratification spec.
9. mid_slot chapter home (deep MINOR): the record-boundary resolution
   lived only in governance. Fixed: execution's head+tail paragraph states
   the `mid_record` record-boundary split.
10. Undefined terms (deep MINOR + currency): `GATE-2`, `G2-Nd`, `Swap-D`,
    `Capacity`/`Dim<N>`, `RunCfg`, and "unit-outer" now carry definitions
    at their first or governing use.
11. Consumer scale envelope (spec sweep): the per-project scale profiles
    the MANIFEST claimed folded in were absent. Fixed: execution's
    strategy section states the three-scale target envelope; the MANIFEST
    left-out note now matches reality.
12. Lint-list gap (spec sweep): the three cross-project lints
    (no-toml-value, no-ui-below-cli, sdk-traits-only) are now recorded in
    the MANIFEST as deliberate scope exclusions.
13. Perf-gate baseline phrasing (chain-sweep advisory): execution now
    distinguishes A1-8's persistent-pool ruling from the fairness run's
    actually-measured spawn-per-frame baseline.

## Standing result

With the batch applied, the seed's outstanding gate is unchanged: the op
rulings listed in `seed/governance.md#lifecycle` (swap S1-S7 ratification,
spectral role restore, the soundness-required plane relocation for
deviations 1/5/6, pointer-size spawn), then `frozen = true`, then the
registry drain and the second verification pass on the candidate registry.
