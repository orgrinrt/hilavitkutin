# Swap semantics panel, memo 3: synthesis and draft specification

**Date:** 2026-07-19
**Author:** Fabian Giesen (lens: synthesis; audit the prior memos, resolve what the evidence decides, draft the spec for op's ruling)
**Inputs:** memo 01 (soundness), memo 02 (cost shapes), sketch `202607193310_planaffecting-replaceable-exclusivity/FINDINGS.md`, A2 (`202607193200`), the storage addendum (`202606210600`), consolidation spec R5, the hybrid call, shipped source.

## 1. Audit

Memo 01's four soundness obligations all check out against the source. The `Selector`-witnessed whole-blob write mirrors the shipped read (`dispatch/engine_ctx.rs:733`, `ptr::read` through the backcast pointer) and the drain's `ptr::write` (`resource/bindings.rs:377`). The drop-free overwrite follows from `T: ColumnValue` (`Copy`). The parked-between-frames exclusivity argument is correctly grounded: `&mut self` plus the workers' raw-pointer discipline, not the borrow checker alone. The plan-dirty gap is real and exactly as cited: `scheduler/mod.rs:1389` computes the band gate from `first_frame` only, `:1394` touches `self.plan_dirty` solely to suppress the unused-field warning, and the `replace_resource` body (`:1217-1221`) never sets a bit.

Two things memo 01 missed. First, a citation slip: "`store.rs:270`" covers only `Replaceable` (`hilavitkutin-api/src/store.rs:276`); `PlanAffecting` lives at `hilavitkutin-api/src/run_cfg.rs:43`, and it is today `sealed::Sealed + BuilderInput` with one implementor (`DefaultRunCfg`, `:155`). The double-impl fork is therefore not expressible in shipped code at all; it becomes real exactly when #696 (A2-3) unseals the trait onto consumer resource value types. Second, and more consequential: the shipped signatures are asymmetric. `replace_value<V: Replaceable>` locates `Resource<V>` (`scheduler/mod.rs:1232`); `replace_resource<T: PlanAffecting>` locates `T` directly (`:1212`), which is the pre-A2-3 shape where the plan-affecting thing was the `RunCfg` builder input, not a resource value. The unsealing forces a signature change neither memo states; clause 2 below states it.

Memo 02's structural claims verify: the band gate at `:1416-1420` runs the whole leading plan band from phase 0 on a plan-dirty frame, versus `dirty_units()` (`:1284`) seeding only the read-intersecting cone. The asymmetry is real and belongs in the contract, not just the mechanism notes. The Swap-D bench arm survives audit with one condition attached: it cannot run before the #344 write side exists, so it is commissioned with that trigger, not scheduled now. Memo 02's decision not to bench the accessor-shape question is correct (one write per swap, between frames; the stakes do not justify an arm).

The sketch closes memo 01's exclusivity fork. `impl<T: PlanAffecting> !Replaceable for T` compiles on the pinned nightly, rejects the double impl with E0751 in both the same-crate and the real cross-crate shape, and the #133556 coherence gap does not bite because this use wants negative impls to forbid, which is the half that works. The supertrait alternatives fail on the merits, not just on taste: `PlanAffecting: Replaceable` makes both entry points legal on a plan-affecting type, which is the silent-skip hole itself; `Replaceable: PlanAffecting` routes every app-state swap through the plan band, inverting memo 02's cost asymmetry. The lint alternative is advisory where a structural wall is available. This fork is decided; it is not presented as a decision point below.

No genuinely open fork remains for op beyond ratifying the spec. The collection write mechanism is not a fork: it is a named contingency on #344, deferred by construction.

## 2. Draft specification

Normative clauses, written for near-verbatim lift into a canon amendment.

**S1 (replace_value mechanism).** `Scheduler::replace_value<V: Replaceable>(&mut self, new: V)` installs `new` as the whole one-record blob value: a `ptr::write` of `V` through the same `Selector<V, Index>` witness (the binding's backcast pointer) that the drain and the projection read use. No pointer derived by any other route. No drop of the old value (`V: ColumnValue` is `Copy`; the bytes are overwritten). The call additionally sets the store-dirty bit for `Resource<V>` via `mark_dirty`, and never touches `plan_dirty`. The call is legal only between frames: `&mut self` plus the parked-between-frames invariant (named as load-bearing, since worker raw pointers are outside the borrow checker's jurisdiction) means no swap can race a live-streamed read or an in-flight snapshot. The swap is observable starting at the next `run()` and at no earlier point; of multiple swaps before a `run()`, only the last is ever observed.

**S2 (replace_resource mechanism).** `Scheduler::replace_resource<T: PlanAffecting>(&mut self, new: T)` performs the identical S1 write and store-dirty mark, and additionally sets the `plan_dirty` bit for `T`'s `PlanAffectingId` in the scheduler's plan-dirty bit array (`scheduler/mod.rs:667`). The very next `run()` consumes that array: the band gate is `first_frame OR any plan_dirty bit`, the plan-dirty frame enters the leading plan band at phase 0, and the consumed bits clear. Signature obligation riding #696: `PlanAffecting` becomes an open marker on resource value types (the `BuilderInput` supertrait and the seal drop), and the `Locate` target unifies with S1 to `Stores: Locate<Resource<T>, Index>`. The shipped `Locate<T, Index>` shape is the pre-A2-3 form and is corrected by the implementing round.

**S3 (collection members).** Specifiable now, normative: (a) a swap never changes the base pointer of a `Seq`/`Map` member's ptr+len view; columns are sized at plan time and never resized, and a foreign base pointer breaks provenance. (b) A swap never writes past the member's `Cap` bound N; the new value shares the old value's exact `Seq<T, N>` type, so the bound holds by construction. (c) Element bytes are written into the member's existing collection-column region, up to N' elements, then the length updates; the blob carries only the ptr+len view. Explicitly contingent on the #344 write-side wiring: the concrete accessor that performs (c). Until #344 lands its write half, a collection-bearing swap installs the scalar members and the ptr+len view only, and the element-write half is unimplementable, not underspecified. The contingency is #344 by name.

**S4 (trait relationship, resolved).** hilavitkutin-api ships `impl<T: PlanAffecting> !Replaceable for T` under `#![feature(negative_impls)]` (WATCH tier per `unstable-features.md`, allowed; the gate carries the standard vetting comment). PlanAffecting is dominant: a plan-affecting type can never take the cheap `replace_value` path, structurally, with E0751 at the consumer's impl site. Evidence: the exclusivity sketch, both arms.

**S5 (cost asymmetry, caller-facing contract).** `replace_value` costs one blob write plus, next frame, the store's dirty cone: O(sizeof(V)) then O(cone). `replace_resource` costs one blob write plus, next frame, the entire leading plan band: O(sizeof(T)) then O(plan band). `replace_value` is the supported every-frame path. `replace_resource` swaps must be rare relative to frame rate; an every-frame `PlanAffecting` swap is misuse regardless of correctness. The implementing round's first obligation is the S2 wiring (today's `:1389`/`:1394`/`:1217` gap).

**S6 (test surface).** The catalogued red at `tests/resource_bindings.rs:353` maps to S1: its assertions (same blob pointer before and after, new value observed) are already the spec's; only its ignore reason is refuted. The implementing round rewrites the reason to cite this spec, then removes the ignore when the install lands. New tests: blob-address byte-identity across a swap (S1); double-swap-before-run, last value wins (S1); ZST swap, dirty set, no byte write, no UB (S1); swap of an unread resource, dirty set and cleared, no unit re-runs (S1); `replace_resource` causes plan-band entry on a non-first frame (S2, catalogued red until the wiring lands); collection swap asserting base-pointer stability and length update (S3, catalogued red on #344); compile-fail, double impl rejected with E0751 (S4).

**S7 (bench).** Swap-D (memo 02): cold-write-then-stream, N swept 16/64/256, U at 4/16/64 bytes, baseline versus swap-at-frame-end conditions, commissioned with trigger "when #344's write side lands". It decides whether the `write_resource_collection_sizes` morsel term absorbs the swap's write-then-read traffic or a separate budget line is owed. No accessor-shape arm.

## 3. Artifacts the implementing round must correct

All three carry the refuted member-by-member-copy layout language A2-5 traced to the tier-4 input memo; each contradicts S1 and is corrected in the round that implements this spec:

1. `mock/crates/hilavitkutin/DESIGN.md.tmpl:540-552`: "A `Replaceable` swap is a member-by-member copy of each leaf into its column slot ... never an in-place blob write", plus the paragraph calling the whole-value pointer write "exactly the blob write this rule excludes". Under S1 that pointer write, through the witness, IS the specified install.
2. `mock/crates/hilavitkutin/src/scheduler/mod.rs:1235-1245`: the `replace_value` FIXME citing "Canon (`202606210600`)" for the per-leaf copy. The citation is the A2-5 mis-grounding; the canonical addendum decided the blob.
3. `mock/crates/hilavitkutin/tests/resource_bindings.rs:353`: the ignore reason (same language, same mis-citation). The test body stands.

Additionally, the S2 signature correction (clause 2) touches `replace_resource`'s bounds and `run_cfg.rs:43`'s seal alongside #696.
