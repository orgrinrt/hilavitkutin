# DOC changelist: G2-0b RunPhase / RunPipeline + waist barrier

**Date:** 2026-06-07
**Phase:** DOC
**Round:** 202606070100 (topic `202606070100_topic.gate2-g2-0b-runphase-waist.md`)

## Scope

Document the phase and pipeline nesting levels above the landed `RunTrunk`: the `TrunkCons` / `TrunkNil` phase carrier and `RunPhase` walk, the `PhaseCons` / `PhaseNil` pipeline carrier and `RunPipeline` walk, and the degenerate single-core waist barrier between phases. Single-core, output-equivalent to the flat GATE-1 walk. No `Scheduler::run` re-pointing (G2-0c).

## Files

### `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Extend the trunk-nesting paragraph (added in round 202606070048, the paragraph introducing `FiberCons` / `RunTrunk`) with the two levels above it:

- `TrunkCons<T, Rest>` / `TrunkNil`: the value-carrying phase carrier, a cons-list of trunks mirroring `FiberCons` / `FiberNil`, each cell carrying one trunk (itself a `FiberCons` fiber list) plus the tail. `RunPhase<A, WL>` walks it, delegating each trunk to `RunTrunk` and recursing. Single-core the trunks run sequentially; at many cores a phase's trunks run concurrently on pinned cores with zero cross-trunk sync.
- `PhaseCons<P, Rest>` / `PhaseNil`: the pipeline carrier, a cons-list of phases. `RunPipeline<A, WL>` walks it, running each phase via `RunPhase` then arriving at a waist barrier before the next phase. The waist is the phase boundary (the point every trunk of a phase reaches before the next phase begins). At single core the barrier is a degenerate one-arriver (an `AtomicUsize` counter that never spins when the expected arriver count is one); the load-bearing N-core waist is the shipped `phase_barrier_arrive`, wired in a later round. The barrier call site is present so the waist composes into the nest without breaking devirt.

State that the full `RunPipeline -> RunPhase -> RunTrunk -> RunFiber` nest is a 3-deep witness cons-list inferred at the call site with no turbofish, and that the whole nest plus the waist barrier devirtualises under fat LTO (sketch-proven). State that `Scheduler::run` still drives the flat walk; consuming the nested carrier built from the plan is the following round.

## Verification

`grep -n 'RunPhase\|RunPipeline\|TrunkCons\|TrunkNil\|PhaseCons\|PhaseNil\|waist barrier' mock/crates/hilavitkutin/DESIGN.md.tmpl` returns the extended paragraph's backticked names. The names become source-backed in this round's SRC CL, so `design-doc-source-mismatch` is satisfied at round close.

## No other doc surfaces

The carrier types are documented from the engine side (the convention `WuCons` / `FiberCons` already follow); no `hilavitkutin-api/DESIGN.md.tmpl` edit. No BACKLOG / SHAME edits.
