**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605291700 (#623 remove full specialization from the engine accessor)
**Source topic:** 202605291007_topic.unstable-feature-soundness-sweep.md
**Sketch:** mock/research/sketches/202605291007_engine-ctx-spec-free-accessor/ (op-gate PASS)

Refresh the hilavitkutin-api provider-API design doc to reflect the spec-free type-keyed
accessor. The forbidden full `feature(specialization)` is removed from the engine; the
keyed `resource` / `read` / `write` accessors gain an inferred `Selector` index generic and a
bridge-trait bound, resolved at the concrete WU call site. The conceptual call shape is
unchanged (`resource::<T>()` or full inference); the index never appears at a call site.

## DOC CHANGE: hilavitkutin-api/DESIGN.md.tmpl provider-API trait signatures

File: `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`

Update the `ColumnReaderApi` / `ColumnWriterApi` / `ResourceProviderApi` signatures in the
provider-API code fence to carry the inferred index generic and the `Resolve*` bridge bound,
add the three `Resolve{Resource,ColumnRead,ColumnWrite}` bridge traits, and add a short prose
note explaining that the index witness resolves spec-free at the concrete call site (so the
engine needs no `feature(specialization)`). `VirtualFirerApi::fire` is unchanged (a B3 no-op,
no keyed lookup). The accessor-generation macros (`provider_generic!`) are unchanged.
