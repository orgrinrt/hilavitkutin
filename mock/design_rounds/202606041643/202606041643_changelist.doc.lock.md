# Doc changelist: phase-sequential dispatch

**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041643 (phase-sequential-dispatch, branch `feat/hilavitkutin-phase-sequential-dispatch`)

The engine design doc describes `Scheduler::run` / `build` dispatch order as "the topological permutation". The src changelist changes that order to the plan's phase-sequential traversal (`phases -> trunks -> fibers -> units`), derived at build. Update the prose to match.

## `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The two mentions of dispatch following "the topological permutation `build` computed" (the `run` `CollectFiber`-path paragraph, and the build paragraph that stores the permutation) update to state that `build` derives the dispatch order by flattening the plan's phase structure (`phases -> trunks -> fibers -> units`, a topological order grouped by the plan's locality structure) and `run` dispatches the units in that order. Name the traversal so the design-doc-source-mismatch lint ties it to the source.
