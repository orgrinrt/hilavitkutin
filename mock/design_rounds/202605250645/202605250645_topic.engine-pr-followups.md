# Topic: post-engine PR review follow-ups

**Date:** 2026-05-25
**Phase:** TOPIC
**Scope:** consolidated polish across the four engine-body PRs (#80, #81, #82, #83). One round, multi-file. Addresses non-blocking reviewer findings that were deferred from each PR's own merge.

## Current state

Four engine PRs merged this session: #80 assign_cores, #81 select_approach, #82 codegen_fiber+core stubs, #83 scheduler_run no-op body. Each accumulated reviewer findings that were either non-blocking, declined-with-rationale, or doc-precision. Rather than four × Round-2-per-PR ceremonies, the consolidated sweep is one round that pulls only the legitimate findings into a single bounded scope.

## Changes proposed

Four sources touched: viola repo not involved; hilavitkutin only.

### From PR #80 (assign_cores)

- `thread/mod.rs`: add fourth test `core_count_zero_returns_empty_assignment` exercising `assign_cores(USize(0), plan_with_phase_trunks(&[3]))` and asserting `assigned_count == USize::ZERO`, `trunk_index[0..MAX_LANES] == NO_TRUNK`. Pins the boundary case left uncovered in Round 1.
- `DESIGN.md.tmpl` `## assign_cores base implementation`: add one sentence naming the per-phase trunk-index alignment assumption (the function assumes trunk i in phase A is logically the same trunk as trunk i in phase B; the dispatch stage relies on this).

### From PR #82 (codegen_stubs)

- `dispatch/mod.rs::codegen_stub_tests`: tighten both tests. `codegen_fiber_returns_empty_skeleton` adds asserts for `phase == PhaseId::ZERO`, `morsel_range.start == USize::ZERO`, `morsel_range.len == USize::ZERO`. `codegen_core_returns_empty_skeleton` adds asserts for boundary_count + sync_point_count + one element check on `fibers[0]`. Pins the full skeleton shape rather than sampling three fields each.

### From PR #83 (scheduler_run)

- `DESIGN.md.tmpl` `## Scheduler::run no-op transitional body`: fix the paragraph that says "constrains downstream RunCfg impls via the Scheduler<Cfg> impl block" to correctly say "constrains this method (not the impl block) via a method-level where-clause". Matches the source doc-comment + actual source shape.
- `scheduler/mod.rs::run` rustdoc: trim the seven-step contract enumeration. Replace with a short "transitional no-op body; returns `Cfg::Out::default()`. Real body tracked in BACKLOG entry `Scheduler::run real morsel loop body`". The seven-step description belongs in the BACKLOG entry now (it describes the deferred work, not the current behaviour); move it there if not already redundant with what BACKLOG carries.

### Skipped (with rationale)

- PR #80 finding 2 (per-phase trunk index alignment): addressed via DESIGN sentence (above).
- PR #81 findings: finding 2 (`codegen_core` lint:allow drive-by) was declined inline in Round 2 of #81 with rationale already shipped. Finding 4 (bench-validated wording nit) was absorbed in BACKLOG entry wording. Nothing remains.
- PR #82 finding 1 (dead `Identity` import in test module): false positive. `USize::ZERO` requires the `arvo::strategy::Identity` trait in scope. Sibling files (`fiber_dispatch.rs:8`, `core_dispatch.rs:10`) import the same trait for the same reason. No action.
- PR #83 finding 2 (em-dash in topic memo): closed-round artefact under `mock/design_rounds/202605250628/`. Topic memos are ungated per writing-style.md. Editing closed round artefacts is awkward; skipping per the discipline-but-not-gated framing.

## Lock criteria

- `cargo build -p hilavitkutin` clean.
- `cargo test -p hilavitkutin` clean (all existing tests + the new boundary test + the tightened skeleton tests pass).
- `cargo test --workspace` clean modulo pre-existing trybuild flake.
- `cargo mock --lint-only --commit` clean.
