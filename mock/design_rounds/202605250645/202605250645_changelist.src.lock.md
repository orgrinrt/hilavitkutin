**Date:** 2026-05-25
**Phase:** SRC
**Scope:** Engine PR follow-ups source CL. Three source touches: assign_cores test, codegen_stub tests tightening, scheduler_run rustdoc trim.
**Source TOPIC:** `mock/design_rounds/202605250645_topic.engine-pr-followups.md`.
**Source DOC:** `mock/design_rounds/202605250645_changelist.doc.lock.md`.

# Source changes

## CHANGE: zero-core-count boundary test ADDED to assign_cores_tests
File: `mock/crates/hilavitkutin/src/thread/mod.rs`
Verification: `grep -q "core_count_zero_returns_empty_assignment" mock/crates/hilavitkutin/src/thread/mod.rs`.

New test `core_count_zero_returns_empty_assignment` calls `assign_cores(USize(0), plan_with_phase_trunks(&[3]))`. Asserts `assigned_count == USize::ZERO` and every `trunk_index[i] == NO_TRUNK`. Closes the boundary-case gap in PR #80's test matrix.

## CHANGE: codegen_stub_tests tightened to pin full skeleton shape
File: `mock/crates/hilavitkutin/src/dispatch/mod.rs`
Verification: `grep -A 20 'codegen_fiber_returns_empty_skeleton' mock/crates/hilavitkutin/src/dispatch/mod.rs | grep -q 'morsel_range' && grep -A 20 'codegen_core_returns_empty_skeleton' mock/crates/hilavitkutin/src/dispatch/mod.rs | grep -q 'fibers'`.

`codegen_fiber_returns_empty_skeleton` adds asserts for `phase == PhaseId::ZERO`, `morsel_range.start == USize::ZERO`, `morsel_range.len == USize::ZERO`. `codegen_core_returns_empty_skeleton` adds asserts for `boundary_count`, `sync_point_count`, and one element check on `fibers[0]` to verify it is itself an empty skeleton. Pins the full skeleton contract instead of sampling three fields.

## CHANGE: Scheduler::run rustdoc trimmed; seven-step list moved to BACKLOG
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `! grep -B 2 'pub fn run' mock/crates/hilavitkutin/src/scheduler/mod.rs | grep -q '7. Return' && grep -q 'transitional no-op body' mock/crates/hilavitkutin/src/scheduler/mod.rs`.

The seven-step body contract enumeration at scheduler/mod.rs:103-132 (the seven steps describing what the real body would do) shrinks to a one-paragraph "transitional no-op body" description naming the BACKLOG entry that tracks the real body. The full seven-step description belongs in the BACKLOG entry (which already covers the deferred work); the source rustdoc should describe what the function actually does today, not what it would do under the deferred follow-up. Closes reviewer finding #3 on PR #83 (rustdoc drift between described contract and shipped no-op behaviour).

# Lock criteria

- All three `## CHANGE:` verification predicates pass.
- `cargo build -p hilavitkutin` clean.
- `cargo test -p hilavitkutin` clean (existing tests + new boundary test + tightened skeleton tests all pass).
- `cargo test --workspace` clean modulo pre-existing trybuild flake.
