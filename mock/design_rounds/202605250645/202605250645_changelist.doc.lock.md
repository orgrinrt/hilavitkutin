**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Engine PR follow-ups DOC CL. Two DESIGN section revisions reflecting the source-level shape (PR #80 trunk-index assumption; PR #83 method-level bound).
**Source TOPIC:** `mock/design_rounds/202605250645_topic.engine-pr-followups.md`.

# Doc changes

## CHANGE: per-phase trunk-index alignment assumption ADDED to `## assign_cores base implementation`
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "trunk-index alignment" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

Adds one paragraph to the existing `## assign_cores base implementation` DESIGN section naming the per-phase trunk-index alignment assumption that the pipeline-wide round-robin relies on. The function assumes trunk i in phase A is logically the same trunk as trunk i in phase B; the dispatch stage that consumes `CoreAssignment` depends on this alignment to drive the same core through different phase's matching trunk slots. Closes reviewer finding #2 on PR #80 by making the assumption explicit so the heterogeneous-core follow-up does not silently break it.

## CHANGE: method-level bound correction in `## Scheduler::run no-op transitional body`
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "method-level where-clause" mock/crates/hilavitkutin/DESIGN.md.tmpl && ! grep -E "constrains downstream RunCfg impls via the .Scheduler<Cfg>. impl block" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

Replaces the prose that says the bound is on the `Scheduler<Cfg>` impl block (incorrect; the source has the bound on the method itself). Updated wording says: "method-level `where Cfg::Out: Default` clause on the `.run()` method (not the impl block), so `Scheduler::builder`, `replace_resource`, `replace_value`, and `Default for Scheduler<Cfg>` stay unaffected for `Cfg::Out: !Default` consumers". Closes reviewer finding #1 on PR #83 (DESIGN-source mismatch).

# Verification target

After the round ships: `cargo build -p hilavitkutin` clean. `cargo test -p hilavitkutin` covers the tightened skeleton tests + the new boundary test. Both `## CHANGE:` verification predicates above pass.
