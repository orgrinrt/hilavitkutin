# DOC changelist: template leakage scrub

**Date:** 2026-05-04
**Round:** 202605051100
**Phase:** DOC
**Source topic:** `202605051100_topic_template_leakage_scrub.md`

## Scope

DOC-only round. Edits 14 template files to remove references to local sibling repositories (`polka-dots`, `saalis`, `loimu`, `clause-dev`) that violate the workspace `readme-format.md` rule on local-dev-routine leakage. No source changes. No surface changes. No new exports.

## Per-file edits

### `mock/crates/hilavitkutin-ctx/README.md.tmpl`

Line 6: `across saalis, polka-dots, loimu.` -> `across the hilavitkutin consumer surface.`

### `mock/crates/hilavitkutin-extensions/README.md.tmpl`

Line 9: `(viola-plugin-abi, clause-plugin-abi, loimu-plugin-abi) layer their` -> `(viola-plugin-abi and similar consumer-side ABI crates) layer their`

### `mock/crates/hilavitkutin/README.md.tmpl`

Line 5: `polka-dots, saalis, loimu. ` -> `multiple downstream consumer ecosystems. ` (preserves the `#![no_std]` clause that follows).

### `mock/crates/hilavitkutin-persistence/DESIGN.md.tmpl`

Line 6: `consumer (saalis, polka-dots, loimu).` -> `consumer.`

Line 8: `Lives in saalis workspace for now. Will be extracted to` -> `Currently developed alongside the hilavitkutin engine. Will be extracted to`

Line 184: `wraps in whatever container format they want (.saalis ZIP+zstd,` -> `wraps in whatever container format they want (consumer-defined ZIP plus zstd,`

### `mock/crates/hilavitkutin-ctx/DESIGN.md.tmpl`

Line 4: `Reusable across saalis, polka-dots, loimu.` -> `Reusable across hilavitkutin consumer ecosystems.`

### `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`

Line 4: `(polka-dots, saalis, loimu) depend on this crate to define WorkUnits.` -> `depend on this crate to define WorkUnits.`

Surrounding sentence may need a small rephrasing to flow without the parenthetical; preserve the meaning.

### `mock/crates/hilavitkutin-api/BACKLOG.md.tmpl`

Line 23: `ops instead of one. polka-dots research shows typical pipelines stay` -> `ops instead of one. Prior consumer-ecosystem research shows typical pipelines stay`

### `mock/crates/hilavitkutin-linking/DESIGN.md.tmpl`

Line 123: `- Research input: \`EXTENSIONS_RESEARCH.md\` at the repo root (loimu / saalis / polka-dots synthesis).` -> `- Research input: \`EXTENSIONS_RESEARCH.md\` at the repo root (synthesis of dlopen patterns from prior consumer ecosystems).`

### `mock/crates/hilavitkutin-extensions/DESIGN.md.tmpl`

Line 111: `viola-plugin-abi / clause-plugin-abi / loimu-plugin-abi` -> `viola-plugin-abi and similar consumer-side ABI crates`

Line 179: `Downstream consumers (loimu, viola-cli) build them on top.` -> `Downstream consumers (e.g. viola-cli) build them on top.`

### `mock/crates/hilavitkutin-str/DESIGN.md.tmpl`

Line 6: `across saalis, polka-dots, loimu. Lives in saalis workspace for` -> `across multiple consumer ecosystems. Currently developed alongside the hilavitkutin engine for`

### `mock/crates/hilavitkutin-persistence/BACKLOG.md.tmpl`

Line 42: `consumer-chosen container (saalis's ZIP+zstd, polka's .polka,` -> `consumer-chosen container (e.g. ZIP plus zstd archives,`

### `mock/crates/hilavitkutin-str/BACKLOG.md.tmpl`

Line 43: `different backing than the one first saalis / hilavitkutin supply.` -> `different backing than the one first consumer-stack supplies.`

### `mock/DESIGN.md.tmpl`

Line 42: `Downstream consumers (viola, future loimu, future clause extension surfaces) build domain-specific contract crates on top.` -> `Downstream consumers (viola and other planned consumer-side ABI crates) build domain-specific contract crates on top.`

Line 95: `- **loimu** -- signal/DAG framework (research docs in \`design_rounds/\`).` -> remove this bullet. The research that informed the architecture is already cited elsewhere; the consumer-ecosystem name does not belong in a public design doc.

Line 97: `- **polka-dots**, **saalis** -- misc pipeline consumers.` -> remove this bullet entirely. Same reasoning.

Surrounding bullet list will need a brief rewrite if the deletions leave the section thin.

### `mock/PRINCIPLES.md.tmpl`

Line 55: `hilavitkutin-api's 22 access/context/platform traits, saalis's` -> `hilavitkutin-api's 22 access/context/platform traits, the engine's own`

The exact replacement preserves the surrounding sentence's meaning (the trait count plus the engine's primary trait family).

## Verification

Post-edit:
- `grep -E "polka-dots|saalis|loimu|clause-dev" mock/crates/*/{README,DESIGN,BACKLOG}.md.tmpl mock/{DESIGN,PRINCIPLES}.md.tmpl` returns zero hits.
- `cargo mock` regenerates `docs/` cleanly with no leakage propagating.
- All 14 templates remain valid markdown (no broken section structure from removed bullets).

## Out of scope

- Em-dash sweep across templates (separate follow-up round).
- Source file changes.
- Changes to round-artefact files under `mock/design_rounds/*/`.
- Research notes under `mock/research/`.

## Frozen at lock

Locked via `cargo mock lock`. Any subsequent revision deprecates this CL and opens a new one.
