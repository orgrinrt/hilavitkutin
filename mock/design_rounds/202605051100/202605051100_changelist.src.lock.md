# SRC changelist: template leakage scrub

**Date:** 2026-05-04
**Round:** 202605051100
**Phase:** SRC
**Source:** `202605051100_changelist.doc.lock.md` (DOC CL, locked)

## Scope

This is a DOC-only round. There are no source changes.

The DOC CL edited `mock/crates/*/{README,DESIGN,BACKLOG}.md.tmpl` and the workspace-level `mock/{DESIGN,PRINCIPLES}.md.tmpl` to remove references to the author's local sibling repositories. The state machine requires both DOC and SRC changelists for every round; this SRC CL is the no-op record that proves no source touched.

## Per-file changes

None. Source under `mock/crates/*/src/**` is untouched in this round. `cargo +nightly check` continues to pass with the same set of pre-existing warnings.

## Verification

- `git diff dev..HEAD -- 'mock/crates/*/src/**'` reports zero changes.
- `cargo +nightly check` clean (no new warnings introduced).

## Frozen at lock

Locked via `cargo mock lock`. Round closes via `cargo mock close` after this lock.
