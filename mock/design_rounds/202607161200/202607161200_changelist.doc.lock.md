# Doc changelist: post-review fixes for the storage-model round

**Date:** 2026-07-16
**Phase:** DOC (this changelist opens it; no doc-template edits in scope)
**Scope:** reviewer findings on PR #165 (round 202607161000): comment-only source
clarifications. No `*.md.tmpl` edits; this changelist exists to walk the phase
machine to IMPL for the source comments.

## Changes proposed

None on doc templates. The source changelist that follows carries: (1) a note on
`ResourceBinding::__ptr`'s debug assertion being forward-looking (tautological
in-process, a real guard only at a future extension boundary), and (2) greppable
`// FIXME:` markers at the Context projection's resource arm for the two designed
but unbuilt refinements (the Seq/Map live-stream member wiring and the
mutable-resource write-back), per the placeholder-marking convention.
