# Src changelist: post-review comment clarifications

**Date:** 2026-07-16
**Phase:** IMPL (this changelist opens it)
**Doc CL:** `202607161200_changelist.doc.lock.md`
**Scope:** comment-only fixes from the PR #165 review.

## CHANGE: `ResourceBinding::__ptr` debug assertion gains a forward-looking note
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`
The `debug_assert_eq!` comparing the recorded shape to `ValueShape::of::<T>()` is
tautological in-process (the binding's type parameter is both recorder and
checker); it becomes a real guard only at a future extension boundary where a
foreign shape is supplied. A comment states this so a reader does not mistake it
for an active check.
Verification: `grep -A2 "debug_assert_eq" mock/crates/hilavitkutin/src/resource/bindings.rs` shows the note.

## CHANGE: greppable FIXME markers at the projection resource arm
File: `mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs`
The `Project` resource-head arm gains `// FIXME:` markers for the two designed
but unbuilt refinements: the Seq/Map collection-member live-stream wiring
(tracked #344, catalogued test in tests/resource_snapshot.rs) and the
mutable-resource write-back (no mutable resource surface exists yet).
Verification: `grep -c "FIXME" mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs` returns 2.
