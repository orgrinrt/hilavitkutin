**Date:** 2026-05-30
**Phase:** SRC
**Round:** 202605300351 spectral-width-gate
**Scope:** wide-block spectral former + per-block width-gate; arvo bump + live_dim

## Summary

The arvo dependency moves to the live-node-count fix; the engine `SymmetricLaplacian` gains the required `live_dim` override. `project_fiber_components` computes the global spectral grouping once and routes wide blocks through it via a new filter-and-remap helper; narrow blocks keep the greedy former.

## CHANGE: bump arvo dependency past the live-node-count fix
File: `mock/Cargo.lock`
Verification: `grep -A2 'name = "arvo"' mock/Cargo.lock` shows the arvo git source at the live_dim commit (4cec32a), not the prior 0d6fb4f. `cargo +nightly-2026-05-28 test -p hilavitkutin` builds against it.

## CHANGE: impl `LinearOperator for SymmetricLaplacian` GAINS `live_dim`
File: `mock/crates/hilavitkutin/src/plan/laplacian.rs`
Verification: `grep -n "fn live_dim" mock/crates/hilavitkutin/src/plan/laplacian.rs` returns one match returning `self.adjacency.node_count()` (the live node count of the bidirectional CSR). Required by the bumped arvo `LinearOperator` trait; the engine does not compile without it.

## CHANGE: new `steps::spectral_grouping_in_block`
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: `grep -n "fn spectral_grouping_in_block" steps.rs` returns one match. Takes the global spectral `FiberGrouping` and a block's units; filters the assignment to those units and remaps the block's distinct spectral ids to contiguous block-local FiberIds, returning the same block-local `FiberGrouping` shape `group_fibers_in_block` returns.

## CHANGE: `project_fiber_components` width-gate routes wide blocks to spectral
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: the function computes the global `spectral_partition` once; at the per-block former-selection point, a block whose unit count exceeds the threshold (DESIGN.md.tmpl ">5 fibers", applied to block unit count, tunable) uses `spectral_grouping_in_block`, narrow blocks keep `group_fibers_in_block`. `grep -n "spectral_grouping_in_block\|spectral_partition" steps.rs` shows the wiring.

## CHANGE: new strict test cases for the spectral arm
File: `mock/crates/hilavitkutin/tests/fiber_projection.rs`
Verification: a WIDE block (a connected two-triangle-plus-bridge graph, >5 units) routes to spectral and partitions at the bridge into two fibers; a NARROW block stays greedy. Flip-check: forcing the narrow former on the wide block (or lowering the gate) yields the greedy roll grouping instead of the spectral cut. Plus the C1d-3a deferred corners where tractable (empty input).

## Deferred

`Branch`/`Bridge` components and the non-id `Fiber` fields stay deferred (C2 / column classification). The per-fiber / id-width cap-overflow hardening stays in #641. The definitive spectral-vs-real-greedy end-to-end bench stays in #644.
