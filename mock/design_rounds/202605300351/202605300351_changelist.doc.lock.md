**Date:** 2026-05-30
**Phase:** DOC
**Round:** 202605300351 spectral-width-gate
**Scope:** DESIGN.md.tmpl step 7/8 prose reflects the wide-block spectral former landing

## Intent

C1d-3a's step 8 prose said the width-gated spectral former for wide blocks "follows". This round lands it, so the step list drops the "follows" hedge and states the width-gate routing.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

### Change 1: step 7 (spectral) prose

The spectral former is now consumed by step 8's width-gate (no longer a standalone follow-on). Note it runs over the live-node-count-aware Laplacian (loose-CSR correct).

### Change 2: step 8 (fiber grouping) prose

State the width-gate: wide blocks (unit count above the threshold) form fibers via the spectral former, narrow blocks via the greedy former; both project per-block onto `Trunk.components`. Drop the "the width-gated spectral former for wide blocks follows" clause.
