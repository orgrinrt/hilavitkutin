**Date:** 2026-06-06
**Phase:** DOC
**Round:** 202606062047 (d668 engine adjrow cap lift)

# Doc changelist

One template edit: extend the `PlanDims` section of the engine crate DESIGN to
carry the new `AdjRow` associated type and explain the waist cap lift. The
`design-doc-source-mismatch` lint requires the backticked trait surface in the
doc to match source; adding `type AdjRow` to the trait in the src CL means the
doc must list it too.

## EDIT: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Section "Plan dimensions: `PlanDims`" (the `pub trait PlanDims { ... }` code
block plus surrounding prose).

- Add `type AdjRow: BitSequence + BitAccess + BitLogic + Copy + Default + Identity;`
  to the trait code block, after the thirteen `Capacity` dimensions.
- Add a paragraph after the code block: the adjacency / waist analysis row word
  is carried as a concrete associated type pinned per impl, sized to cover
  `Units` node indices. Explain why it is a concrete per-impl type and not an
  inline `Bits<{cap_size(...)}>` (arvo's `Bits` resolves its storage container
  by per-concrete-N projection; a symbolic capacity expression never reduces to
  a literal during bound-checking, so the row word must be concrete at the impl).
  Note the `AdjRow`-covers-`Units` relation is a documented contract, not
  type-enforced, matching arvo's own concrete-width waist tests, and that this is
  what lifts the engine's old 64-node waist cap (`DefaultPlanDims` keeps a
  64-wide row; a consumer with a larger `Units` budget pins a wider row).
