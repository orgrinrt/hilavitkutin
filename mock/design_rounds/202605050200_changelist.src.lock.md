**Date:** 2026-05-05
**Phase:** SRC
**Round:** 202605050200

# Source changelist: USize(0) → USize::ZERO

Three sites in hilavitkutin-api migrate from the literal `USize(0)` to the canonical `USize::ZERO` constant exposed by arvo Round 8 (#306).

## CHANGE: `StoreId::default()` body
File: `mock/crates/hilavitkutin-api/src/id.rs:20`
From: `StoreId(USize(0))`
To: `StoreId(USize::ZERO)`
Verification: `grep "USize::ZERO" mock/crates/hilavitkutin-api/src/id.rs` returns one match; `! grep "USize(0)" mock/crates/hilavitkutin-api/src/id.rs` returns zero.

## CHANGE: import for `Identity` trait in id.rs
File: `mock/crates/hilavitkutin-api/src/id.rs:6-7`
Adds `use arvo::strategy::Identity;` (required for `USize::ZERO` to resolve).
Verification: `grep "use arvo::strategy::Identity" mock/crates/hilavitkutin-api/src/id.rs` returns one match.

## CHANGE: `CountingSink::new()` body
File: `mock/crates/hilavitkutin-api/src/sink.rs:74`
From: `count: USize(0)`
To: `count: USize::ZERO`
Verification: `grep "count: USize::ZERO" mock/crates/hilavitkutin-api/src/sink.rs` returns one match.

## CHANGE: import for `Identity` trait in sink.rs
File: `mock/crates/hilavitkutin-api/src/sink.rs:14-15`
Adds `use arvo::strategy::Identity;` (required for `USize::ZERO` to resolve).
Verification: `grep "use arvo::strategy::Identity" mock/crates/hilavitkutin-api/src/sink.rs` returns one match.

## CHANGE: `AccessSet::LEN` const for unit tuple
File: `mock/crates/hilavitkutin-api/src/access.rs:42`
From: `const LEN: USize = USize(0);`
To: `const LEN: USize = USize::ZERO;`
Verification: `grep "const LEN: USize = USize::ZERO" mock/crates/hilavitkutin-api/src/access.rs` returns one match.

## CHANGE: import for `Identity` trait in access.rs
File: `mock/crates/hilavitkutin-api/src/access.rs:8-9`
Adds `use arvo::strategy::Identity;` (required for `USize::ZERO` to resolve).
Verification: `grep "use arvo::strategy::Identity" mock/crates/hilavitkutin-api/src/access.rs` returns one match.

## Out of scope (tracked, not migrated in this round)

The remaining `USize(0)` sites in the engine, persistence, and providers crates are tracked for a follow-up sweep:

- `mock/crates/hilavitkutin/src/thread/assignment.rs`
- `mock/crates/hilavitkutin/src/thread/convergence.rs`
- `mock/crates/hilavitkutin/src/thread/pool.rs`
- `mock/crates/hilavitkutin/src/scheduler/metrics.rs`
- `mock/crates/hilavitkutin/src/scheduler/plan.rs`
- `mock/crates/hilavitkutin/src/dispatch/morsel.rs`
- `mock/crates/hilavitkutin/src/adapt/metrics.rs`
- `mock/crates/hilavitkutin-persistence/src/manifest.rs`
- `mock/crates/hilavitkutin-providers/src/interner.rs`

Task #328's stated scope was the api crate; the rest belong in a follow-up task. Recommend a single mechanical sweep PR after this lands.

## Verification gate

`cargo +nightly check` from `mock/` succeeds for the workspace post-apply. Verified locally before SRC CL was written.
