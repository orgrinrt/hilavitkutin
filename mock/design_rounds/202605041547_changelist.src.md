**Date:** 2026-05-04
**Phase:** SRC
**Scope:** hilavitkutin-linking unix backend (1 helper fn body, doc comment on shared variant)
**Source topics:** 202605041547 (D2 unix real errno)

# Source changelist: surface real errno on Unix dlopen failure

Mechanical execution of the locked doc CL. One file edited. No public API change.

## CHANGE: fn `read_errno` body REPLACED FROM `USize(0)` sentinel TO `*__errno_location()` / `*__error()` per-platform

File: `mock/crates/hilavitkutin-linking/src/backend/unix.rs:65-73`

Replace the stubbed body with per-platform extern blocks declaring `__errno_location` (Linux/Android) and `__error` (Darwin/BSD), then dereference the returned thread-local pointer to read errno. Cast the resulting `c_int` to `usize` (always non-negative for set errno). On unsupported unixes, fall through to `c_int = 0` and document.

Verification: `grep -n "fn __errno_location" mock/crates/hilavitkutin-linking/src/backend/unix.rs` returns one match, and `grep -n "fn __error" mock/crates/hilavitkutin-linking/src/backend/unix.rs` returns one match, and `! grep -n 'USize(0)$' mock/crates/hilavitkutin-linking/src/backend/unix.rs` (the `USize(0)` sentinel disappears from the source).

## CHANGE: doc comment ADDED on `LinkError::LoadFailed::platform_code`

File: `mock/crates/hilavitkutin-linking/src/error.rs:17-19`

Update the existing `LoadFailed` variant's doc comment to mention the Unix best-effort semantics established in the doc CL. The variant signature does not change.

Verification: `grep -A 4 "LoadFailed" mock/crates/hilavitkutin-linking/src/error.rs` shows the variant doc mentions both `__errno_location` (or "errno") and "best-effort" / "POSIX does not require".

## What is NOT changed

- `LinkError` enum variant signatures unchanged.
- `is_null_terminated`, `platform_load`, `platform_resolve`, `platform_close` bodies unchanged.
- Windows backend unchanged.
- No new pub items.
- No tests added or modified. Cross-platform errno extraction has no portable harness in `no_std`; runtime correctness rides on the per-platform extern declarations being canonical.

## Lock criteria

- [ ] Both verification greps above succeed.
- [ ] `cargo check -p hilavitkutin-linking` passes on host platform (macOS in this workspace), confirming the macOS branch resolves and no other code path is broken.
- [ ] `cargo mock --lint-only --build` passes scoped to `hilavitkutin-linking`.
