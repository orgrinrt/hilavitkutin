# Topic: lint:allow format drift residual — hilavitkutin-str/src/lib.rs

**Date:** 2026-04-21
**Status:** TOPIC phase
**Task:** #72 slice (follow-up to 202604320000)

## Context

Round 202604320000 declared and applied format reformat for nine
`lint:allow(...)` sites in hilavitkutin source. One site —
`hilavitkutin-str/src/lib.rs:25` — was listed in the src CL but the
actual edit did not land on the merged branch (tool execution hiccup
during stash round-trip). The `lint-allow-requires-task-id` lint
still warns on this single site.

## Decision

Apply the same reformat as the other eight `lint:allow(no-alloc)`
sites in this family: `-- StringInterner is the no-alloc interner
wrapper, not std String.` → `reason: StringInterner is the no-alloc
interner wrapper, not std String; tracked: #72`.

No new semantic, no new escape, no design change — purely finishing
the prior round's declared scope.

## Out of scope

Everything else covered by round 202604320000 (already landed).
