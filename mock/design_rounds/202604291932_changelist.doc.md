# Changelist (doc): arvo Round D alignment

**Date:** 2026-04-29
**Phase:** DOC
**Source topics:** `202604291932_topic.arvo-round-d-alignment.md`

## Doc-side changes

None. The hilavitkutin-api and hilavitkutin-str public surfaces describe types and access semantics, not their construction syntax. The Round D shape change in arvo (`arvo::newtype::*` → `arvo::*` re-export, `IBits(N)` literal → `ibits(N)` helper) is invisible at hilavitkutin's documented surface.

Verified by grep across `mock/crates/**/*.md.tmpl`:

- No `arvo::newtype` references.
- No `IBits(` or `FBits(` or `Width(` literal-construction examples.
- The `Urgency` type alias is not documented at the per-crate DESIGN.md level (it appears only in source as a `pub type` re-export shape).

## Src-side changes deferred

All edits land in the src changelist. Per-file detail there. Summary:

- `hilavitkutin-api`: 6 src files (`column_value.rs`, `context.rs`, `id.rs`, `platform.rs`, `store.rs`, `work_unit.rs`, `hint.rs`) and 4 test files (`column_value.rs`, `work_unit.rs`, `store_types.rs`, `providers.rs`).
- `hilavitkutin-str`: 1 src file (`handle.rs`).
- `mock/Cargo.lock`: bump arvo deps to current dev SHA via `cargo update -p arvo -p arvo-bits -p arvo-bitmask -p arvo-graph -p arvo-sparse -p arvo-spectral -p arvo-comb -p arvo-hash`.
