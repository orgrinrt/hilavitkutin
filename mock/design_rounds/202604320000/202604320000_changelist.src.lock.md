# Source Changelist: lint:allow format drift cleanup

**Date:** 2026-04-21
**Phase:** SRC
**Topic:** `202604320000_topic.lint-allow-format-drift.md`
**Doc lock:** `202604320000_changelist.doc.lock.md`

All changes are comment-only reformats from pre-#80 `-- reason` form
to the current `reason: <why>; tracked: #72` form. No semantic
change to the lint escapes themselves; all sites remain structurally
justified under #72 as residual cleanup.

## `mock/crates/hilavitkutin-api/src/hint.rs`

Line 18:

- `pub trait Sealed {} // lint:allow(undocumented_type)`
  → `pub trait Sealed {} // lint:allow(undocumented_type) reason: sealing marker trait; semantics live on parent trait docs; tracked: #72`

## `mock/crates/hilavitkutin-api/src/lib.rs`

Line 21:

- `pub(crate) trait Sealed {} // lint:allow(undocumented_type)`
  → `pub(crate) trait Sealed {} // lint:allow(undocumented_type) reason: crate-private sealing supertrait; semantics live on mod + parent trait docs; tracked: #72`

## `mock/crates/hilavitkutin-persistence/src/manifest.rs`

Lines 17, 23:

- `// lint:allow(no-bare-numeric) -- tracked: #72; array-size const must be usize`
  → `// lint:allow(no-bare-numeric) reason: array-size const must be usize (rust grammar); tracked: #72`

## `mock/crates/hilavitkutin-persistence/src/lib.rs`

Line 34:

- `pub use string_table::{StringTable, StringTableEntry}; // lint:allow(no-alloc) -- `StringTable` / `StringTableEntry` are the persistence string header types, not std `String`.`
  → `pub use string_table::{StringTable, StringTableEntry}; // lint:allow(no-alloc) reason: `StringTable` / `StringTableEntry` are persistence string header types, not std `String`; tracked: #72`

## `mock/crates/hilavitkutin-str/src/interner.rs`

Lines 21, 25:

- `pub struct StringInterner<A: ArenaInterner> { // lint:allow(no-alloc) -- interner wrapper name, not std `String`.`
  → `pub struct StringInterner<A: ArenaInterner> { // lint:allow(no-alloc) reason: interner wrapper name, not std `String`; tracked: #72`

- `impl<A: ArenaInterner> StringInterner<A> { // lint:allow(no-alloc) -- interner wrapper name, not std `String`.`
  → `impl<A: ArenaInterner> StringInterner<A> { // lint:allow(no-alloc) reason: interner wrapper name, not std `String`; tracked: #72`

## `mock/crates/hilavitkutin-str/src/lib.rs`

Line 25:

- `pub use crate::interner::{ArenaInterner, StringInterner}; // lint:allow(no-alloc) -- `StringInterner` is the no-alloc interner wrapper, not std `String`.`
  → `pub use crate::interner::{ArenaInterner, StringInterner}; // lint:allow(no-alloc) reason: `StringInterner` is the no-alloc interner wrapper, not std `String`; tracked: #72`

## `mock/crates/hilavitkutin-str/src/ergonomics.rs`

Lines 4, 23, 27, 33:

- `use crate::interner::{ArenaInterner, StringInterner}; // lint:allow(no-alloc) -- interner wrapper import, not std `String`.`
  → `use crate::interner::{ArenaInterner, StringInterner}; // lint:allow(no-alloc) reason: interner wrapper import, not std `String`; tracked: #72`

- `fn into_str(self, interner: &StringInterner<impl ArenaInterner>) -> Str; // lint:allow(no-alloc) -- interner wrapper, not std `String`.`
  → `fn into_str(self, interner: &StringInterner<impl ArenaInterner>) -> Str; // lint:allow(no-alloc) reason: interner wrapper, not std `String`; tracked: #72`

- `fn into_str(self, _: &StringInterner<impl ArenaInterner>) -> Str { // lint:allow(no-alloc) -- interner wrapper, not std `String`.`
  → `fn into_str(self, _: &StringInterner<impl ArenaInterner>) -> Str { // lint:allow(no-alloc) reason: interner wrapper, not std `String`; tracked: #72`

- `fn into_str(self, interner: &StringInterner<impl ArenaInterner>) -> Str { // lint:allow(no-alloc) -- interner wrapper, not std `String`.`
  → `fn into_str(self, interner: &StringInterner<impl ArenaInterner>) -> Str { // lint:allow(no-alloc) reason: interner wrapper, not std `String`; tracked: #72`

## Verification

- `cargo mock --lint-only --commit` — no `lint-allow-requires-task-id`
  warnings remain.
- `cargo test` — unchanged (no source semantics changed, only comments).
