# Src changelist: per-fiber morsel slice 1 rename

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606201500

## CHANGE: `ExecutionPlan::morsel_sizes` FROM `morsel_sizes` TO `morsel_windows`
File: `mock/crates/hilavitkutin/src/plan/mod.rs`
Verification: `grep "pub morsel_windows:" mock/crates/hilavitkutin/src/plan/mod.rs` returns one match; `! grep "morsel_sizes" mock/crates/hilavitkutin/src/plan/mod.rs`.

Field rename + init (`:127`) + corrected doc (window semantics, placeholder-partition flag).

## CHANGE: `size_morsels` FROM `size_morsels` TO `compute_fiber_morsel_windows`
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: `grep "pub fn compute_fiber_morsel_windows" mock/crates/hilavitkutin/src/plan/steps.rs` returns one match.

Fn rename; same `(record_count, fiber_count)` signature + partition body for now,
marked `// PLACEHOLDER (slice 4)`. Call site updated at `plan/mod.rs:391`.

## CHANGE: `morsel_sizes_id` FROM `morsel_sizes_id` TO `morsel_windows_id`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep "pub fn morsel_windows_id" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match; the two call sites (`:568`, `:2582`) and the store-back read `plan.morsel_windows` updated.

## CHANGE: reader in `core_program.rs` uses `morsel_windows`
File: `mock/crates/hilavitkutin/src/plan/core_program.rs`
Verification: `grep "plan.morsel_windows" mock/crates/hilavitkutin/src/plan/core_program.rs` returns one match (rename only, line 72).

## NEW TEST: `tests/r6_morsel_window_formula.rs`
File: `mock/crates/hilavitkutin/tests/r6_morsel_window_formula.rs`
Verification: `cargo test -p hilavitkutin --test r6_morsel_window_formula -- --ignored` lists the catalogued contract.

`#[ignore = "catalogue: per-fiber L1 morsel-window formula (L1_usable / Σ write_bytes); tracked #341"]` asserting the spec formula for a known fiber; red until slice 4 lands the real `compute_fiber_morsel_windows` body. Numeric output of slice 1 is the old partition, so existing tests pass unchanged.
