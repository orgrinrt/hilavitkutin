# Doc changelist — resource-snapshot disambiguation

Round: 202605282256. Phase: DOC. Covers `*.md.tmpl` edits that reconcile the documentation with the deletion of the placeholder `ResourceSnapshot<const N>` and the corrected status of the typed phase-bounded view.

The source deletion lives in the src changelist. This doc changelist removes a stale signature presented as a shipping contract and records where the dispatch-time stack-caching optimization actually belongs.

## File: `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`

The `### ResourceSnapshot<'phase, R>` section presents a concrete struct whose bound (`BuilderInput<Dispatch = ResourceDispatch>`) and field projection (`Pin<&'phase R::Owned>`) reference machinery that does not exist: the round 202605101036 topic 2 builder reshape replaced per-kind dispatch with `StoreDispatch` and friends, and `BuilderInput` carries only `Init` and `Dispatch`, no `Owned`. Replace the stale concrete struct with a prose statement of the contract (read-only typed view, captured at a phase barrier, lifetime-invariant, phase-bounded, observed by `AdaptWu` at `ScheduleEnd`) and an explicit status note: the concrete signature lands with the data plane (HILA-RUNTIME-C6), reconciled against the current `BuilderInput` contract. Keep the lifetime-contract prose mention consistent.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The "not yet done" follow-up list mentions "No `ResourceSnapshot` persistence drain" and "draining ResourceSnapshots through persistence". These remain accurate (the typed view is not yet shipped) and stay as prose. No change required unless the wording implies the type currently exists; trim to keep it a forward statement.

## File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`

Add a designed-but-deferred note (UNBACKTICKED type names per the BACKLOG convention) recording that the dispatch-time stack-caching of accessed resources (cache resources into a stack-local array before a morsel loop so LLVM proves the stack does not alias the column buffers, then run the loop with resources in registers) belongs to the dispatch codegen (HILA-RUNTIME-C2, the Phase D rust-pipe pattern), not a standalone resource type. This preserves the idea that the deleted placeholder module doc carried, attached to its correct home.

## File: `mock/DESIGN.md.tmpl`

The root design doc carries the same stale `ResourceSnapshot<'phase, R: BuilderInput<Dispatch = ResourceDispatch>>` signature in its resource-resolution glossary line. Drop the stale bound; keep the contract statement and add the data-plane status note.

## Not changed

`mock/crates/hilavitkutin-providers/DESIGN.md.tmpl` mentions ResourceSnapshot only in a second-cell table description (AdaptWu consumes it); the concept survives, so no edit is needed there.
