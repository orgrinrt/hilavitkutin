# Source changelist — resource-snapshot disambiguation

Round: 202605282256. Phase: IMPL. Deletes the placeholder `ResourceSnapshot<const N>` and its sibling scaffolding; aligns one doc comment to the locked `'phase` lifetime. No replacement type ships in this round: the typed phase-bounded view lands with the data plane (HILA-RUNTIME-C6), where the resource arena gives it real storage to view.

The placeholder has no engine call site (grep finds only its definition, the `ResourceCache` wrapper, the `resource/mod.rs` re-exports, and one round-trip test). Per the pre-1.0 no-legacy-shims rule it is deleted outright. The dispatch-time stack-caching idea its module doc described is recorded in `hilavitkutin/BACKLOG.md.tmpl` against its real home (dispatch codegen).

## CHANGE: file `mock/crates/hilavitkutin/src/resource/snapshot.rs` REMOVED
Removes `struct ResourceSnapshot<const N>` and `struct Slot`.
Verification: `! test -e mock/crates/hilavitkutin/src/resource/snapshot.rs`.

## CHANGE: file `mock/crates/hilavitkutin/src/resource/cache.rs` REMOVED
Removes `struct ResourceCache<'a, R, const N>` (wrapper over the placeholder; no engine consumer).
Verification: `! test -e mock/crates/hilavitkutin/src/resource/cache.rs`.

## CHANGE: file `mock/crates/hilavitkutin/tests/resource_snapshot.rs` REMOVED
Removes the placeholder round-trip test.
Verification: `! test -e mock/crates/hilavitkutin/tests/resource_snapshot.rs`.

## CHANGE: module `resource` FROM `declares + re-exports snapshot + cache` TO `declares + re-exports accumulator + provenance only`
File: `mock/crates/hilavitkutin/src/resource/mod.rs`
Drops `pub mod snapshot;`, `pub mod cache;`, `pub use snapshot::{ResourceSnapshot, Slot};`, `pub use cache::ResourceCache;`. Keeps `accumulator` and `provenance`. Updates the module doc to drop the stack-local-snapshot + typed-cache mention.
Verification: `! grep -q "mod snapshot\|mod cache\|ResourceCache\|ResourceSnapshot\|::Slot" mock/crates/hilavitkutin/src/resource/mod.rs` and `cargo check` green for the `hilavitkutin` crate.

## CHANGE: doc comment `WorkUnit::Ctx` snapshot lifetime FROM `ResourceSnapshot<'frame, R>` TO `ResourceSnapshot<'phase, R>`
File: `mock/crates/hilavitkutin-api/src/work_unit.rs:53`
The `Ctx` GAT carries `Pin<&'frame PoolFrame>` (frame lifetime) and the narrower per-phase `ResourceSnapshot<'phase, R>` view (phase lifetime). The comment conflated the two; align it to the locked Topic 8 axis A lifetime.
Verification: `grep -q "ResourceSnapshot<'phase, R>" mock/crates/hilavitkutin-api/src/work_unit.rs`.

## Lock criteria
- `cargo check` green across the mock workspace.
- No remaining reference to `ResourceSnapshot`, `ResourceCache`, or the snapshot `Slot` in `hilavitkutin` source (`grep`).
- `#335` resolved: one meaning for `ResourceSnapshot` (the typed phase-bounded view, shipping in HILA-RUNTIME-C6); placeholder gone.
