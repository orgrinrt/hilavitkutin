# Doc changelist: GATE-2 R4c round B2, threaded run_parallel

**Date:** 2026-06-07
**Phase:** DOC
**Scope:** no `*.md.tmpl` edits

## Rationale

`run_parallel`'s body + signature change and the new private engine fields /
helpers are runtime-internal. The public design surface describes the parallel
executor at the contract level (WorkUnit / scheduler / ThreadPool provider),
which is unchanged: `ThreadPoolApi` is the existing contract, and `run_parallel`
remains the parallel entry. The `Pin<&mut Self>` receiver is a runtime ergonomics
detail of the persistent-pool lifecycle. No `*.md.tmpl` edits.

## No documentation edits

No `*.md.tmpl` files change in this changelist.
