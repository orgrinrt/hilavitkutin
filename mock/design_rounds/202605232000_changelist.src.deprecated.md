# Src changelist — round 202605232000 (runtime megaround post-review fixes)

**Date:** 2026-05-23
**Phase:** SRC
**Scope:** Three source-state corrections against the prior round's locked claims, plus the test-tombstone curation. Each `## CHANGE:` carries the verification command per `cl-claim-sketch-discipline.md`.
**Source topics:** `202605232000_topic.runtime-megaround-post-review-fixes.md`.
**Prior round:** `202605101036` (locked, closed). This round honours its locked claims by bringing source to match.

## CHANGE: `mock/crates/hilavitkutin/src/scheduler/result.rs` DELETED

File: `mock/crates/hilavitkutin/src/scheduler/result.rs`
Topic A1. The prior round's Pass 6 specified the file as deleted; the file remained on disk as orphaned dead code. This change deletes it.
Verification: `! test -f mock/crates/hilavitkutin/src/scheduler/result.rs` returns successful negative.

## CHANGE: `Scheduler::plan_dirty` array dimension FROM `[AtomicBool; 256]` TO `[AtomicBool; Cfg::MAX_PLAN_AFFECTING_RESOURCES.0]`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Topic A2. The hardcoded 256 defeats the `Scheduler<Cfg: RunCfg>` typestate. Lifting to `Cfg::MAX_PLAN_AFFECTING_RESOURCES.0` (the inner `usize` extracted from the `arvo::USize` trait constant) under `feature(generic_const_exprs)` restores the per-Cfg typestate guarantee.
Verification: `grep 'plan_dirty: \[AtomicBool; Cfg::MAX_PLAN_AFFECTING_RESOURCES.0\]' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: `feature(generic_const_exprs)` enabled in `hilavitkutin/src/lib.rs`

File: `mock/crates/hilavitkutin/src/lib.rs`
Topic A2 support. Required for the const-generic array dimension to resolve at type-check time.
Verification: `grep 'feature(generic_const_exprs)' mock/crates/hilavitkutin/src/lib.rs` returns one match.

## CHANGE: `AdaptCtxPlaceholder` renamed to `AdaptCtxUnimplementedStub` with explicit dual-unreachability rustdoc

File: `mock/crates/hilavitkutin-providers/src/adapt_wu.rs`
Topic A3. The placeholder Ctx is publicly shipped; its `Stub` provider's `unimplemented!()` no-ops are footgun-shaped if a consumer ever reaches the executor's invocation of AdaptWu. The footgun is unreachable today because `Scheduler::run` is itself `unimplemented!()`, but the rustdoc obscures this. Rename to `AdaptCtxUnimplementedStub` and document the dual-unreachability invariant in the rustdoc of `AdaptWu`.
Verification: `grep 'pub struct AdaptCtxUnimplementedStub' mock/crates/hilavitkutin-providers/src/adapt_wu.rs` returns one match.

## CHANGE: drop three tombstone test files

Files: `mock/crates/hilavitkutin/tests/{pipeline_result,adapt_config,thread_pool}.rs`
The three files were stubbed in the prior round as placeholders commemorating retired API surfaces (PipelineResult, AdaptConfig+AdaptMetrics, ThreadPool::new(2-arg)). Per the no-legacy-shims rule, the audit trail is git log + locked CL; the tombstone files are not stubs for forthcoming tests and should not ship.
Verification: `! test -f mock/crates/hilavitkutin/tests/pipeline_result.rs && ! test -f mock/crates/hilavitkutin/tests/adapt_config.rs && ! test -f mock/crates/hilavitkutin/tests/thread_pool.rs` returns successful triple-negative.

## Lock criteria

1. `cargo +nightly check --workspace --tests` clean.
2. `cargo mock` lint gate green.
3. All five `## CHANGE:` verification commands return their stated result.
4. PR #75 body amended to cite this round's resolution of the reviewer's three blockers + three test-file tombstones.
