**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291830 (#626 scope GCE gates, hilavitkutin side)
**Source topic:** 202605291830_topic.scope-gce-gates.md

## CHANGE: remove unused `#![feature(generic_const_exprs)]` from hilavitkutin-api crate root
File: `mock/crates/hilavitkutin-api/src/lib.rs:16`
The crate writes no GCE-requiring construct (no `cap_size`, no const-expr array length, no
`where [(); N]:`); GCE does not propagate from consumed arvo types. Confirmed unused by removing
the gate and rebuilding.
Verification: `! grep "feature(generic_const_exprs)" mock/crates/hilavitkutin-api/src/lib.rs`; `cargo +nightly-2026-05-28 check --workspace --all-targets` clean.

## CHANGE: remove unused `#![feature(generic_const_exprs)]` from hilavitkutin-str crate root
File: `mock/crates/hilavitkutin-str/src/lib.rs:10`
Same: no GCE-requiring construct in the crate. Confirmed unused by rebuild.
Verification: `! grep "feature(generic_const_exprs)" mock/crates/hilavitkutin-str/src/lib.rs`; `cargo +nightly-2026-05-28 check --workspace --all-targets` clean.

OUTCOME: the empirical rebuild confirmed BOTH gates unused (workspace check clean without
them). No gate needed retaining, so no SAFETY comment was added on the hilavitkutin side
(scoping removed both). The arvo side of #626 (12 gates) is a separate round.

Pre-existing-failure note: `cargo test --workspace` has one red binary independent of this
round, the `hilavitkutin-extensions-macros` trybuild (missing `tests/fixtures/fail/*.stderr`
goldens since PR #73, unrelated to GCE; tracked as #632). All other test binaries pass with
the gates removed, and the workspace `cargo check --all-targets` is clean.
