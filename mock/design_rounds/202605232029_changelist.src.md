# Src changelist — round 202605232029 (deferrals PR review polish)

**Date:** 2026-05-23
**Phase:** SRC
**Scope:** Four polish items from `pr-reviewer-senior` audit on PR #76. No design changes; doc honesty + small assertions + README cross-reference.
**Source topics:** `202605232029_topic.deferrals-pr-review-polish.md`.

## CHANGE: trybuild fixture docstring honesty

File: `mock/crates/hilavitkutin-api/tests/trybuild/double_derive_runcfg.rs`
Reviewer A1. Replace the "exercises the same shape" overclaim with honest framing: the fixture is a harness check (proves trybuild is wired and the workspace can capture compile-error goldens). The real derive-macro fixture lands when the derive macros ship.
Verification: `grep 'harness check' mock/crates/hilavitkutin-api/tests/trybuild/double_derive_runcfg.rs` returns one match.

## CHANGE: `ema_update` stub-status note + slice-length assertion

File: `mock/crates/hilavitkutin-providers/src/adapt_ema.rs`
Reviewer A2 + A3. Add `debug_assert_eq!(dst.len(), src.len())` at the top of `ema_update` so unequal slices surface in dev. Extend the public doc-comment with a "STUB: body lands in Pass-7-or-later" note so a consumer integrating today is not surprised by silent no-op behaviour.
Verification: `grep 'STUB:' mock/crates/hilavitkutin-providers/src/adapt_ema.rs` returns at least one match.

## CHANGE: README pointer to MIRI.md

File: `README.md`
Reviewer A4. Add a one-line pointer to `MIRI.md` in the contributor section (or create one). Repo-root placement is greppable but unadvertised; one cross-reference makes it discoverable.
Verification: `grep 'MIRI.md' README.md` returns at least one match.

## Lock criteria

1. `cargo +nightly check --workspace --tests` clean.
2. `cargo mock` lint gate green.
3. All three `## CHANGE:` verification commands return their stated result.
