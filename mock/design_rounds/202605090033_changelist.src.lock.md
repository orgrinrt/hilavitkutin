---
round: 202605090033
phase: DRAFT
scope: hilavitkutin-extensions
---

# Src CL — descriptor v1.1 review-fix

Two source edits closing reviewer findings F2 and F3 from the PR #70
audit on round 202605090011. Both are documentation-only changes
inside source files; no behavioural diff.

## Changes

### CHANGE: file `mock/crates/hilavitkutin-extensions/tests/descriptor_validation.rs` header comment FROM (stale "pub(crate) + cfg(test) hook" claim) TO (accurate "pub fn called directly" description)

File: `mock/crates/hilavitkutin-extensions/tests/descriptor_validation.rs`
Verification: `grep -c "pub(crate)" mock/crates/hilavitkutin-extensions/tests/descriptor_validation.rs` returns 0; `grep "validate_descriptor.* part of the public surface" mock/crates/hilavitkutin-extensions/tests/descriptor_validation.rs` returns one match.

### CHANGE: fn `Extension::capability` GAINS inline cast-safety comment ABOVE the `capabilities_len as usize` line

File: `mock/crates/hilavitkutin-extensions/src/extension.rs`
Verification: `grep "cast cannot truncate" mock/crates/hilavitkutin-extensions/src/extension.rs` returns one match. The comment text mentions both `validate_descriptor` and `MAX_DESCRIPTOR_LIST_LEN` so a future reader has the soundness chain explicit.

## Lock criteria

- Both `## CHANGE:` blocks above verify against current source.
- `cargo check -p hilavitkutin-extensions -p hilavitkutin-extensions-macros` green.
- `cargo test -p hilavitkutin-extensions` green (no test additions or removals).
- `cargo mock --lint-only --commit` green.

## Cross-references

- Topic: `202605090033_topic.descriptor-v1-1-review-fix.md`.
- Source round: `202605090011/`.
- Tracked task: #398 (CL wording reconciliation).
