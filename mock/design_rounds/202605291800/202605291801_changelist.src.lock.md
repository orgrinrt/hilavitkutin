**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291800 (#627 remove associated_type_defaults)
**Source topic:** 202605291800_topic.remove-associated-type-defaults.md

## CHANGE: `BuilderInput::Init` FROM `type Init = Self;` TO `type Init;`
File: `mock/crates/hilavitkutin-api/src/builder_input.rs:61`
Remove the associated-type default. Every `impl BuilderInput` in the workspace (22 sites,
including the two `macro_rules!`-emitted ones) already declares `type Init` explicitly, so no
impl spell-outs are added. Refresh the three doc comments in the same file (module doc, trait
doc, and the `Init` field doc) that describe the now-removed default.
Verification: `grep -n "type Init;" mock/crates/hilavitkutin-api/src/builder_input.rs` returns the trait declaration; `! grep "type Init = Self" mock/crates/hilavitkutin-api/src/builder_input.rs` (the default is gone); `cargo +nightly-2026-05-28 check --workspace` passes (no impl was relying on the default).

## CHANGE: remove `#![feature(associated_type_defaults)]` from the hilavitkutin-api crate root
File: `mock/crates/hilavitkutin-api/src/lib.rs:14`
The `BuilderInput::Init` default was the crate's only use of the feature.
Verification: `! grep "feature(associated_type_defaults)" mock/crates/hilavitkutin-api/src/lib.rs`; `cargo +nightly-2026-05-28 test --workspace` passes (no other associated-type default in the crate).
