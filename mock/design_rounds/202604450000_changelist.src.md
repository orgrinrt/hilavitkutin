**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** hilavitkutin writing-style sweep — Rust doc comments
**Source topics:** 202604450000_topic.writing-style-sweep.md
**Doc CL:** 202604450000_changelist.doc.lock.md

# Comparison to deprecated changelist

`202604450000_changelist.src.deprecated.md` had the same scope
(two lib.rs files). Deprecated because the doc CL missed
hilavitkutin-ctx/BACKLOG.md.tmpl. Unlock → doc CL updated to cover
BACKLOG → re-lock → this new src CL. No source-scope change.

# Source edits

Prose-only reductions in top-of-file `//!` doc comments.

- `mock/crates/hilavitkutin-build/src/lib.rs` — ~52 prose lines,
  21 em-dashes in the header. Convert the 13-item Pragma roster
  from em-dash bullets into a 2-column table (drops 13 em-dashes
  at once). Restructure the layout bullets into prose (drops ~7
  more). Target ≤5.
- `mock/crates/hilavitkutin-ctx/src/lib.rs` — drop the em-dash on
  the header title line (matches arvo-bits / arvo-tensor pattern).

No code changes. No sig changes.

# Validation

- `cargo check --all` clean.
- `cargo mock --lint-only | grep writing-style` — zero
  hilavitkutin warnings.
