# Topic: Correct the Wake Budget Doc Comment to the Re-Examined Evidence

**Date:** 2026-07-19
**Scope:** one doc-comment correction following the wake-policy
re-examination (`202607210100_wake-policy-reexamination.md`).

Op flagged the round-1 wake_policy result as suspicious; the
re-examination confirmed the margins were inflated by per-process
placement variance and that the spin iteration (`spin_loop` = ISB on
aarch64, 9 to 20 ns each) was an order costlier than the budget model
assumed. The ruling survives on reproducibility grounds (park is never
worse, spin never better, across seven policies, three sizes, three
invocations). The `RunCfg::WAKE_SPIN_BUDGET` doc comment cites the
retired point margins and the partial contention story; it updates to
the corrected evidence and the ISB caveat.
