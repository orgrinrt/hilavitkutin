# Src CL: WAKE_SPIN_BUDGET Doc Comment Follows the Re-Examination

**Date:** 2026-07-19
**Round topic:** `202607210110_topic.wake-doc-correction.md`
**Doc CL:** `202607210111_changelist.doc.lock.md`

## CHANGE: `RunCfg::WAKE_SPIN_BUDGET` doc comment FROM round-1 margins TO re-examined evidence

File: `mock/crates/hilavitkutin-api/src/run_cfg.rs`
The comment drops the retired 1.64x / 1.34x / 1.11x point margins and
the contention-only mechanism story, and states the corrected basis:
park is never worse and spin never better across seven policies, three
sizes, three invocations (record `202607210100`); `spin_loop` is ISB on
aarch64 at 9 to 20 ns per iteration, so any non-zero tuning must drop
the hint or budget in wall-time. Default stays 0.
Verification: `grep -c "202607210100" mock/crates/hilavitkutin-api/src/run_cfg.rs` returns 1; `grep -c "1.64x" mock/crates/hilavitkutin-api/src/run_cfg.rs` returns 0; `cargo test -p hilavitkutin-api` green.
