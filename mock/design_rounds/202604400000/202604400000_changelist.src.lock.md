**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** engine-scheduler-sweep source CL

# Per-file

## `metrics.rs`

Add imports `use arvo::USize;`, `use hilavitkutin_api::Nanos;`,
`use arvo::ufixed::UFixed;`.

- `frame_time_ns: u64` → `frame_time_ns: Nanos`.
- `morsels_dispatched: u64` → `USize`.
- `changes_detected: u64` → `USize`.

`new()` initialisers: `0` → `UFixed::from_raw(0)` for Nanos, `USize(0)`
for USize fields.

Drop `Default` derive; keep Copy/Clone/Debug/(maybe PartialEq/Eq).
Add manual `impl Default for SchedulerMetrics { fn default() -> Self
{ Self::new() } }` — avoids the derive needing USize:Default.

## `mod.rs`

All eight `const MAX_UNITS/STORES/LANES: usize` sites (struct + impl
+ impl Default, on both Scheduler and SchedulerBuilder) carry
trailing inline lint:allow tracked #121.

Note: each `const` sits on its own line in the existing formatting,
so each line must carry the annotation individually.

## `plan.rs`

Add `use arvo::USize;`.

- `LaneAssignment { lane_id: u32, first_unit: u32, unit_count: u32 }`
  → all three → USize.
- `ExecutionPlan { lanes, count: usize }` — `count: usize` → `count:
  USize`.
- Three `const MAX_LANES: usize` sites carry trailing lint:allow.
- `new()` inits `0` → `USize(0)` (four times: three lane fields +
  `count`).

Drop `Default` derive on `LaneAssignment` (USize:Default gap).

# Validation

- `cargo check` clean.
- `cargo mock --lint-only | grep scheduler/` returns nothing.
