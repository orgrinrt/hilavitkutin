**Date:** 2026-04-21
**Phase:** TOPIC
**Scope:** hilavitkutin::scheduler module — #72 slice

# Current state

`scheduler/` has 45 warnings across `metrics.rs`, `mod.rs`, `plan.rs`.
Three types of site:

- **Counts / identifiers**: `lane_id: u32`, `first_unit: u32`,
  `unit_count: u32`, `morsels_dispatched: u64`, `changes_detected:
  u64`, `count: usize` (plan's populated count).
- **Nanos-shaped field**: `frame_time_ns: u64` in SchedulerMetrics
  reuses the api `Nanos` alias.
- **Const-generic array sizes**: eight `const MAX_*: usize` sites
  across Scheduler, SchedulerBuilder, ExecutionPlan (struct + impl
  + impl Default across three types).

# Changes proposed

- Counts / IDs → `USize` (uniform sweep pattern).
- `frame_time_ns: u64` → `Nanos` via `hilavitkutin_api::Nanos`.
- All `const MAX_*: usize` carry trailing inline lint:allow
  `(no-bare-numeric)` `(arvo-types-only)` tracked `#121`.

Derives where applicable stay; drop Hash/Default if arvo USize/Nanos
derives-gap blocks. SchedulerMetrics currently derives `Default`:
`frame_time_ns: Nanos` needs Nanos:Default (UFixed provides Default
via impl; should survive). morsels_dispatched/changes_detected:
USize:Default — that's the same gap as earlier rounds; drop Default
derive and keep `new()` as the zero-init path.

# Validation

- `cargo check` clean.
- `cargo mock --lint-only | grep scheduler/` returns nothing.
