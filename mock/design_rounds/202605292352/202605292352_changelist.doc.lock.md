# Doc changelist: block-diagonal trunk wiring

**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605292352 (C1c, #640)

## Current state

`DESIGN.md.tmpl` plan-stage step 6 reads "Block diagonal + D-M, phase validation plus dead column elimination (arvo-sparse)". Step 5 (`block_diagonalise`) is a stub returning `Bool::TRUE`; `Phase.trunks`, `Phase.trunk_count`, and `Phase.id` are never populated.

## Changes proposed

### `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Refine the block-diagonal step line to state it now detects the connected-component partition and projects per-phase trunk skeletons (one trunk per block, populating `Phase.id` / `Trunk.id` / `trunk_count`), with `Trunk.components` (fibers), the Dulmage-Mendelsohn fine decomposition, dead-column elimination, and the phase-alignment check layered on in later rounds. This reflects the source change landing in the src CL (`block_diagonalise` returns a `BlockPartition`; the runner projects trunk skeletons). Backtick only types that exist in source.
