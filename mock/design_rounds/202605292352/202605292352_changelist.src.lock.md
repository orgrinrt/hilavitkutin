# Src changelist: block-diagonal trunk wiring

**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605292352 (C1c, #640)

Wires plan step 5 to arvo's `block_diagonal_via` and projects per-phase trunk skeletons. No alignment check (deliberate deviation from the design note, see topic): distinct blocks are column-disjoint by construction, so `PhaseAlignmentMismatch` has no testable condition before column classification; the variant stays reserved.

## CHANGE: struct `BlockPartition` FROM `absent` TO `pub struct BlockPartition<const MAX_UNITS: Cap> { block_count: USize, block_of_unit: [USize; cap_size(MAX_UNITS)] }`
File: `mock/crates/hilavitkutin/src/plan/trunk.rs`
Verification: `grep -n "pub struct BlockPartition" mock/crates/hilavitkutin/src/plan/trunk.rs` returns one match; re-exported from `plan/mod.rs`.

## CHANGE: fn `block_diagonalise` FROM `(graph, phases) -> Bool returning Bool::TRUE` TO `(graph) -> BlockPartition<MAX_UNITS> wiring block_diagonal_via`
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: `grep -n "block_diagonal_via" mock/crates/hilavitkutin/src/plan/steps.rs` returns one match; the `phases` parameter and `MAX_PHASES` generic are gone from `block_diagonalise`.

## CHANGE: fn `phase_trunk_counts` FROM `absent` TO `(partition, waists, topo, unit_count) -> [USize; cap_size(MAX_PHASES)]`
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: `grep -n "pub fn phase_trunk_counts" mock/crates/hilavitkutin/src/plan/steps.rs` returns one match.

## CHANGE: runner `compute_execution_plan` step 5 FROM `feasible / PhaseAlignmentMismatch path` TO `block_diagonalise + phase_trunk_counts projecting Phase.id / Trunk.id / trunk_count`
File: `mock/crates/hilavitkutin/src/plan/mod.rs`
Verification: `! grep -n "let feasible" mock/crates/hilavitkutin/src/plan/mod.rs`; `grep -n "phase_trunk_counts" mock/crates/hilavitkutin/src/plan/mod.rs` returns one match; `PhaseAlignmentMismatch` is no longer constructed in the runner.

## CHANGE: test `tests/block_diagonalise.rs` FROM `absent` TO `strict block-partition + per-phase trunk-count tests`
File: `mock/crates/hilavitkutin/tests/block_diagonalise.rs`
Verification: `cargo +nightly-2026-05-28 test -p hilavitkutin --test block_diagonalise` passes.

## Doc-comment updates (mechanical, not source-state claims)

`steps.rs` module doc drops the `*Stub*` marker for step 5 and narrows the wired-versus-stub note to "steps 4-5 wired; step 6 (spectral) remains a stub".
