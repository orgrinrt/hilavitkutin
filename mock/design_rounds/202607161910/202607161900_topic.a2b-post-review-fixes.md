# Topic: A2b post-review doc fixes

**Date:** 2026-07-16
**Phase:** TOPIC
**Scope:** two reviewer nits from the PR #167 review (round `202607161810`)

The reviewer's pass on the A2b loop inversion returned merge-after-fixes with
two documentation findings and no logic findings:

1. `mock/benches/src/bin/asm_gate.rs`: the `FIXTURE_NAMES` array grew from
   four to six entries but its doc comment and the `nm`-collection comment
   still say "four". Update both counts.
2. `mock/crates/hilavitkutin/DESIGN.md.tmpl`: the rewritten `Scheduler::run`
   paragraph reads as missing a verb after the parenthetical ("retained at
   registration (see the scheduler builder section) phase-outer over ...").
   Rephrase so the adverbial attaches cleanly.

Both are prose corrections with no source-behaviour change.
