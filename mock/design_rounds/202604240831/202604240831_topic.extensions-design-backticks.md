**Date:** 2026-04-24
**Phase:** TOPIC
**Scope:** `hilavitkutin-extensions/DESIGN.md.tmpl` backtick expansion (doc cleanup follow-up to round 202604240745)

# Context

Round 202604240745 shipped the hilavitkutin-extensions crate with source + tests. During that round the DESIGN.md.tmpl was intentionally authored in prose-only form to avoid the `design-doc-source-mismatch` lint firing during DOC phase before source existed. The src CL (S12) planned to expand backticks against shipped source, but doc templates freeze after doc CL lock, so S12 could not land in that round.

The `undocumented-type` lint (build-gate severity) fires on every public item in `src/lib.rs` because the prose descriptions in DESIGN.md.tmpl never name the items by backticked identifier. This is a push blocker.

This round addresses exactly that: add backticked type names to DESIGN.md.tmpl for every public item so the lint can verify matched declarations. No source changes. The round is a small doc-only cleanup that closes out the hilavitkutin-extensions feature cleanly.

# Changes proposed

1. Add a "Public items" section to DESIGN.md.tmpl that lists each public item with a backticked name: `Extension`, `Symbol`, `ExtensionSymbol`, `ExtensionError`, `IncompatibilityError`, `compatibility_check`.
2. Sprinkle backticks through the existing prose where natural (e.g. "The `Extension` struct owns…", "`Symbol<'ext, T>` is the typed handle…").
3. Keep all existing prose sections intact — "Arbitrary-time linking invariant", "Non-scope", "Cross-references" are unchanged except for backtick insertion.

# Out of scope

- Source changes of any kind.
- BACKLOG.md.tmpl updates (the v1 scope cut on static-data symbol access is captured in the src CL of the previous round and can be surfaced in a later round if needed).
- New public items. The public surface is frozen at what round 202604240745 shipped.

# Validation

- `undocumented-type` lint clears after the doc CL applies.
- `design-doc-source-mismatch` still passes (every backticked name corresponds to a shipped source item).
- `cargo mock --lint-only --strict` passes — push unblocks.
