**Date:** 2026-04-24
**Phase:** DOC
**Scope:** `hilavitkutin-extensions/DESIGN.md.tmpl` backtick expansion

**Source topics:**
- 202604240831_topic.extensions-design-backticks.md

# Purpose

Add backticked type names to `mock/crates/hilavitkutin-extensions/DESIGN.md.tmpl` so the `undocumented-type` lint can verify matched declarations against shipped source from round 202604240745. This is purely doc presentation; the shipping contract is unchanged.

# Deliverables

## D1 — update `mock/crates/hilavitkutin-extensions/DESIGN.md.tmpl`

Edit the existing DESIGN.md.tmpl to:

1. Backtick the five public types and one free function where they appear in prose: `Extension`, `Symbol`, `ExtensionSymbol`, `ExtensionError`, `IncompatibilityError`, `compatibility_check`.
2. Add a short "Public items" section near the top of the file (after "Shape") that lists the six identifiers explicitly with a one-line description each — gives the lint a stable anchor and gives human readers a scan-friendly index.
3. Preserve every existing section (opening prose, "Architectural position", "Dependencies", "Platform coverage", "Arbitrary-time linking invariant", "Non-scope", "Cross-references", "Shipping commitment") exactly as-is, except for backtick insertions inside the prose where natural.

# Deliberately NOT changed

- `BACKLOG.md.tmpl` — no backlog items added or removed in this round.
- `README.md.tmpl` — the 3-10 line terse summary is already correct.
- `mock/DESIGN.md.tmpl` top-level crate table — unchanged.
- Agent rule templates — unchanged.
- `mockspace.toml` — unchanged.

# Validation

- `cargo mock --lint-only --commit` passes.
- `cargo mock --lint-only --strict` passes — this is the whole point; this lint gate must unblock push.
- `undocumented-type` lint clears.
- `design-doc-source-mismatch` stays green (every backticked name exists in source).
- Writing-style lint green.

# Ordering

1. D1 — single commit, edit DESIGN.md.tmpl.
2. `cargo mock lock` → SRC-PLAN. Create empty src CL (no source changes this round).
3. `cargo mock lock` → DONE.
4. `cargo mock close` → archive to 202604240831/.
