**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** hilavitkutin writing-style sweep — Rust doc comments
**Source topics:** 202604450000_topic.writing-style-sweep.md
**Doc CL:** 202604450000_changelist.doc.lock.md

# Source edits

Prose-only reductions in top-of-file `//!` doc comments.

- `mock/crates/hilavitkutin-build/src/lib.rs` — ~52 prose lines in
  the `//!` header, 21 em-dashes. Reduce to ≤5 by replacing
  em-dashes with periods / commas / parens / colons.
- `mock/crates/hilavitkutin-ctx/src/lib.rs` — drop the 2
  exclamation marks that appear in prose (not inside macro tokens
  like `define_providers!`).

No code changes. No signature changes.

# Validation

- `cargo check --all` clean.
- `cargo mock --lint-only | grep writing-style` — zero
  hilavitkutin warnings.
