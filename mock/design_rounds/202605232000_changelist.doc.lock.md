# Doc changelist — round 202605232000 (runtime megaround post-review fixes)

**Date:** 2026-05-23
**Phase:** DOC
**Scope:** No template-side changes. The prior round's locked DESIGN entries describe the intended runtime behaviour correctly; this round corrects source-state drift only. Rustdoc updates ship through the src CL because rustdoc lives in `.rs` source, not `.md.tmpl` templates.
**Source topics:** `202605232000_topic.runtime-megaround-post-review-fixes.md`.

## Order of operations

1. No `.md.tmpl` edits.
2. `cargo mock lock` to freeze the doc CL.
3. Open src CL with the three source-state fixes.

## Mock root templates

No changes.

## Per-crate `DESIGN.md.tmpl` updates

None. The prior round's DESIGN entries for `Scheduler<Cfg: RunCfg>`, `PipelineResult` retirement, and `AdaptWu` already describe the intended state; this round's src changes bring source to match.

## Per-crate `BACKLOG.md.tmpl` updates

None. The deferred work (test-suite curation, trybuild fixture, MIRI.md, EMA sample-shape upgrade) is recorded in the topic file's "Out of scope" section and tracked via subsequent rounds, not via BACKLOG promotion.

## Why this doc CL is empty

The reviewer's findings are about source-state drift from the prior round's locked claims, not about a design rewrite. The prior round locked the intended design; this round corrects the source to honour those locked claims. A doc CL that re-authored the DESIGN entries here would either duplicate the prior round (already locked) or relitigate (forbidden under "Locked decisions" framing).

The cleanest discipline: the doc CL records that no doc-side change is required, the src CL carries the substance, and the round closes when source matches the prior round's locked claims.
