# Src CL: PR #145 review fixes

**Round:** 202606200400
**Phase:** IMPL

## CHANGE: update stale saturation comments in accum_dispatch.rs
File: `mock/crates/hilavitkutin/tests/accum_dispatch.rs:128-135`
The `CAP_SMALL` comment and the `SaturateWu` doc block describe the removed silent-saturation contract; rewrite to the panic contract (the append past the capacity panics).
Verification: `! grep -nE "dropped \(saturated\)|buffer never overruns|appends must be dropped" mock/crates/hilavitkutin/tests/accum_dispatch.rs`.

## CHANGE: note the parallel-path panic failure mode at the run_parallel capture
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs` (~1792, near the change_seen_count capture)
A one-line comment that an over-append panic on a worker thread can stall the join; acceptable for a contract violation.
Verification: `grep -n "worker thread" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns a match near the parallel fold.
