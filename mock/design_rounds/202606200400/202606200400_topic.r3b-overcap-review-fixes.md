# Topic: PR #145 review fixes

**Date:** 2026-06-20
**Crate:** hilavitkutin
**Tracked:** #691, PR #145 review

## Fixes

PR #145 review (pr-reviewer-senior) surfaced three points; all cleanup, code sound:

1. `accum_dispatch.rs` had stale comments above `CAP_SMALL` and `SaturateWu`
   still describing the removed silent-saturation contract ("overflow appends
   must be dropped", "dropped (saturated)", "buffer never overruns"). Update them
   to the panic contract.
2. A comment at `scheduler/mod.rs` ~1792 noting the parallel-path failure mode:
   an over-append now panics on a worker thread, which can stall the join rather
   than cleanly abort. Acceptable for a contract violation; documented.
3. AUDIT-TRAIL CORRECTION for the locked src CL of round 202606200300: its
   "CHANGE: ADD over-append should_panic test to r3b_change_class_count.rs (runs 3
   tests)" misdescribed the shipped edit. What actually shipped: the over-capacity
   should_panic test is a REWRITE of the pre-existing `accum_append_saturates_at_
   reserved_capacity` test in `accum_dispatch.rs` (renamed to
   `accum_append_panics_past_reserved_capacity`), which previously asserted the
   silent-saturation contract; `r3b_change_class_count.rs` received the PARALLEL
   change_seen_count test, not the over-append test. The locked CL is frozen
   (no amend); this note is the correction so a future reader is not misled, and
   so the green-test rewrite (the strict-by-design sensitive case) is recorded.
