**Date:** 2026-06-06
**Phase:** DOC
**Round:** 202606061730 (gate1-dispatch-topo-registration-gate)

# Doc changelist: build-time topological-registration gate

## Current state

`mock/crates/hilavitkutin/DESIGN.md.tmpl` already documents the provisional
producer-before-consumer registration constraint (lines 633 to 636: "build()
requires the registration order be a valid topological order of the dependency
DAG, returning a BuildError that names the recommended order otherwise; this
producer-before-consumer constraint is provisional"). It does not yet distinguish
the two distinct build-error paths: a dependency cycle (acyclicity failure,
already surfaced as `BuildError::PlanFailed`) versus an acyclic-but-anti-topological
registration (the carrier order is a valid DAG but not a topological order of it).
The build narrative around line 654 to 658 names only the cycle path
(`BuildError::PlanFailed`).

## Changes proposed

One file. One sentence added, no removals.

### File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Distinguish the two build-error paths in the plan-stage build narrative (the
paragraph at lines 654 to 658). After the existing sentence about a dependency
cycle returning `BuildError::PlanFailed`, add a sentence naming the
acyclic-but-anti-topological path: an acyclic registration whose carrier order
runs a reader before its writer returns
`BuildError::NonTopologicalRegistration`, distinct from the cycle path, because
the static dispatch walk follows carrier order directly (no runtime permutation
once the D1e walk rewrite lands), so an anti-topological carrier would run a
reader before its writer. This is the same provisional constraint already
described at lines 633 to 636, stated at the error-path level.

The `BuildError::NonTopologicalRegistration` variant lands as a pub enum variant
in this round's src CL, so the backticked name resolves under the
`design-doc-source-mismatch` lint (same shape as the existing
`BuildError::PlanFailed` reference at line 658).

## Lock criteria

- The added sentence names `BuildError::NonTopologicalRegistration` and ties it
  to the carrier-order-is-dispatch-order rationale.
- No em-dashes, vocabulary canonical.
- The provisional framing at lines 633 to 636 is unchanged (the new sentence is
  the error-path restatement, not a duplicate of the constraint description).
