# Doc changelist: GATE-2 phase-sectioned single-core run re-point (round 2b)

**Round:** 202606071300 (topic: gate2-phase-sectioned-run)
**Phase:** DOC
**Scope:** `mock/crates/hilavitkutin/BACKLOG.md.tmpl` only.

## Why no DESIGN.md.tmpl change this round

`Scheduler::run` gains an inferred grouping-witness generic and dispatches the morsel-outer path phase by phase, but the consumer contract is unchanged: the call shape stays `scheduler.run()` (the new generic infers like the existing projection witness), and single-core output is identical (degenerate waist barriers are no-ops; phases run in topological order either way). The trunk/phase/waist model becomes consumer-observable at G2-N (real parallelism); the DESIGN prose lands then. This round records progress in BACKLOG, matching rounds 1 and 2a.

## CHANGE: extend the GATE-2 BACKLOG note with phase-sectioned dispatch

File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Append to the existing GATE-2 section that round 2b re-points the scheduler run path to dispatch phase by phase (each phase's units run before the next, the waist sectioning), single-core sequential with degenerate barriers, reusing the round-1 grouping (phase assignment) and the existing gated walk. Names stay unbackticked per BACKLOG convention.
