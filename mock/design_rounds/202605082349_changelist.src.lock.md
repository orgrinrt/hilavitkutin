**Date:** 2026-05-08
**Phase:** DRAFT
**Round:** 202605082349
**Source topic:** `202605082349_topic_builder_api_rename.md`

# Source CL: SchedulerBuilder method renames

## Scope

Direct rename of seven methods on `SchedulerBuilder` in `mock/crates/hilavitkutin/src/scheduler/mod.rs`, plus call-site updates and string-literal updates in diagnostic notes / doc comments across the workspace. Per `no-legacy-shims-pre-1.0.md`, no aliases.

## CHANGE: `add` RENAMED TO `add_unit` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace the inherent method `pub fn add<W: WorkUnit>(self) -> ...` with `pub fn add_unit<W: WorkUnit>(self) -> ...`. Body and signature otherwise unchanged. Surrounding doc comment updates `Register a WU type` to name the new identifier if it does. Verbose by name only.

Verification:
- `! grep -nE '^\s*pub fn add<W: WorkUnit>' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns no match.
- `grep -nE '^\s*pub fn add_unit<W: WorkUnit>' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: `resource` RENAMED TO `add_resource` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace `pub fn resource<T: 'static>(self, _initial: T) -> ...` with `pub fn add_resource<T: 'static>(self, _initial: T) -> ...`.

Verification:
- `grep -nE '^\s*pub fn add_resource<T' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.
- `! grep -nE '^\s*pub fn resource<T' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns no match.

## CHANGE: `resource_default` RENAMED TO `add_resource_default` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace `pub fn resource_default<T: Default + 'static>(self) -> ...` with `pub fn add_resource_default<T: Default + 'static>(self) -> ...`.

Verification:
- `grep -nE '^\s*pub fn add_resource_default<T' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: `column` RENAMED TO `add_column` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace `pub fn column<T: 'static>(self) -> ...` with `pub fn add_column<T: 'static>(self) -> ...`.

Verification:
- `grep -nE '^\s*pub fn add_column<T' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: `memory` RENAMED TO `with_memory` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace `pub fn memory<M: ...>(self, _provider: M) -> Self` with `pub fn with_memory<M: ...>(self, _provider: M) -> Self`.

Verification:
- `grep -nE '^\s*pub fn with_memory<' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: `threads` RENAMED TO `with_threads` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace `pub fn threads<P: ...>(self, _pool: P) -> Self` with `pub fn with_threads<P: ...>(self, _pool: P) -> Self`.

Verification:
- `grep -nE '^\s*pub fn with_threads<' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: `clock` RENAMED TO `with_clock` in `scheduler/mod.rs`

File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

Replace `pub fn clock<C: ...>(self, _clock: C) -> Self` with `pub fn with_clock<C: ...>(self, _clock: C) -> Self`.

Verification:
- `grep -nE '^\s*pub fn with_clock<' mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: call sites in `mock/crates/hilavitkutin/tests/scheduler_builder.rs` UPDATE to renamed methods

Replace every `.add::<W>()`, `.resource(...)`, `.column::<T>()` invocation with the renamed form. `.add_virtual::<T>()` and `.add_kit::<K>()` stay as-is. Test file imports and structure otherwise unchanged.

Verification:
- `! grep -nE '\.add::<|\.resource\(|\.column::<' mock/crates/hilavitkutin/tests/scheduler_builder.rs` returns no match (preserving `.add_virtual::<` / `.add_kit::<` / `.add_resource(` / `.add_unit::<` / `.add_column::<`).
- `cargo test -p hilavitkutin --test scheduler_builder` passes the same set as before.

## CHANGE: call site in `mock/crates/hilavitkutin-providers/tests/smoke.rs` UNCHANGED

The smoke test calls only `.add_kit::<...>()` (unchanged). No edits.

## CHANGE: doc comments in `mock/crates/hilavitkutin-providers/src/lib.rs`, `src/interner.rs` UPDATE

Replace `builder.resource(...)` mentions with `builder.add_resource(...)`.

Verification:
- `grep -nF 'builder.add_resource(' mock/crates/hilavitkutin-providers/src/lib.rs mock/crates/hilavitkutin-providers/src/interner.rs` returns matches at the previously identified sites.
- `! grep -nF 'builder.resource(' mock/crates/hilavitkutin-providers/src/lib.rs mock/crates/hilavitkutin-providers/src/interner.rs` returns no matches.

## CHANGE: diagnostic notes in `mock/crates/hilavitkutin-api/src/access.rs` UPDATE

The `Contains<S>` and `ContainsAll<L>` notes mention `.resource::<T>(initial)` and `.column::<T>()`. Update both to the renamed forms.

Verification:
- `grep -F '.add_resource::<T>(initial)' mock/crates/hilavitkutin-api/src/access.rs` returns two matches (one in each diagnostic).
- `grep -F '.add_column::<T>()' mock/crates/hilavitkutin-api/src/access.rs` returns two matches.
- `! grep -E '\.resource::<T>\(initial\)|\.column::<T>\(\)' mock/crates/hilavitkutin-api/src/access.rs` returns no matches.

## CHANGE: diagnostic note in `mock/crates/hilavitkutin-api/src/work_unit.rs` UPDATE

The `WorkUnitBundle` note mentions `.add::<W>()`. Update to `.add_unit::<W>()`.

Verification:
- `grep -F '.add_unit::<W>()' mock/crates/hilavitkutin-api/src/work_unit.rs` returns one match.

## CHANGE: diagnostic note in `mock/crates/hilavitkutin-api/src/store.rs` UPDATE

The `StoreBundle` note mentions `.resource::<T>(initial)`, `.column::<T>()`, `.add_virtual::<T>()`. Update the first two to renamed forms; `.add_virtual::<T>()` stays.

Verification:
- `grep -F '.add_resource::<T>(initial)' mock/crates/hilavitkutin-api/src/store.rs` returns one match.
- `grep -F '.add_column::<T>()' mock/crates/hilavitkutin-api/src/store.rs` returns one match.

## CHANGE: doc comment in `mock/crates/hilavitkutin-kit/src/lib.rs` UPDATE

The module doc and trait diagnostic both mention `.add_kit::<K>()` (unchanged). No edits required to identifiers; if any prose mentions other renamed methods, update.

Verification:
- `grep -nE '\.add::<|\.resource\(|\.column::<' mock/crates/hilavitkutin-kit/src/lib.rs` returns no matches.

## CHANGE: doc comments in `mock/crates/hilavitkutin-providers/src/interner.rs` (already covered above)

Listed as separate file above; counted once in source CL scope.

## Lock criteria

- All seven inherent methods renamed in `scheduler/mod.rs`.
- All call sites in `scheduler_builder.rs` use renamed methods.
- All doc comments / diagnostic notes across api, kit, providers reference renamed names.
- `cargo check --workspace` passes clean.
- `cargo test --workspace` passes the same set as before this round.
- `! grep -rEn '\.add::|\.resource\(|\.resource_default\(|\.column::<|\.memory\(|\.threads\(|\.clock\(' mock/crates/hilavitkutin*/src/ mock/crates/hilavitkutin*/tests/ --include='*.rs'` returns no matches in builder-method position. The `ctx.memory()` / `ctx.threads()` / `ctx.clock()` accessor calls in `mock/crates/hilavitkutin-persistence/{src/,tests/}` are different traits (`HasMemoryProvider::memory(&self) -> &Self::Provider` etc) and stay unchanged.
