**Date:** 2026-05-08
**Phase:** DOC
**Round:** 202605082344
**Source topic:** `202605082344_topic_hint_diagnostic_marker_lists.md`

# Doc CL: no documentation edits

## Scope

This round corrects four diagnostic `note` fields in source. The corresponding template documentation does not enumerate per-axis markers (the round 202605082321 doc CL added a `### Diagnostic coverage across the public surface` subsection that names which traits carry diagnostics, not which markers each note enumerates), so no template edits are required.

The src CL of this round carries the four `note = ...` corrections as structured `## CHANGE:` blocks per `cl-claim-sketch-discipline.md`. Each block grep-verifies both the corrected text AND the actual implementor set in the same file.

## Files touched in this CL

None.

## Lock criteria

- This file locks immediately; no template edits exist that require the doc-edit window.
