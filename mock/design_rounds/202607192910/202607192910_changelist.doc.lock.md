# Doc changelist: remove the paragraphs describing the replaced mechanisms

**Date:** 2026-07-19
**Phase:** DOC
**Topic:** `202607192900_topic.delete-superseded-dispatch-and-barrier.md`

Two template passages describe mechanisms r4 replaced. They go with the code, because a design
document that still explains the losing side of a supersession is drift regardless of whether the
code behind it compiles.

## CHANGE: `hilavitkutin/DESIGN.md.tmpl` nested-carrier paragraph

`:222-232` describes the `RunPipeline -> RunPhase -> RunTrunk -> RunFiber` nest, its `TrunkCons` /
`PhaseCons` carriers, and the waist arrival between phases, ending with the promise that
`phase_barrier_arrive` is "wired in a following round".

The whole passage goes. What replaced it, the const-eval grouping with const-gated DCE over a flat
carrier, is already described elsewhere in the same document, so nothing is lost by removing the
account of the mechanism that lost.

The `phase_barrier_arrive` promise goes with it rather than being retargeted. Retargeting would keep
a second barrier protocol alive as a plan.

## CHANGE: `mock/DESIGN.md.tmpl` head+tail row, if it names the nest

Checked during the round: the domain table's `dispatch` row describes the flattener generally and
does not name `RunPhase` or `RunPipeline`, so no change is needed there. Recorded so a later reader
knows it was checked rather than missed.

## Not in scope

The `core_mask` prose, which describes a shipped mechanism.
