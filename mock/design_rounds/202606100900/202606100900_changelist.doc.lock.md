**Date:** 2026-06-10
**Phase:** DOC
**Round:** 202606100900 (accum-per-frame-reset)

# Doc changelist: document the per-frame accumulator reset in the run contract

## Current state

`mock/crates/hilavitkutin/DESIGN.md.tmpl` lines 701-711 describe the `Accum<T>` drain arm and the `run` dispatch contract. It states the live-length "starts at zero and a WorkUnit advances it by appending during the frame", that an append saturates at the reserved capacity, and that `run` then dispatches the retained units, projecting each `EngineCtx` and calling `execute` per morsel. It does not state what happens to the live-length at the start of a reused frame.

Line 470 already states the governing model: "Schedule reuse across frames (construct once, reuse)." The drain text (701-711) describes the live-length as build-time-zeroed but is silent on per-frame behaviour, leaving the reuse-model implication (the live-length must reset each frame) unstated. Source matches the silence: `run` never resets it, so a reused frame appends from the prior frame's offset. The design oracle says reuse; the missing reset is the gap.

## Changes proposed

One edit to `DESIGN.md.tmpl`, in the accumulator drain / run-contract paragraph (around lines 705-708). After the sentence ending "so it never runs past the reserved buffer.", add a sentence stating that, under the schedule-once-reuse model, `run` resets every accumulator live-length to zero at frame start via a type-level `ResetAccumulators` walk over the bindings, so each frame appends into a fresh buffer rather than continuing from the prior frame's live offset. The existing following sentence ("`run` then dispatches the retained units...") is unchanged.

This documents the frame-lifecycle reset as part of the run contract, naming the `ResetAccumulators` type so the design doc references a real source type the src changelist adds.

## Lock criteria

- The DESIGN.md.tmpl accumulator paragraph names the per-frame reset and the `ResetAccumulators` walk.
- `ResetAccumulators` is added in the src changelist, so the new design-doc reference resolves to a real source type (no `design-doc-source-mismatch`).
- No em-dashes; writing-style clean; vocabulary canonical.
