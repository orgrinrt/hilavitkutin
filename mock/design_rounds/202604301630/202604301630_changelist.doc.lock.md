# Doc CL: arvo-refit adoption + #290 lint:allow sweep

**Date:** 2026-04-30
**Round:** 202604301630
**Phase:** DOC

This round is source-side mechanical: dep rename in
`Cargo.toml`s, two `use arvo_narrow::*` to `use arvo_refit::*`
import flips, and four `lint:allow tracked: #290` annotations
removed (with the underlying call sites rewritten to use the new
arvo `Widen<T>` and `BitsRefitCtor` machinery).

No doc-template content needs editing. Hilavitkutin's per-crate
`mock/crates/*/{README,DESIGN,BACKLOG}.md.tmpl` files do not
reference `arvo-narrow` anywhere; they speak in terms of arvo
trait surfaces (`Narrow<T>`, `Widen<T>`) which keep their names.
The `mock/PRINCIPLES.md.tmpl` and `mock/DESIGN.md.tmpl`
top-level templates do not mention the arvo crate-level dep
either. A workspace-wide grep confirms zero live references to
`arvo-narrow` or `arvo_narrow` outside the existing source-side
sites this round will rewrite.

## Files

No `*.md.tmpl` edits in this round.

## Out of scope

- The src CL adopts the new arvo API at four sites and renames
  the crate dep entries; nothing here is doc-template work.
- Future polish rounds may consolidate hilavitkutin onto the arvo
  facade re-exports (`use arvo::Narrow;` instead of `use arvo_refit::Narrow;`),
  but that is a separate decision tracked outside this round.

## Comparison to deprecated changelist

None. Fresh round.
