**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** hilavitkutin-str primitive-vocabulary sweep (#72)
**Source topics:** 202604430000_topic.str-sweep.md
**Doc CL:** 202604430000_changelist.doc.lock.md (no doc changes)

# Files touched

## mock/crates/hilavitkutin-str/Cargo.toml

Add `arvo.workspace = true` under `[dependencies]` so handle.rs can
import `arvo::Bool`.

## mock/crates/hilavitkutin-str/src/hash.rs

- Line 7 `pub const FNV_OFFSET: u64 = ..;` — trailing allow, reason
  "FNV-1a 64-bit algorithm constant; hash width fixed by algorithm;
  tracked: #72".
- Line 10 `pub const FNV_PRIME: u64 = ..;` — same.
- Line 13 `pub const fn const_fnv1a(s: &str) -> u64 {` — allow
  (no-bare-numeric, arvo-types-only, no-bare-string), reason
  "FNV-1a input/output boundary; byte-addressable &str; u64 hash
  width; tracked: #72".
- Line 18 `hash ^= bytes[i] as u64;` — allow (no-bare-numeric,
  arvo-types-only), reason "FNV-1a step cast to algorithm width;
  tracked: #72".

## mock/crates/hilavitkutin-str/src/handle.rs

- Import `use arvo::newtype::Bool;` (or `use arvo::Bool;` if
  re-exported in arvo's prelude).
- Line 55 `pub const fn is_const(self) -> bool {` → `-> Bool {`.
  Body `self.0.origin().bits() == 0` → `Bool(self.0.origin().bits() == 0)`.
- Line 60 `pub const fn is_runtime(self) -> bool {` → `-> Bool {`.
  Body `!self.is_const()` → `Bool(!self.is_const().0)`.

## mock/crates/hilavitkutin-str/src/interner.rs

- Line 14 `fn arena_intern(&self, s: &str) -> u32;` — trailing allow
  (no-bare-numeric, arvo-types-only, no-bare-string), reason
  "interner boundary — &str is the input string the arena wraps;
  u32 is the 28-bit id width; tracked: #72".
- Line 16 `fn arena_resolve(&self, id: u32) -> &str;` — same.
- Line 38 `pub fn intern(&self, s: &str) -> Str {` — allow with
  (no-bare-string), reason "interner boundary — incoming &str;
  tracked: #72".
- Line 43 `Str::__runtime((id as u64).into())` — allow
  (no-bare-numeric, arvo-types-only), reason "u32 id widened to u64
  for `Bits<28>::from`; tracked: #72".
- Line 53 same allow as line 43.
- Line 64 `pub fn resolve(&self, s: Str) -> Option<&str> {` — allow
  (no-bare-string), reason "interner boundary — resolved &str;
  tracked: #72".
- Line 65 `if s.is_const() { ... }` → `if s.is_const().0 { ... }`
  — propagate the handle.rs Bool-return flip.
- Line 68 `self.arena.arena_resolve(s.id().bits() as u32)` — allow
  (no-bare-numeric, arvo-types-only), reason "u32 cast from Bits
  back to id width for arena resolve; tracked: #72".
- Line 75 `fn lookup_const_by_value(s: &str) -> Option<Str> {` —
  allow (no-bare-string), reason "interner-internal &str math;
  mirrors boundary width; tracked: #72".
- Line 96 `fn str_eq(a: &str, b: &str) -> bool {` — allow
  (no-bare-numeric, arvo-types-only, no-bare-string), reason
  "interner-internal &str equality helper; returns bare bool
  because it is below the API boundary; tracked: #72".

## mock/crates/hilavitkutin-str/src/section.rs

- Line 35 `let len = stop.offset_from(start) as usize;` — allow
  (no-bare-numeric, arvo-types-only), reason "slice length from
  raw pointer `offset_from`; host-width usize required by
  `core::slice::from_raw_parts`; tracked: #72".
- Line 63 same allow as line 35.

# Validation

- `cargo check -p hilavitkutin-str` clean.
- `cargo mock --lint-only | grep hilavitkutin-str | grep -vE "writing-style|no-empty-crate"` returns zero.
