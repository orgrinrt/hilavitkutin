# Src CL: P1 R2a keystone test

**Round:** 202606191900
**Phase:** IMPL

## CHANGE: ADD test `tests/r2a_keystone_ctxfor.rs`
File: `mock/crates/hilavitkutin/tests/r2a_keystone_ctxfor.rs`
A nine-`Resource` Read access set + `Accum`/`Virtual` Write on an
`OnMeta<ScheduleEnd>` WU whose `Ctx = CtxFor<'frame, Read9, Write,
OnMeta<ScheduleEnd>>` (the merged alias, not hand-spelled). Registers the nine
resources + accum + the WU, runs a frame, asserts the execute read a resource
value through the projected Ctx and appended it. Proves the nine-resource CtxFor
projection compiles + dispatches (the P1 keystone). Modelled on
`gate2_meta_metrics.rs`.
Verification: `cargo test -p hilavitkutin --test r2a_keystone_ctxfor` passes.
