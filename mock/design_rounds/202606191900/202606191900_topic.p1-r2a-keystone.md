# Topic: P1 R2a keystone — nine-resource CtxFor Ctx through OnMeta<ScheduleEnd>

**Date:** 2026-06-19
**Crate:** hilavitkutin (engine test)
**Tracked:** #341 (P1 adapt), chart-the-path phase 10 keystone sketch

## Hypothesis

An `OnMeta<ScheduleEnd>` WorkUnit whose `Read` is a nine-`Resource` access set
and whose `Write` carries an `Accum` plus a `Virtual`, with its `Ctx` computed by
the merged `CtxFor` alias (not hand-spelled), compiles and dispatches on a real
scheduler frame, and its `execute` reads a resource and fires the virtual through
the projected Ctx accessors. This is the keystone the whole P1 adapt arc depends
on (AdaptWu carries nine metrics Resources + one anomaly Virtual). The unproven
part is whether the projection machinery closes all nine index-witness chains at
once under the real trait solver (the granularity pass flagged trait-solver depth
/ GCE normalization as the risk). The OnMeta dispatch band itself is proven
(`gate2_meta_metrics.rs` EndWu), so this isolates the access-set-width question.

## Proof vehicle

A test `tests/r2a_keystone_ctxfor.rs`, modelled on `gate2_meta_metrics.rs`:
register nine `Resource` singletons + an `Accum`, register a WU with the
nine-resource Read and `Ctx = CtxFor<'frame, Read9, Write, OnMeta<ScheduleEnd>>`,
run a frame, assert the execute read one resource's value and appended it (the
fire is observable via the resource read landing). Green = WORKS (R2a proven +
landed as a regression test). A trait-solver-depth compile failure = the keystone
FAILS finding, which is the routine's AskUserQuestion course-correction point.
