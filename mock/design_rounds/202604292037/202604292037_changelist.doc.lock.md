# Changelist (doc): hilavitkutin arvo Round D adoption

**Date:** 2026-04-29
**Phase:** DOC
**Source topics:** `202604292037_topic.hilavitkutin-arvo-round-d-adoption.md`

## Status

DOC phase. No doc-template edits planned.

## Doc-template footprint

Zero. The seven reshaped arvo surfaces (the `arvo::newtype` import path, `IBits(N)` / `FBits(N)` literal forms, `arvo_bits::bitfield!` macro path, `Bits::new` constructor, `Bits::bits()` accessor, `Bits<N, S>: From<u64>` coercion, `ContentHash` cascade) do not appear in any `mock/crates/*/DESIGN.md.tmpl`, `mock/crates/*/BACKLOG.md.tmpl`, `mock/PRINCIPLES.md.tmpl`, `mock/WORKFLOW.md.tmpl`, or any `mock/agent/` template. Verified at topic time by grep against the dev tree.

The hilavitkutin DESIGN docs reference arvo types by their public surface names (`Bool`, `Cap`, `USize`, the various `UFixed<I, F, S>` aliases, `ContentHash`) without spelling the import path or the construction form. The const-generic positions in DESIGN docs use the type-alias names (`Urgency` for the underlying `UFixed<{ ibits(2) }, { fbits(0) }, Hot>`), not the literal-construction form. Both styles remain accurate after Round D; the docs already match the post-Round-D shape because they avoid the spellings that Round D changed.

## Affected files

None.

## Comparison to deprecated changelist

The abandoned round 202604291932 had the same doc-side footprint (zero edits); the docs were correct at that time too. Nothing to compare on the doc side.

## Verification

`grep -rn 'arvo::newtype' mock/crates/*/DESIGN.md.tmpl mock/crates/*/BACKLOG.md.tmpl mock/PRINCIPLES.md.tmpl mock/WORKFLOW.md.tmpl mock/agent/` returns zero results.

`grep -rnE 'IBits\([0-9]+\)|FBits\([0-9]+\)|Width\([0-9]+\)|FBits::ZERO|IBits::ZERO' mock/crates/*/DESIGN.md.tmpl mock/crates/*/BACKLOG.md.tmpl mock/PRINCIPLES.md.tmpl mock/WORKFLOW.md.tmpl mock/agent/` returns zero results.

`grep -rn 'arvo_bits::bitfield\|Bits::new\|Bits::bits' mock/crates/*/DESIGN.md.tmpl mock/crates/*/BACKLOG.md.tmpl mock/PRINCIPLES.md.tmpl mock/WORKFLOW.md.tmpl mock/agent/` returns zero results.

The src CL captures the source-side migration; nothing crosses into doc-template scope.

## Next phase

`cargo mock lock` on this CL to enter SRC-PLAN phase. The src CL will then carry the actual mechanical sweep across all five hilavitkutin crates plus the lock bump.
