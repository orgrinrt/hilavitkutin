**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291450 (enable notko try_trait_v2)

Build-config: enable notko's `try_trait_v2` feature so notko's core `Try` / `Residual` impls for `Maybe` / `Outcome` / `Just` compile in hilavitkutin's build (exercising the previously-dormant pathway). Pull the merged notko + arvo dev tips into the lockfile.

## CHANGE: notko dep features FROM `["const"]` TO `["const", "try_trait_v2"]`
File: `mock/Cargo.toml`
notko's `try_trait_v2` cargo feature transitively enables its `try_trait_v2_residual` rustc gate, so notko's fixed `Try` + `Residual` impls compile.
Verification: the notko line in `mock/Cargo.toml` carries `features = ["const", "try_trait_v2"]`; `cargo +nightly-2026-05-28 test --workspace --no-run` builds clean.

## CHANGE: Cargo.lock notko + arvo pins advanced to merged dev fixes
File: `mock/Cargo.lock`
`cargo update -p notko -p arvo` pulls notko c2e5b19 (the Residual adaptation) and arvo 5776b61 (the Bool fix).
Verification: `cargo +nightly-2026-05-28 check --workspace` clean.
