**Date:** 2026-05-29
**Phase:** TOPIC
**Scope:** enable notko's `try_trait_v2` feature in the hilavitkutin workspace, alongside the nightly-2026-05-28 toolchain pin already on this branch.
**Source:** workspace decision to enable notko `try_trait_v2` across consumers (so notko's `?`-on-`Maybe`/`Outcome`/`Just` impls compile in consumer builds and the previously-dormant pathway is exercised); notko PR #23 (merged) + arvo #69 (merged) landed the adaptation upstream.

hilavitkutin has no `core::ops::Try` impls of its own, so the recent core `Try::Residual: Residual<Self::Output>` bound change did not break hilavitkutin directly; its earlier build failure was purely transitive through arvo, now fixed on arvo dev. With fixed arvo + notko pulled, the full hilavitkutin workspace (lib + all test targets) compiles clean on `nightly-2026-05-28`.

## Decision

Enable notko's `try_trait_v2` feature in `mock/Cargo.toml`: `features = ["const"]` becomes `["const", "try_trait_v2"]`. notko's `try_trait_v2` cargo feature transitively enables its `try_trait_v2_residual` rustc gate, so notko's fixed `Try` + `Residual` impls compile inside hilavitkutin's build. Pull the merged notko + arvo dev tips into the lockfile.

## Consequences

- Cargo.toml is a SRC-phase (gated) change in mockspace, so it lands via the src CL.
- The `rust-toolchain.toml` pin to `nightly-2026-05-28` is already committed on this branch (not gated); the PR bundles both.
- No source-type changes; hilavitkutin compiles clean on the pin with no drift. This round is build-config only.
