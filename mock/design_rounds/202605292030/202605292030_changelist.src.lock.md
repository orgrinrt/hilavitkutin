**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605292030 drop unused adt_const_params gate from cap-lift integration tests

# Source changelist

## CHANGE: remove unused `adt_const_params` gate FROM `6 integration test crate roots` TO `generic_const_exprs only`
Files: `mock/crates/hilavitkutin/tests/{core_assignment,plan_inputs,plan_runner,plan_types,synthesise_core_programs,dispatch_types}.rs`
Remove the `#![feature(adt_const_params)]` crate-root attribute from each of the
six integration test files, keeping `#![feature(generic_const_exprs)]` and
`#![allow(incomplete_features)]`. These crates name `Cap` as a const type
argument but never declare a `Cap` const-generic parameter, so `adt_const_params`
is unused there (rustc emits `feature adt_const_params is declared but not used`).
Correct the accompanying comment block to state that the consuming crate needs
only `generic_const_exprs`, and that `adt_const_params` is required only where a
`Cap` const-generic parameter is declared (the engine crate root). `src/lib.rs`
is unchanged: it declares the `Cap` const params and keeps `adt_const_params`.
Verification: `grep -rl "feature(adt_const_params)" mock/crates/hilavitkutin/tests/` returns nothing; `grep -c "feature(adt_const_params)" mock/crates/hilavitkutin/src/lib.rs` returns 1; `cargo +nightly-2026-05-28 test -p hilavitkutin --manifest-path mock/Cargo.toml` is green (90 tests) with no `adt_const_params ... declared but not used` warning.
