# Doc changelist: hilavitkutin-sym in the root design overview

**Round:** 202607242000
**Phase:** DOC
**Scope:** `mock/DESIGN.md.tmpl` only. Doc-only round; no source changes.

## `mock/DESIGN.md.tmpl`

- Crate-layout table: add a `hilavitkutin-sym` row before the `hilavitkutin-str` row, describing it as the generic interned-identity core (`Sym` handle, domain traits, `Interner`, `Generator`; ecosystem, standalone) that `hilavitkutin-str` builds on.
- Dependency-graph prose: add `-sym` to the ecosystem-crate list and state that `hilavitkutin-str` depends on `hilavitkutin-sym` for the identity handle, while `hilavitkutin-sym` depends only on the foundations.

## Not in this changelist

No source changes. The crate and its consumers already shipped in round 202607241915.
