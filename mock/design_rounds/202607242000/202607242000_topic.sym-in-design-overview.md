# Topic: list hilavitkutin-sym in the root design overview

**Date:** 2026-07-24
**Phase:** TOPIC
**Scope:** add the `hilavitkutin-sym` row to the hand-authored crate-layout table in `mock/DESIGN.md.tmpl`, and name it in the dependency-graph prose. Doc-only completeness follow-up to the sym-interning round (202607241915), which shipped the crate but missed the root design overview's hand-maintained crate list.

The prior round factored the `hilavitkutin-sym` generic interned-identity core and made `hilavitkutin-str` a consumer of it. The per-crate design (`HILAVITKUTIN_SYM_OVERVIEW.md`), the structure index, the dependency graph, and the agent roster all pick the crate up automatically. The root `DESIGN.md`'s "Crate layout" table and its "Dependency graph" prose are hand-authored, so they do not, and the new crate is absent from the design overview a reader lands on first. This round closes that gap.

## Current state

`mock/DESIGN.md.tmpl` carries a hand-authored "Crate layout" table with one row per crate and a "Dependency graph" prose paragraph naming the ecosystem crates (`hilavitkutin-ctx, -persistence, -str, -kit, -providers`). Neither mentions `hilavitkutin-sym`, though it now ships as the identity core beneath `hilavitkutin-str`.

## Changes proposed

Add a `hilavitkutin-sym` row to the crate-layout table, placed before the `hilavitkutin-str` row (the foundation reads before its consumer): "Generic interned-identity core: the `Sym` handle, domain traits, `Interner`, `Generator` (ecosystem, standalone). `hilavitkutin-str` builds on it." Update the dependency-graph prose to include `-sym` in the ecosystem-crate list and note that `hilavitkutin-str` depends on `hilavitkutin-sym` for the identity handle, while `hilavitkutin-sym` depends only on the foundations (arvo, notko).
