**Date:** 2026-05-25
**Phase:** SRC
**Scope:** Scheduler::run no-op body source CL. One source edit on `scheduler/mod.rs` replacing the `unimplemented!()` body with `Cfg::Out::default()` under a `where Cfg::Out: Default` bound. One test update on `tests/scheduler_run.rs` exercising the no-op return.
**Source TOPIC:** `mock/design_rounds/202605250628_topic.hila-scheduler-run-body.md`.
**Source DOC:** `mock/design_rounds/202605250628_changelist.doc.lock.md`.

# Source changes

## CHANGE: `Scheduler::run` body FROM `unimplemented!()` TO `Cfg::Out::default()` in scheduler/mod.rs
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `! grep -A 3 'pub fn run' mock/crates/hilavitkutin/src/scheduler/mod.rs | grep -q 'unimplemented!' && grep -q 'Cfg::Out::default' mock/crates/hilavitkutin/src/scheduler/mod.rs`.

The `unimplemented!()` body at scheduler/mod.rs:140 is replaced with `Cfg::Out::default()`. The `impl<Cfg: RunCfg> Scheduler<Cfg>` block gains `where Cfg::Out: Default`. The doc comment updates to reflect the transitional no-op state per the DOC CL. `DefaultRunCfg::Out = notko::Outcome<(), ()>` satisfies the bound after notko PR #21 (merged) added `impl<T: Default, E> Default for Outcome<T, E>`.

## CHANGE: `_scheduler_run_validated_in_followup` test updated to exercise the no-op return
File: `mock/crates/hilavitkutin/tests/scheduler_run.rs`
Verification: `grep -q 'fn scheduler_run_returns_default_outcome' mock/crates/hilavitkutin/tests/scheduler_run.rs`.

The empty placeholder test renames to `scheduler_run_returns_default_outcome` and gains a body: builds a default-configured `Scheduler` via `Scheduler::default()`, calls `.run()`, asserts the returned value is `Outcome::Ok(())`. The test pins the no-op contract: a Scheduler with no WUs and the default RunCfg returns the success-default value without panic. Real-runtime behaviour testing waits for the `Scheduler::run real morsel loop body` BACKLOG entry.

# Lock criteria

- Both `## CHANGE:` verification predicates pass.
- `cargo build` clean.
- `scheduler_run_returns_default_outcome` test passes.
- `cargo test --workspace` clean modulo pre-existing trybuild flake.
- `! grep "todo!\|unimplemented!" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns no matches.
