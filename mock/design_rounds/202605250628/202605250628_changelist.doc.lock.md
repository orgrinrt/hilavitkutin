**Date:** 2026-05-25
**Phase:** DOC
**Scope:** Scheduler::run no-op body DOC CL. Adds a DESIGN section and BACKLOG entry. Notes the `Cfg::Out: Default` bound + the notko PR #21 prerequisite (already merged).
**Source TOPIC:** `mock/design_rounds/202605250628_topic.hila-scheduler-run-body.md`.

# Doc changes

## CHANGE: doc section `## Scheduler::run no-op transitional body` ADDED to DESIGN.md.tmpl
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "^## Scheduler::run no-op transitional body" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

New DESIGN section under Scheduler builder + meta. Documents the transitional `Scheduler::run` body that returns `Cfg::Out::default()` instead of panicking. Names the prerequisite (notko `Default for Outcome`, merged as notko PR #21), the bound (`where Cfg::Out: Default` on the Scheduler impl block), and the constraint impact (only `DefaultRunCfg` is a real source-level consumer; its `Out = Outcome<(), ()>` is Default after notko PR #21). Names what the no-op body does NOT do (no morsel loop, no per-core dispatch walk, no adapt metric flow, no persistence snapshot drain) and which BACKLOG entry covers the real body.

## CHANGE: BACKLOG entry `Scheduler::run real morsel loop body` ADDED to BACKLOG.md.tmpl
File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Verification: `grep -q "Scheduler::run real morsel loop body" mock/crates/hilavitkutin/BACKLOG.md.tmpl`.

New entry naming the real-body follow-up: wire the per-core dispatch programs from `codegen_core`, walk the morsel loop, handle adapt subsystem metrics, fire the meta-virtual `PlanStage → ScheduleReady → PassStart → ScheduleEnd` boundaries at the right phase + waist transitions, drain `ResourceSnapshots` through persistence. Sequencing: depends on `codegen_fiber + codegen_core LLVM-driven monomorphisation` (the C2 follow-up entry) landing first; without monomorphised bodies, the morsel loop has nothing to dispatch.

# Verification target

After the round ships: `cargo build` clean. `scheduler_run` integration test passes calling `.run()` and getting `Ok(())`. No `todo!()` or `unimplemented!()` in `scheduler/mod.rs`. viola-cli's `scheduler.run()` (per Slice 8b.2) no longer panics at runtime.
