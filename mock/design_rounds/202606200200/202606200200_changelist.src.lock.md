# Src CL: R3b change_seen_count parallel/fused

**Round:** 202606200200
**Phase:** IMPL

## CHANGE: increment change_seen_count in run_parallel fold
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs` (~1792 capture, ~1806 fold, via `(*me)`)
Capture `!(*me).store_dirty.get().is_empty().0` before the reset; increment in the unsafe fold block when set.
Verification: `grep -c "change_seen_count.set" mock/crates/hilavitkutin/src/scheduler/mod.rs` == 3.

## CHANGE: increment change_seen_count in run_fused fold
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs` (~2026 capture, ~2033 fold, via `self`)
Same capture-before-reset + increment.

## CHANGE: ADD parallel-path test to r3b_change_class_count.rs
File: `mock/crates/hilavitkutin/tests/r3b_change_class_count.rs`
A second test runs the same WUs through `run_parallel` (multi-core), marks the store dirty, asserts change_seen_count counted through the bridge on the parallel path.
Verification: `cargo test -p hilavitkutin --test r3b_change_class_count` runs 2 tests, both pass.
