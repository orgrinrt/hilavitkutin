# Doc CL: R3b change_seen_count parallel/fused

**Round:** 202606200200
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Remove the single-core qualifier: `change_seen_count` is now maintained in all
three run paths (`run`, `run_parallel`, `run_fused`), so the "do not yet maintain
it" sentence is dropped and the prose states the metric holds on every path.
