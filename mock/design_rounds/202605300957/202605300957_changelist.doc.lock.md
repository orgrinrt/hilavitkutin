# DOC changelist: c2 build-captures-wus

**Date:** 2026-05-30
**Phase:** DOC
**Round:** 202605300957
**Scope:** prose-only edits to the engine crate DESIGN.md.tmpl

This round's doc work is two prose updates to
`mock/crates/hilavitkutin/DESIGN.md.tmpl`. No new table rows: the
slice-2 surface is described in prose, consistent with how slice 1
described the walk. New types land in the src CL; the doc describes
behaviour, not a type catalogue, so the `design-doc-source-mismatch`
lint (table-row checker) stays green at every phase.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

### Edit 1: the sequential-walk paragraph (the slice-1 `RunFiber` paragraph)

Extend the paragraph that introduces `RunFiber` / `run_fiber_walk` to
state that the scheduler now drives the walk from `run()`. Slice 1
described the walk as a standalone entry that a test calls directly;
slice 2 connects it: the builder retains the registered WorkUnit
instances on a value-list, the built `Scheduler` holds that list, and
`run()` walks it over a single full-range morsel. Note the order is
registration-list order this slice (last-registered runs first because
the builder prepends), with dependency-topological order deferred to the
plan-driven slice.

### Edit 2: the scheduler builder section (domain 23)

Update the routing prose that currently claims "workunit and platform
values are dropped." After this round, workunit values are retained on a
parallel WorkUnit-value list (the value-carrying unit cons-list the walk
consumes), while platform and run-config values are still dropped (their
type stays tracked in the typestate). State that `build` carries the
retained WorkUnit-value list into the `Scheduler` alongside the
store-value arena, and that `run` walks it. Note the scope boundary:
resource-only (columns deferred), registration-list order (topological
order deferred), Kit-contributed units not yet retained (a Kit hands the
builder unit types, not instances).

## Verification

`cargo mock check --lint-only` green at doc-commit and at doc-lock:
the edits are prose, add no table rows, and reference only names that
already exist in source (`RunFiber`, `run_fiber_walk`, `Scheduler`,
`Place`, `RouterKind`, `StoreValues`). The new src-side names land in
the src CL.
