# Doc CL: P1 R3c throughput

**Round:** 202606192000
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

One sentence in the SchedulerMetrics/meta section: `SchedulerMetrics` also carries
a `last_record_count` cell (set beside the pass-duration EMA each frame) so an
`OnMeta` hook can derive the throughput trend (records / EMA duration) without new
instrumentation.
