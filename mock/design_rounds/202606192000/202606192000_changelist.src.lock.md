# Src CL: P1 R3c throughput

**Round:** 202606192000
**Phase:** IMPL

## CHANGE: ADD field `last_record_count` to `SchedulerMetrics`
File: `mock/crates/hilavitkutin-api/src/meta.rs:89`
Add `pub last_record_count: Cell<USize>` after `ema_pass_duration_ns`, plus its
`Default` (`Cell::new(USize(0))`).
Verification: `grep "last_record_count" mock/crates/hilavitkutin-api/src/meta.rs` returns 2+ matches (field + default).

## CHANGE: SET `last_record_count` beside the pass-duration EMA fold in all run paths
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs` (~1374 run, ~1794 run_parallel, ~2024 run_fused)
After each `ema_pass_duration_ns` fold, `m.last_record_count.set(<receiver>.record_count)`.
Verification: `grep -c "last_record_count.set" mock/crates/hilavitkutin/src/scheduler/mod.rs` == 3.

## CHANGE: ADD test `tests/r3c_throughput_record_count.rs`
File: `mock/crates/hilavitkutin/tests/r3c_throughput_record_count.rs`
An `OnMeta<ScheduleEnd>` consumer reads `ctx.meta::<SchedulerMetrics>().last_record_count`
and appends it; the test asserts it equals the registered record count after a frame
(the throughput numerator is exposed through the meta bridge).
Verification: `cargo test -p hilavitkutin --test r3c_throughput_record_count` passes.
