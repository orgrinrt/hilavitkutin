**Date:** 2026-05-30
**Phase:** SRC
**Round:** 202605301800 plandims-review-fixes

## Intent

Two cosmetic fixes from the PR #111 review. No behaviour change.

## CHANGE: drop the dead `DefaultSchedulerDims` re-export
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Remove the unused `pub use crate::plan::DefaultPlanDims as DefaultSchedulerDims;` re-export and its doc comment. The `Scheduler`'s `D: PlanDims = DefaultPlanDims` default names `DefaultPlanDims` directly; nothing references the alias.
Verification: `! grep "DefaultSchedulerDims" mock/crates/hilavitkutin/src/scheduler/mod.rs`.

## CHANGE: `ExecutionPlan` `Debug` uses `finish_non_exhaustive`
File: `mock/crates/hilavitkutin/src/plan/mod.rs`
The manual `Debug` for `ExecutionPlan` omits its array fields; switch its `.finish()` to `.finish_non_exhaustive()` to match every sibling manual `Debug` and not misrepresent the struct as fully printed.
Verification: `grep "finish_non_exhaustive" mock/crates/hilavitkutin/src/plan/mod.rs` returns a match in the `ExecutionPlan` Debug impl.
