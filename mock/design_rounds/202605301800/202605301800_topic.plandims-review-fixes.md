**Date:** 2026-05-30
**Phase:** TOPIC
**Scope:** post-review cosmetic fixes for PR #111 (PlanDims adoption), on the same branch.

# Topic

The pr-reviewer-senior pass on the PlanDims adoption (round 202605301730) raised two cosmetic findings, neither affecting correctness:

1. `scheduler/mod.rs` ships a dead `pub use crate::plan::DefaultPlanDims as DefaultSchedulerDims;` re-export. The `Scheduler`'s `D` default names `DefaultPlanDims` directly and nothing references the alias. An unused public alias is drift; drop it (and its doc comment).
2. `ExecutionPlan`'s manual `Debug` impl uses `.finish()` while omitting its array fields, misrepresenting the struct as fully printed. Every sibling manual `Debug` in the migrated modules uses `.finish_non_exhaustive()`. Switch `ExecutionPlan` to match.

Comment/surface-only edits; no behaviour change. Lands on the branch before the PR merges.
