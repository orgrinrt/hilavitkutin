**Date:** 2026-06-06
**Phase:** DOC
**Round:** 202606061830 (gate1-dispatch-recommended-order)

# Doc changelist: recommended order in the build error

## Current state

`mock/crates/hilavitkutin/DESIGN.md.tmpl` states the design target at line 634 ("a
BuildError that names the recommended order") and the D1d-b build narrative now
names `BuildError::NonTopologicalRegistration` and ties it to the
carrier-order-is-dispatch-order rationale. Neither passage states WHAT the
recommended order is (the RCM-reordered topological order) or that the error
carries it.

## Changes proposed

One file. One sentence added, no removals.

### File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

In the D1d-b build-narrative sentence about `BuildError::NonTopologicalRegistration`,
add that the error carries the recommended registration order: the RCM-reordered
topological order (canonical Step 5: the RCM row reordering is the WU execution
order, cache-optimal among valid topological orders), the same order the auto-
ordering relaxation will apply. So the consumer sees both the first offending
carrier-slot pair and the full order that satisfies the gate.

## Lock criteria

- The added sentence states the recommended order is the RCM-reordered topological
  order and that the error carries it.
- No em-dashes, vocabulary canonical.
- The provisional framing is unchanged.
