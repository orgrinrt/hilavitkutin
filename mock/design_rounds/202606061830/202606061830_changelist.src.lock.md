**Date:** 2026-06-06
**Phase:** SRC
**Round:** 202606061830 (gate1-dispatch-recommended-order)

# Src changelist: recommended order in the build error

## Summary

Carry the recommended registration order (the RCM-reordered topological order) in
`BuildError::NonTopologicalRegistration`. New `RecommendedOrder` fixed-capacity
carrier-slot sequence, the variant gains a `recommended` field, `compute_plan`
populates it from the successful plan's `rcm_order`, and the D1d-b rejection test
widens to assert it.

## CHANGE: struct `RecommendedOrder` ADDED
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Detail: `pub struct RecommendedOrder { order: <DefaultPlanDims as PlanDims>::Units as Capacity>::Array<USize>, count: USize }`. A fixed-capacity carrier-slot sequence
sized by the engine's default unit capacity, no allocation. `#[derive(Copy, Clone,
PartialEq, Eq)]` plus a hand-written `Debug` that prints the live slot sequence.
Constructor `from_rcm_order(plan_rcm: &[UnitId], count: USize) -> Self` fills `order`
from the RCM permutation's slot indices; `pub fn as_slice(&self) -> &[USize]`
exposes the live prefix. Doc states it is the RCM-reordered topological order
(canonical Step 5), the cache-optimal recommended registration order.
Verification: `grep "pub struct RecommendedOrder" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: variant `BuildError::NonTopologicalRegistration` GAINS field `recommended: RecommendedOrder`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Detail: the struct variant gains `recommended` alongside `producer` / `consumer`.
Doc updated to state the recommended order is the RCM-reordered topological order.
Verification: `grep -n "recommended: RecommendedOrder" mock/crates/hilavitkutin/src/scheduler/mod.rs` shows the field in the variant and the construction site.

## CHANGE: fn `compute_plan` POPULATES the recommended order from `plan.rcm_order`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Detail: in the `Ok(plan)` arm, on a back-edge build
`RecommendedOrder::from_rcm_order(plan.rcm_order.as_ref(), plan.unit_count)` and pass
it as the `recommended` field. The plan is already in hand at the gate, so no extra
plan computation. Still runs before any allocation.
Verification: `grep -n "from_rcm_order" mock/crates/hilavitkutin/src/scheduler/mod.rs` shows the construction in `compute_plan`.

## CHANGE: test `build_rejects_anti_topological_registration` WIDENED to assert the recommended order
File: `mock/crates/hilavitkutin/tests/scheduler_run_walk.rs`
Detail: the matched variant now binds `recommended`; the test asserts the producer
slot (writer) precedes the consumer slot (reader) in `recommended.as_slice()`, i.e.
the named order is a valid topological order of the chain. Strict: fails if the
recommended order is empty, mis-ordered, or omits a slot.
Verification: `cargo test -p hilavitkutin --test scheduler_run_walk build_rejects_anti_topological_registration` passes.

## Lock criteria

- All four CHANGE verifications pass.
- `cargo test -p hilavitkutin` all-green; the all-green audit confirms the widened
  test exercises the recommended-order contract (fails on empty / mis-ordered).
- No em-dashes, no bare primitives outside existing `lint:allow` patterns,
  vocabulary canonical.
