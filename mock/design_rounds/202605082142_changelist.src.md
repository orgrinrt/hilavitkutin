**Date:** 2026-05-08
**Phase:** DRAFT
**Round:** 202605082142
**Source topic:** `202605082142_topic_access_set_arity_solve.md`

# Source CL: AccessSet legacy-A deletion + LEN fix + macro-form test

## Scope

One source file edited (`mock/crates/hilavitkutin-api/src/access.rs`). One test file rewritten (`mock/crates/hilavitkutin-api/tests/access_set.rs`). No other crate touched. Engine, kit, providers, ctx, persistence, str, extensions, linking, and macros crates compile without source edits because they reference `AccessSet`, `Contains`, `ContainsAll`, `Empty`, `Cons`, `Concat` through stable trait names that survive the deletion unchanged.

## CHANGE: lines 40 to 189 of `access.rs` REMOVED

File: `mock/crates/hilavitkutin-api/src/access.rs`

Deleted block: arity-0 unit-tuple impl, the `impl_access_set!` and `impl_contains!` declarative macros, all macro invocations for arities 1 through 12, and the cons-cell bridge impl `impl<H, R, T> Contains<T> for (H, R) where R: Contains<T>`. Specifically the lines reading from `// Arity 0.` through `impl<H: 'static, R: 'static, T: 'static> Contains<T> for (H, R) where R: Contains<T> {}` plus the section banner comment ending with the cons-list-recursion explanation immediately before `pub struct Empty;`.

Verification: `! grep -nE "impl AccessSet for \(|impl_access_set!|impl_contains!|Contains<T> for \(H, R\)" mock/crates/hilavitkutin-api/src/access.rs` returns no matches.

## CHANGE: `Cons<H, T>::LEN` FROM `USize(0)` TO `USize(T::LEN.0 + 1)`

File: `mock/crates/hilavitkutin-api/src/access.rs:212-214`

Replace the body of `impl<H: 'static, T: 'static + AccessSet> AccessSet for Cons<H, T>` from `const LEN: USize = USize(0);` to `const LEN: USize = USize(T::LEN.0 + 1);`. The `T::LEN.0 + 1` arithmetic is callable in const contexts via the const-arith surface on `arvo::USize` (round 202605021200 layer A).

Verification: `grep -n 'const LEN: USize = USize(T::LEN.0 + 1);' mock/crates/hilavitkutin-api/src/access.rs` returns exactly one match.

## CHANGE: `tests/access_set.rs` REWRITTEN to macro form

File: `mock/crates/hilavitkutin-api/tests/access_set.rs`

The current file body asserts `<(A,) as AccessSet>::LEN == 1` and similar through arity 6 against the legacy flat-tuple impls. Those impls leave the source under the prior change. The test file is rewritten to assert the same arity range against the canonical HList shape, using the `read!` macro to construct the cons-list typestate. The doc-comment header `//! AccessSet::LEN compile-time assertions for arities 0..=6.` updates to name the macro form. Test function names stay (`len_arity_0` through `len_arity_6`).

Verification: after rewrite, `cargo test -p hilavitkutin-api --test access_set` passes seven tests; `! grep '<(.*) as AccessSet>' mock/crates/hilavitkutin-api/tests/access_set.rs` returns no matches.

## CHANGE: `tests/work_unit.rs` and `tests/providers.rs` UNCHANGED

Files: `mock/crates/hilavitkutin-api/tests/work_unit.rs`, `mock/crates/hilavitkutin-api/tests/providers.rs`

Both files were inspected during the topic phase. `work_unit.rs` already uses `read![Column<Pos>, Column<Vel>]` form (macros emit Cons; no flat-tuple AccessSet usage). `providers.rs` uses generic `<R: AccessSet>` bounds in trait impls (works uniformly across the canonical HList impls). No source edits required.

Verification: `cargo test -p hilavitkutin-api` passes the full test suite after the prior changes land.

## CHANGE: downstream crates UNCHANGED

Files: `mock/crates/hilavitkutin/**/*.rs`, `mock/crates/hilavitkutin-kit/**/*.rs`, `mock/crates/hilavitkutin-providers/**/*.rs`, `mock/crates/hilavitkutin-ctx/**/*.rs`, `mock/crates/hilavitkutin-persistence/**/*.rs`, `mock/crates/hilavitkutin-str/**/*.rs`, `mock/crates/hilavitkutin-extensions/**/*.rs`, `mock/crates/hilavitkutin-linking/**/*.rs`, `mock/crates/hilavitkutin-extensions-macros/**/*.rs`

Workspace-wide grep ahead of the round confirmed no consumer of `hilavitkutin-api` references the legacy flat-tuple AccessSet shape concretely. The engine's `SchedulerBuilder<Wus, Stores>` is HList from initialisation. `WorkUnitBundle` and `StoreBundle` impls are recursive HList. Provider accessor traits take `<R: AccessSet>` generic bounds. Kit's `Owned: StoreBundle` is HList. All resolve through the canonical impls preserved by this round.

Verification: `cargo check --workspace` passes after the prior changes land.

## Lock criteria

- All `## CHANGE:` blocks above land verbatim in source.
- `cargo check --workspace` and `cargo test -p hilavitkutin-api` both pass clean.
- No new lint:allow comments. No mockspace.toml severity edits.
- No legacy flat-tuple AccessSet impls remain in source.
- `Cons<H, T>::LEN` returns the cons-list cardinality at any depth.
