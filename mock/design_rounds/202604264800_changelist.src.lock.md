# Src Changelist: hilavitkutin cross-repo integration polish

**Date:** 2026-04-20
**Phase:** SRC
**Scope:** `hilavitkutin` crate (plan module + tests), workspace
`mock/Cargo.toml`, and lint config for cross-repo consumption.
**Doc CL:** 202604264700_changelist.doc.lock.md

## Deliverables

### F1 — rename `plan::inputs::NodeId` → `UnitId`

**Files:**
- `mock/crates/hilavitkutin/src/plan/inputs.rs` — rename the type
  definition + any associated impls (Debug, Default, etc.).
- `mock/crates/hilavitkutin/src/plan/mod.rs` — update the re-export.
- `mock/crates/hilavitkutin/src/plan/graph.rs` — use sites.
- `mock/crates/hilavitkutin/src/plan/fiber.rs` — use sites.
- `mock/crates/hilavitkutin/src/plan/steps.rs` — use sites in the
  12 stubbed step signatures.
- `mock/crates/hilavitkutin/src/plan/column.rs` — if it carries
  per-unit data.
- `mock/crates/hilavitkutin/src/plan/dirty.rs` — if it references
  unit ids.
- `mock/crates/hilavitkutin/src/plan/access.rs` — if it references
  unit ids.
- `mock/crates/hilavitkutin/tests/plan_types.rs` — test assertions
  for the type.
- `mock/crates/hilavitkutin/tests/plan_inputs.rs` — ditto.

Semantic: `UnitId` is a work-unit identifier index, wrapping
`u32`, `#[repr(transparent)]`, `Copy + Eq + Hash`.

### F2 — `default-features = false` on arvo deps

**File:** `mock/Cargo.toml`.

In `[workspace.dependencies]`, append `, default-features = false`
to each of the 7 arvo entries:
- `arvo`
- `arvo-bits`
- `arvo-bitmask`
- `arvo-graph`
- `arvo-sparse`
- `arvo-spectral`
- `arvo-comb`

### F3 — Mask64/Mask256 note in access.rs + dirty.rs

**Files:**
- `mock/crates/hilavitkutin/src/plan/access.rs` — locate the
  "swap for arvo-bitmask" BACKLOG comment and expand with the
  target variant selection rule.
- `mock/crates/hilavitkutin/src/plan/dirty.rs` — same expansion.

The new text names Mask64 for MAX_STORES ≤ 64, Mask256 for
MAX_STORES ≤ 256, const-generic Mask<N> BACKLOG above.

### F4 — forbidden-imports lint for arvo-bits

**File:** `mock/mockspace.toml` (or wherever the forbidden-imports
table lives).

Add an entry under `[[lints.forbidden-imports.scopes]]` (or
equivalent shape — verify by reading neighbouring entries) scoping
to the `hilavitkutin` crate, forbidding any `arvo_bits::` import.
Reason: engine consumes L1 bit contracts via the L2 `arvo-bitmask`
surface; direct L1 coupling bypasses the architecture.

## Tests

Existing 9-test suite in `tests/plan_types.rs` + `tests/plan_inputs.rs`
will keep all assertions green after the rename (one sed-style
change per test file). No new tests required — this is a
rename + metadata polish round.

## Verification

- `cargo mock check` strict-green.
- `grep -rn "\bNodeId\b" mock/crates/hilavitkutin/src/plan/ mock/crates/hilavitkutin/tests/`
  returns no matches after the rename.
- Workspace `Cargo.toml` shows `default-features = false` on
  all 7 arvo deps.
- BACKLOG notes for Mask64/Mask256 present in both `access.rs`
  and `dirty.rs`.
- `cargo mock --lint-only --strict` emits no `forbidden-imports`
  noise on the new entry.

## Deferred to later rounds

All 5 pre-empted issues in Fix 5 of the doc CL live in BACKLOG
for the plan-stage impl round that first surfaces each.
