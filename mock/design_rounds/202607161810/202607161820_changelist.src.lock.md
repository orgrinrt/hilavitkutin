# Src CL: A2b dispatch loop inversion

**Date:** 2026-07-16
**Phase:** IMPL
**Source topic:** `202607161800_topic.a2b-dispatch-loop-inversion.md`
**Doc CL:** `202607161810_changelist.doc.lock.md`

## CHANGE: `Scheduler::run` FROM morsel-outer-shared (uniform `Cfg::MORSEL_SIZE` window, `dispatch_trunks` per window) TO phase-outer / fiber-outer / morsel-inner (meta bands once per frame; consumer fibers windowed by `FiberDispatch.morsel_size`, members = fiber mask & consumer mask & dirty; accumulator fibers whole-range ungated)
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep -n "fiber-outer" mock/crates/hilavitkutin/src/scheduler/mod.rs` matches in the `run` doc; the old `unit_outer ||` global either/or branch and the `dispatch_trunks` calls inside `run` are gone; engine suite green.

## CHANGE: `FiberDispatch::morsel_size` doc FROM "populated but not yet consumed" TO the consumed contract (the dispatch loop windows each `morsel_local` fiber by it; zero falls back to `Cfg::MORSEL_SIZE`)
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep -n "not yet consumed\|Populated but not yet" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns no match at the `FiberDispatch` field or `__fiber_morsel_size` accessor.

## CHANGE: asm-gate fixtures GAIN `asm_gate_windowed_fibers` (two-pipeline chain through the inverted run) and `asm_gate_resource_snapshot` (hot loop reads a snapshot resource scalar)
File: `mock/benches/asm_gate_fixtures/src/lib.rs`, `mock/benches/src/bin/asm_gate.rs`
Verification: `grep -n "asm_gate_windowed_fibers\|asm_gate_resource_snapshot" mock/benches/asm_gate_fixtures/src/lib.rs mock/benches/src/bin/asm_gate.rs` matches in both; `cargo run --bin asm_gate` check 1 passes on the new symbols.

## CHANGE: tests GAIN `a2b_loop_inversion.rs` (per-fiber window counts per blueprint S3; fiber-outer order; mixed-carrier per-fiber skip)
File: `mock/crates/hilavitkutin/tests/a2b_loop_inversion.rs`
Verification: `cargo test --manifest-path mock/Cargo.toml -p hilavitkutin --test a2b_loop_inversion` green.

## CHANGE: bench crates FROM the retired `PtrNil` resource projection TO `SnapNil` (storage-round drift catch)
File: `mock/benches/asm_gate_fixtures/src/lib.rs`, `mock/benches/engine_vs_std/src/{element_wise,branching,wide_parallel,accumulator}.rs`
Verification: `grep -rn "PtrNil\b" mock/benches/ --include="*.rs" | grep -v ColPtrNil | grep -v AccPtrNil` returns nothing; the fixtures crate builds. (The bench crates sit outside the mock workspace build, so the PR #165 rename never reached them; the asm gate's own build is what surfaces the drift, which is an argument for wiring the gate into the standing local gate, tracked #340.)

## Behaviour-delta rehab (expected, from the topic's deltas)

Tests that count dispatch calls per uniform window, count `now_ns` reads per
window, or rely on the mixed-carrier all-ones dispatch rehab to the new
counts. Rehab is mechanical; assertions keep their contracts.
