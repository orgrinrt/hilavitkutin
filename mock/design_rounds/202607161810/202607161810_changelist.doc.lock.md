# Doc CL: A2b dispatch loop inversion

**Date:** 2026-07-16
**Phase:** DOC
**Source topic:** `202607161800_topic.a2b-dispatch-loop-inversion.md`

## Files

### `mock/crates/hilavitkutin/DESIGN.md.tmpl`

1. The `Scheduler::run` dispatch paragraph (the "windowing the record range
   into morsels of `RunCfg::MORSEL_SIZE`" description): rewrite to the
   inverted shape. `run` is phase-outer over the const phase axis; meta
   lifecycle bands dispatch once per frame (run_parallel parity, plan band
   skipped on clean frames); consumer passes walk the fiber descriptors in
   plan order, each `morsel_local` fiber windowing `[0, total)` by its own
   plan-baked `FiberDispatch.morsel_size` (the domain-12 L1 window,
   `Cfg::MORSEL_SIZE` demoted to the zero-window fallback), members selected
   by `fiber_mask & consumer_mask & dirty`; accumulator fibers dispatch once
   whole-range ungated by dirty.
2. The per-fiber nesting paragraph ("The nesting is per-fiber..."): extend
   with the fiber-outer/morsel-inner loop statement (each fiber completes its
   own window sequence before the next fiber; the const phase gate does the
   fiber-to-phase filtering) and the mask-composition sentence.
3. The stale closing sentence "Windowing the record range into multiple
   morsels with micro-morsel sync points (the morsel-loop split) is a later
   round; `run` covers all records in one morsel": replace with the current
   state (per-fiber morsel windows live; micro-morsel sync points remain a
   later round).
4. The scheduler-builder section's `run` sentence ("calling `execute` once
   per morsel as it windows the record range into morsels of
   `RunCfg::MORSEL_SIZE`"): reword to the per-fiber window source.
