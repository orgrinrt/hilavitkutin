# Doc changelist: c2 run-fiber-walk surface

**Round:** 202605300858
**Phase:** DOC
**Scope:** Name the pre-codegen sequential fiber walk in the engine `DESIGN.md.tmpl` dispatch-codegen section. One paragraph added; no existing backtick-named type changes.

## Why a doc-surface change

The dispatch-codegen section of `crates/hilavitkutin/DESIGN.md.tmpl` documents the per-fiber-shape monomorphisation `run_fiber<S: FiberShape>` as the codegen entry point, and the per-WU `EngineCtx` the engine constructs at the dispatch call site. This round adds a new public engine surface that sits below that codegen entry: a pure-generics walk over a fiber's typed WorkUnit sequence that constructs each unit's `EngineCtx` and runs `execute`, with no codegen or LLVM. Because `DESIGN.md.tmpl` is the shipping contract and the `design-doc-source-mismatch` lint expects every backtick-named type in it to exist in source, the new public `RunFiber` walk surface is named in the document so the contract and the source stay aligned.

## CHANGE plan

The doc-surface edit is one added paragraph in the dispatch-codegen section. The source changes are recorded in the src changelist for this round under the structured `## CHANGE:` grammar.

## CHANGE: ADD run-fiber-walk paragraph to dispatch-codegen section
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Add a paragraph after the per-fiber-shape monomorphisation bullet (or in the codegen-stub section) naming the pre-codegen sequential walk: `RunFiber`, the value cells `WuCons` / `WuNil`, and the free `run_fiber_walk` entry. Frame it as the pure-Rust-generics dispatch core that constructs each unit's `EngineCtx` and runs `execute`, distinct from the LLVM-monomorphised `run_fiber<S: FiberShape>` codegen path. Note the resource-only boundary for the first slice.
Verification: `grep "RunFiber" mock/crates/hilavitkutin/DESIGN.md.tmpl` returns at least one match.
