# Topic: HILA-RUNTIME-C2 select_approach reviewer fixes

**Date:** 2026-05-25
**Phase:** TOPIC
**Scope:** hilavitkutin engine `dispatch/mod.rs::select_approach` post-review fixes for PR #81.

## Current state

Round 202605250600 closed with the base three-branch heuristic shipped. PR #81 opened. `pr-reviewer-senior` returned four findings:

1. RULES: `.0` unpacking on `USize` at the engine surface. Reviewer recommends typed `USize` thresholds + `PartialOrd` comparison so the bare-primitive literal lives at one declaration site, not three call sites plus three tests.
2. DRIFT: the `codegen_core` `lint:allow` is a drive-by addition outside the C2 scope. Should land in its own round (or be reverted here).
3. TESTS: boundary coverage single-sided. Add `9_999 -> TrunkMega/IndirectPerFiber` and `fiber_count = 0` cases.
4. RULES: minor wording nit on the BACKLOG entry's "bench-validated" reference.

`USize` does implement `PartialOrd` (verified at `arvo/mock/crates/arvo-storage/src/platform.rs:228`), so the typed-comparison path is available today.

## Changes proposed

Three CHANGE blocks address findings 1-3. Finding 4 absorbed in the BACKLOG entry's wording while it gets touched.

1. Add typed `USize` constants `SCHEDULE_MEGA_THRESHOLD = USize(10_000)` and `SINGLE_FIBER_CUTOVER = USize(1)`. Rewrite `select_approach` body to compare typed-to-typed: `if record_count >= SCHEDULE_MEGA_THRESHOLD { ... } else if fiber_count <= SINGLE_FIBER_CUTOVER { ... }`. The bare-primitive literal lives only inside the two const declarations; the function body and tests stop unpacking `.0`.

2. Revert the `codegen_core` `lint:allow` drive-by added in Round 1. It is unrelated to the C2 surface and inherits the pre-existing lint surface state.

3. Add two tests: `threshold_boundary_below_picks_single_fiber_path` checks `9_999, 1` returns `TrunkMega`; `zero_fiber_count_picks_trunk_mega` checks `1_000, 0` returns `TrunkMega`.

## Lock criteria

- `cargo build` clean.
- `cargo test -p hilavitkutin select_approach` returns six tests passing (four existing + two new).
- `cargo test --workspace` clean modulo pre-existing trybuild flake.
- The function body and tests contain zero `.0` field accesses on `record_count` or `fiber_count`.
