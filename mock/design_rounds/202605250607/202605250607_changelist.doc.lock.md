**Date:** 2026-05-25
**Phase:** DOC
**Scope:** HILA-RUNTIME-C2 select_approach reviewer-fix DOC CL. Refines the DESIGN section to describe the typed-comparison shape that lands in this round.
**Source TOPIC:** `mock/design_rounds/202605250607_topic.hila-c2-reviewer-fixes.md`.

# Doc changes

## CHANGE: doc section `## select_approach base heuristic` REVISED in DESIGN.md.tmpl
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "SCHEDULE_MEGA_THRESHOLD" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

Replaces the inline `10_000` and `fiber_count <= 1` numeric mentions with named typed constants: `SCHEDULE_MEGA_THRESHOLD: USize = USize(10_000)` and `SINGLE_FIBER_CUTOVER: USize = USize(1)`. The prose names the constants by identifier so the section reads as a description of the typed body, not a bare-numeric narrative. The heuristic itself (three branches over size + fiber-count) is unchanged.

# Verification target

After the round ships: typed-comparison `select_approach` body in `dispatch/mod.rs`, six tests passing, no `.0` field accesses on `record_count` or `fiber_count` in the function body or tests.
