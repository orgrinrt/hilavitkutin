# Doc changelist: the second review of the arvo-dev review fixes

**Date:** 2026-09-19
**Phase:** DOC
**Topic:** `202609191000_topic.second-review-on-arvo-dev.md`

Item one, the spawn-site lint, and the persistence citations change no design document and are source-only. Item C is the regeneration of `docs/` from the templates this changelist edits.

## CHANGE: the thread-count rule FROM deleted TO restated as a bound on the closures the engine hands out

File: `mock/PRINCIPLES.md.tmpl` (Pre-allocated thread pool).

The paragraph keeps what the previous round said about the executor owning the threads and the pinning, and gets the founding rule back in the form the engine can still honour: the engine hands out `min(runnable worker count, parallelisable_width + 1)` closures, the parallelisable width being the most trunks any phase carries and the "+1" the convergence worker. It says the engine does not honour it yet and hands out one closure per worker.

## CHANGE: the zero-worker behaviour FROM decided in the engine design TO provisional, with the three answers stated

File: `mock/crates/hilavitkutin/DESIGN.md.tmpl` (Platform implementations).

The clamp paragraph keeps the ceiling, which follows from the per-core state being sized by `MAX_CORES`. The zero case becomes a provisional paragraph naming refuse, run inline on the caller, and hand out one closure, with what each does to `frame_await_done`, and saying the choice waits on op and the code's current behaviour (one closure) is provisional.

## CHANGE: `runnable_worker_count` FROM unnamed TO named in the engine design

File: `mock/crates/hilavitkutin/DESIGN.md.tmpl` (Platform implementations).

The clamp paragraph names `runnable_worker_count` as the thread module's `pub const fn` that computes it.

## CHANGE: the generated documents FROM stale TO regenerated

Files: `docs/` at the repository root, by `cargo mock`.
