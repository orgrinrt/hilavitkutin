**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** hilavitkutin-persistence primitive-vocabulary sweep (#72)
**Source topics:** 202604420000_topic.persistence-sweep.md
**Doc CL:** 202604420000_changelist.doc.lock.md (no doc changes)

# Files touched

## mock/crates/hilavitkutin-persistence/src/primitives.rs

Three domain newtypes carry wire-format bit widths. Add trailing
inline `lint:allow(no-bare-numeric) lint:allow(arvo-types-only)` to
each site. Reason: "wire-format bit width fixed for on-disk
representation; exact-width refinement tracked: #72".

- Line 42 `pub struct SchemaVersion(pub u32);` — append allow comment.
- Line 45 `pub const fn new(v: u32) -> Self {` — append allow comment.
- Line 48 `pub const fn bits(self) -> u32 {` — append allow comment.
- Line 58 `pub struct EvictionWeight(pub u64);` — append allow comment.
- Line 61 `pub const fn new(w: u64) -> Self {` — append allow comment.
- Line 64 `pub const fn bits(self) -> u64 {` — append allow comment.
- Line 71 `pub struct BitWidth(pub u32);` — append allow comment.
- Line 74 `pub const fn new(b: u32) -> Self {` — append allow comment.
- Line 77 `pub const fn bits(self) -> u32 {` — append allow comment.

## mock/crates/hilavitkutin-persistence/src/sieve.rs

Private impl fields flip to arvo types; const-generic sites take
trailing allow comments.

- Line 22 `visited: bool` → `visited: Bool` (type change).
- Line 31 `visited: false` → `visited: Bool::FALSE` (init).
- Line 41 `pub struct SieveCache<K, V, const CAP: usize> {` —
  trailing allow comment tracked: #121.
- Line 43 `head: usize,` → `head: USize,`.
- Line 44 `count: usize,` → `count: USize,`.
- Line 47 `impl<K: Copy + Eq, V, const CAP: usize> ..` — trailing
  allow comment tracked: #121.
- Line 186 `impl<K: Copy + Eq, V, const CAP: usize> Default for ..`
  — trailing allow comment tracked: #121.

Body adjustments for flipped fields:
- `Self { .., head: 0, count: 0, .. }` → `head: USize(0), count: USize(0)`.
- `self.count` reads → `self.count.0`.
- `self.head` reads in arithmetic / index → `self.head.0`; index
  into slot array uses `self.slots[self.head.0]`.
- `self.head = (self.head + 1) % CAP` → `self.head = USize((self.head.0 + 1) % CAP)`.
- `self.count += 1` → `self.count = USize(self.count.0 + 1)`.
- `self.count -= 1` → `self.count = USize(self.count.0 - 1)`.
- `slot.visited = true/false` → `slot.visited = Bool::TRUE/FALSE`.
- `if slot.visited` → `if slot.visited.0`.
- `visited: false` in re-insertion initialisers → `visited: Bool::FALSE`.
- `USize(self.count)` return in `len()` is replaced with bare
  `self.count` (already USize).

## mock/crates/hilavitkutin-persistence/src/manifest.rs

Move the two pre-existing preceding-line `lint:allow` comments to
trailing inline form (preceding-line form is silently ignored;
captured as memory `feedback_lint_allow_same_line_only.md`). Add
`arvo-types-only` to the allow list to match the lint pair.

- Lines 17 + 18 collapse to `pub const MAX_TABLES: usize = 256; // lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason: array-size const must be usize (rust grammar); tracked: #121`.
- Lines 23 + 24 collapse to `pub const MAX_COLUMNS_PER_TABLE: usize = 64; // lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason: array-size const must be usize (rust grammar); tracked: #121`.

## mock/crates/hilavitkutin-persistence/src/string_table.rs

`&[u8]` is the canonical byte-slice primitive for borrowed mmap
buffers. Add trailing inline allow to the signature line.

- Line 46 `pub fn lookup(&self, content_hash: ContentHash) -> Maybe<&[u8]> {`
  — append `// lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason: mmap byte-slice return; u8 is the canonical byte primitive; tracked: #72`.

# Validation

- `cargo check` clean inside `mock/`.
- `cargo mock --lint-only | grep hilavitkutin-persistence | grep -E "(arvo-types-only|no-bare-numeric|no-bare-string)"` returns zero.
