# Doc CL: Wake Spin Pre-Roll (No Template Changes)

**Date:** 2026-07-19
**Round topic:** `202607202300_topic.wake-spin-preroll.md`

The engine template's "Three-tier parking" section already states the
canonical design this round moves the implementation toward (tier
thresholds, `WakeStrategy` budgets P=128 / E=32, the per-phase
`predicted_wait_ns` selector); the round changes no design, so no
`*.md.tmpl` edit belongs to it. The api template's `RunCfg` section
lists consumer-tunable consts by example rather than exhaustively, so
the new budget const rides source-side rustdoc. This CL opens the round
toward its source slice.
