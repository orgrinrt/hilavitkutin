**Date:** 2026-05-25
**Phase:** SRC
**Scope:** HILA-RUNTIME-C2 codegen_fiber + codegen_core skeleton-stub source CL. Two source edits replacing `todo!()` bodies with sibling-constructor returns. One test module addition exercising two skeleton-return fixtures.
**Source TOPIC:** `mock/design_rounds/202605250615_topic.hila-c2-codegen-stubs.md`.
**Source DOC:** `mock/design_rounds/202605250615_changelist.doc.lock.md`.

# Source changes

## CHANGE: `codegen_fiber` body FROM `todo!()` TO `FiberDispatch::new()` in dispatch/mod.rs
File: `mock/crates/hilavitkutin/src/dispatch/mod.rs`
Verification: `grep -A 2 'pub fn codegen_fiber' mock/crates/hilavitkutin/src/dispatch/mod.rs | grep -q 'FiberDispatch::new()'`.

The `todo!()` body at line 48 is replaced with `FiberDispatch::new()`. The sibling constructor (in `fiber_dispatch.rs:33`) already returns the documented "empty skeleton record with no body and zero metadata"; this slice promotes the constructor call from "unused" to "called by the codegen entry point". The function signature stays unchanged.

## CHANGE: `codegen_core` body FROM `todo!()` TO `CoreDispatch::new()` in dispatch/mod.rs
File: `mock/crates/hilavitkutin/src/dispatch/mod.rs`
Verification: `grep -A 2 'pub fn codegen_core' mock/crates/hilavitkutin/src/dispatch/mod.rs | grep -q 'CoreDispatch::new()'`.

The `todo!()` body at line 56 is replaced with `CoreDispatch::new()`. The sibling constructor (in `core_dispatch.rs:33`) returns an empty skeleton with an array of `FiberDispatch::new()` records plus zero-init phases and morsel boundaries. Same shape as the codegen_fiber change.

## CHANGE: tests module ADDED to dispatch/mod.rs covering both skeleton-return shapes
File: `mock/crates/hilavitkutin/src/dispatch/mod.rs`
Verification: `grep -q 'mod codegen_stub_tests' mock/crates/hilavitkutin/src/dispatch/mod.rs`.

Two tests in a new `codegen_stub_tests` module:
1. `codegen_fiber_returns_empty_skeleton`: instantiates `codegen_fiber::<(), 4>()`, asserts `body` is `Maybe::Isnt`, `fiber_id` is `FiberId::ZERO`, `sync_point_count` is `USize::ZERO`.
2. `codegen_core_returns_empty_skeleton`: instantiates `codegen_core::<(), 4>()`, asserts `fiber_count` is `USize::ZERO` and `phase_count` is `USize::ZERO`.

# Lock criteria

- All three `## CHANGE:` verification predicates pass.
- `cargo build` clean.
- `cargo test -p hilavitkutin codegen_stub` returns two tests passing.
- `cargo test --workspace` clean modulo pre-existing trybuild flake.
- `! grep "todo!" mock/crates/hilavitkutin/src/dispatch/mod.rs` returns no matches.
