**Date:** 2026-05-25
**Phase:** DOC
**Scope:** HILA-RUNTIME-C2 codegen_fiber + codegen_core skeleton-stub DOC CL. Adds a DESIGN section. Adds a BACKLOG entry for the LLVM-driven monomorphisation follow-up.
**Source TOPIC:** `mock/design_rounds/202605250615_topic.hila-c2-codegen-stubs.md`.

# Doc changes

## CHANGE: doc section `## codegen_fiber + codegen_core skeleton stubs` ADDED to DESIGN.md.tmpl
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "^## codegen_fiber + codegen_core skeleton stubs" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

New DESIGN section under Dispatch codegen. Documents the skeleton-return shape: `codegen_fiber<Ctx, MAX_CORES>` returns `FiberDispatch::new()` (empty record with `body: Maybe::Isnt`); `codegen_core<Ctx, MAX_FIBERS>` returns `CoreDispatch::new()` (array of empty FiberDispatch records). Both signatures unchanged. The skeleton shape exists today because the runtime megaround shipped both sibling constructors; this slice promotes the constructors from "exists, unused" to "exists, called by the codegen entry points". Real LLVM-driven monomorphisation lands in the follow-up.

## CHANGE: BACKLOG entry `codegen_fiber + codegen_core LLVM-driven monomorphisation` ADDED to BACKLOG.md.tmpl
File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Verification: `grep -q "codegen_fiber + codegen_core LLVM-driven monomorphisation" mock/crates/hilavitkutin/BACKLOG.md.tmpl`.

New entry. Documents the real work: `hilavitkutin-build` plugin hooks emit the per-fiber `run_fiber<S>` monomorphisation (`mock/research/sketches/202605101036-fibershape-typing/` confirmed the WORKS path); per-core compiled pipelines fuse the morsel loop + arena progress + S3 fence + micro-morsel sync per Topic 6 axis E. Sequencing: depends on the `hilavitkutin-build` LLVM-pass plugin interface shipping. Until then the skeleton return shapes the rest of the engine consumes are typed-correct but body-empty.

# Verification target

After the round ships: `dispatch/mod.rs` has zero `todo!()` calls. `Scheduler::run` body becomes the only remaining engine surface that panics.
