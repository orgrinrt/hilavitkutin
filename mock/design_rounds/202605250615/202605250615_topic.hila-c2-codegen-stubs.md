# Topic: HILA-RUNTIME-C2 codegen_fiber + codegen_core skeleton stubs

**Date:** 2026-05-25
**Phase:** TOPIC
**Scope:** hilavitkutin engine `dispatch/mod.rs::codegen_fiber` and `dispatch/mod.rs::codegen_core`. Task #340 HILA-RUNTIME-C2 (the two remaining dispatch-codegen surfaces).

## Current state

After PR #80 (assign_cores base) and PR #81 (select_approach base), three engine surfaces remain `todo!()`:

1. `dispatch/mod.rs:48` `codegen_fiber` — emits monomorphised per-fiber dispatch function.
2. `dispatch/mod.rs:56` `codegen_core` — emits per-core compiled pipeline.
3. `scheduler/mod.rs:140` `Scheduler::run` body — umbrella.

`codegen_fiber` and `codegen_core` both have constructor-shaped sibling types ready: `FiberDispatch::new()` (in `fiber_dispatch.rs:33`) returns the empty skeleton with `body: Maybe::Isnt` and zero metadata; `CoreDispatch::new()` (in `core_dispatch.rs:33`) builds an array of `FiberDispatch::new()` records plus zero-init fields. Both constructors are documented "skeleton" shapes.

This slice ships both surfaces as skeleton-returning stubs: `codegen_fiber` returns `FiberDispatch::new()`, `codegen_core` returns `CoreDispatch::new()`. The real LLVM-driven monomorphisation lands in a follow-up that needs `hilavitkutin-build` machinery.

## Why both in one slice

Two reasons:

- Same shape: both are "replace `todo!()` with the existing sibling skeleton constructor". The diff is mechanical and small.
- Sequencing: `codegen_core` calls `FiberDispatch::new()` already (per `core_dispatch.rs:35`), so it cannot meaningfully stub differently from `codegen_fiber`. Splitting them across PRs would add ceremony overhead without semantic value.

## Changes proposed

### Source edits

1. `dispatch/mod.rs::codegen_fiber`: replace `todo!()` body with `FiberDispatch::new()`. The function signature unchanged.
2. `dispatch/mod.rs::codegen_core`: replace `todo!()` body with `CoreDispatch::new()`. The function signature unchanged.

### Tests

Two tests cover the skeleton return shapes:

1. `codegen_fiber_returns_empty_skeleton`: `codegen_fiber::<(), 4>()` returns a record whose `body` is `Maybe::Isnt`, `fiber_id` is `FiberId::ZERO`, `sync_point_count` is `USize::ZERO`.
2. `codegen_core_returns_empty_skeleton`: `codegen_core::<(), 4>()` returns a record whose `fiber_count` is `USize::ZERO`, `phase_count` is `USize::ZERO`.

### Follow-up BACKLOG entry

`codegen_fiber + codegen_core LLVM-driven monomorphisation`: documents the real work — `hilavitkutin-build` plugin hooks (per `dispatch/mod.rs:11 BACKLOG -> Engine 5a3 follow-ups`) to emit the actual per-fiber `run_fiber<S>` monomorphisation and per-core compiled pipelines. Sequencing: depends on the `hilavitkutin-build` LLVM-pass machinery shipping its plugin interface.

### Out of scope

- The LLVM-driven monomorphisation itself (separate slice; needs build-time machinery).
- `Scheduler::run` body (separate slice; the umbrella; ships after this).

## Lock criteria

- `cargo build` clean.
- Two new tests pass.
- `cargo test --workspace` clean modulo pre-existing trybuild flake.
- No `todo!()` remains in `dispatch/mod.rs`.
