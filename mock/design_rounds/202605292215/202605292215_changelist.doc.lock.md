# Doc changelist: csr-bidirectional-adapter

**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605292215
**Topic:** `202605292215_topic.csr-bidirectional-adapter.md`

## Scope

One prose sentence added to the plan-stage description in the engine crate's
design doc. The doc already states (step 1) that `DependencyGraph` uses CSR
backing and (steps 5-6) that the structural-analysis steps run on arvo-sparse.
The adapter this round ships is the bridge between those two facts, so the doc
gains an explicit note that the graph converts to a bidirectional CSR for those
steps.

No new backticked type names are introduced (the conversion is described in
prose), so the `design-doc-source-mismatch` lint has nothing new to verify and
the doc edit does not depend on the source landing first.

## Files

### `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Append to the step-1 description of the plan stage: a sentence noting that the
graph converts to a bidirectional CSR (forward adjacency plus a pre-computed
transpose) to feed the arvo-sparse structural-analysis steps. Phrased in prose,
no new backticks.
