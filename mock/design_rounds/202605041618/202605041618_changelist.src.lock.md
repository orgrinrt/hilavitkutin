**Date:** 2026-05-04
**Phase:** SRC
**Scope:** hilavitkutin-linking error.rs (one new variant) + windows.rs (one call-site map)
**Source topics:** 202605041618 (D3 PathEncodingUnsupported)

# Source changelist: PathEncodingUnsupported variant

Mechanical execution of the locked doc CL. Two files edited. Additive change to `LinkError`; one call-site rewires.

## CHANGE: enum `LinkError` ADD variant `PathEncodingUnsupported`

File: `mock/crates/hilavitkutin-linking/src/error.rs`

Append `PathEncodingUnsupported` to the existing `#[non_exhaustive] enum LinkError`, between `IncompatibleVersion` and the closing brace, with a doc comment matching the topic file's prose.

Verification: `grep -n "PathEncodingUnsupported" mock/crates/hilavitkutin-linking/src/error.rs` returns at least one match (the variant declaration line) and the doc lines reference "non-ASCII" and "MAX_PATH".

## CHANGE: fn `platform_load` REPLACE error map FROM `LinkError::PathNotFound` TO `LinkError::PathEncodingUnsupported`

File: `mock/crates/hilavitkutin-linking/src/backend/windows.rs:18-21`

In the `let Some(wide) = ascii_to_wide(path) else { ... };` arm, return `LinkError::PathEncodingUnsupported` instead of `LinkError::PathNotFound`. The `ascii_to_wide` helper signature and body are unchanged; only the caller's error mapping changes.

Verification: `grep -n "PathEncodingUnsupported" mock/crates/hilavitkutin-linking/src/backend/windows.rs` returns one match (the new arm), and `! grep -n "Outcome::Err(LinkError::PathNotFound)" mock/crates/hilavitkutin-linking/src/backend/windows.rs` (the Windows backend no longer returns `PathNotFound` directly).

## What is NOT changed

- `ascii_to_wide` helper. Signature and body unchanged.
- `LoadLibraryW` failure path. Still maps to `LoadFailed { platform_code }`.
- Unix backend. Still returns `PathNotFound` from its `is_null_terminated` check. Unix paths are opaque bytes; missing NUL is genuinely "no file at this path".
- `IncompatibilityError` enum. No changes.
- No tests added. The Windows path encoding is exercised under platform-conditional test coverage that this round does not extend.

## Lock criteria

- [ ] Both verification greps above succeed.
- [ ] `cargo check -p hilavitkutin-linking --target x86_64-pc-windows-gnu` would pass; on the macOS host workstation, `cargo check -p hilavitkutin-linking` (default target) compiles the Unix backend and confirms the shared error.rs additions resolve cleanly.
- [ ] `cargo mock --lint-only --build` passes scoped to `hilavitkutin-linking`.
