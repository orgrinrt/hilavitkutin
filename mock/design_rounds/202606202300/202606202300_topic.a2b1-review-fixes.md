# Topic: A2b-1 review fixes (test doc + total==0 note)

**Date:** 2026-06-19
**Phase:** TOPIC
**Scope:** `hilavitkutin` test doc + scheduler method doc
**Source topics:** pr-reviewer-senior on PR #158

## Current state

A2b-1's test `//!` header still describes the abandoned start-index approach
("records the FIRST record index... must be `[0, 4, 8]`"); the shipped body records
per-morsel iteration counts and asserts `[4, 4, 2]` (each() yields morsel-local
indices, so start-index would be `[0,0,0]`). The prior round's locked src CL repeats
the dead `[0, 4, 8]` claim. Separately, `run_one_trunk_windowed` calls `run_trunk`
zero times when `total == 0`, a divergence from `run_one_trunk` (one call over
`[0,0)`).

## Changes proposed

1. Rewrite the test `//!` header to the count-based `[4, 4, 2]` contract + the
   morsel-local-index reason.
2. Add a one-line note to `run_one_trunk_windowed`'s doc: `total == 0` runs zero
   windows (the per-record body is a no-op at zero records, so this is benign;
   `run_one_trunk`'s single `[0,0)` call is likewise a no-op).
3. Record that the prior round's (202606202200) src CL `[0, 4, 8]` claim was the
   abandoned approach; the shipped + corrected contract is `[4, 4, 2]`.

## Not in scope

A2b-2 (run() wiring + fiber_mask + asm fixture).
