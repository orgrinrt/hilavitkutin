# Src changelist: A2b-1 review fixes

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606202300

## CHANGE: correct `tests/a2b_windowed_trunk.rs` module doc to the shipped contract
File: `mock/crates/hilavitkutin/tests/a2b_windowed_trunk.rs`
Verification: `! grep "0, 4, 8" mock/crates/hilavitkutin/tests/a2b_windowed_trunk.rs` (the dead start-index claim is gone); the header describes the per-morsel iteration-count `[4, 4, 2]` contract.

## CHANGE: note total==0 behaviour on `run_one_trunk_windowed`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep "total == 0\|zero records\|zero windows" mock/crates/hilavitkutin/src/scheduler/mod.rs` finds the note in the method doc.

## NOTE: prior round 202606202200 src CL claim correction
The 202606202200 src CL described the test as recording start indices `[0, 4, 8]`;
that was the abandoned approach (each() yields morsel-local indices). The shipped +
corrected contract is per-morsel iteration counts `[4, 4, 2]`. Recorded here per
cl-claim-sketch-discipline; the prior locked CL stays as the audit trail.
