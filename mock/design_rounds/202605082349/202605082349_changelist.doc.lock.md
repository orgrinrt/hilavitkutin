**Date:** 2026-05-08
**Phase:** DOC
**Round:** 202605082349
**Source topic:** `202605082349_topic_builder_api_rename.md`

# Doc CL: SchedulerBuilder method-name updates in templates

## Scope

Mock template files referencing the renamed methods. Source-side renames ride in the src CL.

## File: `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`

Sweep for any prose / code example that names a SchedulerBuilder method. Apply renames per Topic Decision 1 (registration: `.add` → `.add_unit`, `.resource` → `.add_resource`, `.column` → `.add_column`, `.resource_default` → `.add_resource_default`) and Decision 2 (configuration: `.memory` → `.with_memory`, `.threads` → `.with_threads`, `.clock` → `.with_clock`). `.add_virtual` and `.add_kit` are unchanged.

Not all references will exist; the api crate's DESIGN content focuses on contracts not on builder usage. Apply only to occurrences that name the renamed methods. Verification: `grep -E '\.add::|\.resource\(|\.resource_default\(|\.column::|\.memory\(|\.threads\(|\.clock\(' mock/crates/hilavitkutin-api/DESIGN.md.tmpl` returns zero hits in builder-method position after edits.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Engine DESIGN.md.tmpl carries the SchedulerBuilder section. Sweep for every method-name occurrence and apply renames. Update the `.build()` where-clause example if it exists. Update any prose paragraph that lists builder methods.

## File: `mock/crates/hilavitkutin-kit/DESIGN.md.tmpl`

Sweep for any builder-call example showing how a Kit author wires a `Kit` impl into the engine. The `add_kit` calls stay as-is; if the example calls `.resource(...)` to seed a Resource value before installing the Kit, that becomes `.add_resource(...)`.

## File: `mock/crates/hilavitkutin-providers/DESIGN.md.tmpl`

Provider crate's DESIGN content references `builder.resource(...)` for installing the InternerKit's resource value. Rename to `builder.add_resource(...)`.

## File: `mock/crates/hilavitkutin-providers/BACKLOG.md.tmpl`

Sweep for any line that references the renamed methods.

## File: `README.md` (repo root)

Sweep for any builder example in the repo-root README. Apply renames if present.

## Lock criteria

- Every renamed method occurrence in the listed templates updates to the new name.
- `cargo mock` regenerates rendered docs cleanly.
- `grep -rE '\.add::|\.resource\(|\.resource_default\(|\.column::|\.memory\(|\.threads\(|\.clock\(' mock/crates/*/DESIGN.md.tmpl mock/crates/*/BACKLOG.md.tmpl mock/crates/*/README.md.tmpl README.md` returns zero hits in builder-method position.
- No other doc files touched.

## Out of scope for this CL

- Source edits (the src CL covers `scheduler/mod.rs`, all call sites in source/tests, and all diagnostic-note string literals).
- Examples or prose unrelated to builder method names.
