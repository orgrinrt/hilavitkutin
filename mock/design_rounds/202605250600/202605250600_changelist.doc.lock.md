**Date:** 2026-05-25
**Phase:** DOC
**Scope:** HILA-RUNTIME-C2 base select_approach DOC CL. Adds a DESIGN section for the base heuristic. Adds a BACKLOG entry for the benchmark-tuned threshold follow-up.
**Source TOPIC:** `mock/design_rounds/202605250600_topic.hila-c2-select-approach-base.md`.

# Doc changes

## CHANGE: doc section `## select_approach base heuristic` ADDED to hilavitkutin DESIGN.md.tmpl
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "^## select_approach base heuristic" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

New DESIGN section under Dispatch codegen documenting the base `select_approach` heuristic. Three branches over `(record_count, fiber_count)`: large records (>=10_000) pick `ScheduleMega` for LLVM's whole-pipeline optimisation window; small records with one fiber pick `TrunkMega` (mega fn fits); small records with many fibers pick `IndirectPerFiber` (per-fiber indirect call has lowest overhead at that size). The `10_000` threshold matches the existing `<10K target` named in the type doc comments. Benchmark-tuned thresholds land in the follow-up.

## CHANGE: BACKLOG entry `select_approach benchmark-tuned thresholds` ADDED to hilavitkutin/BACKLOG.md.tmpl
File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Verification: `grep -q "select_approach benchmark-tuned thresholds" mock/crates/hilavitkutin/BACKLOG.md.tmpl`.

New entry naming the follow-up: sweep `(record_count, fiber_count)` against the bench harness, measure dispatch overhead per approach (IndirectPerFiber / TrunkMega / ScheduleMega), update the cutovers based on real data. Refines both the `record_count >= 10_000` size threshold AND the `fiber_count <= 1` tiebreaker between TrunkMega and IndirectPerFiber in the small-record-count path. Sequencing: ships after the bench harness gains a dispatch-overhead bench fixture.

# Verification target

After the round ships: `cargo build` clean, `cargo test --workspace` clean (modulo pre-existing trybuild flake), four new select_approach tests pass.
