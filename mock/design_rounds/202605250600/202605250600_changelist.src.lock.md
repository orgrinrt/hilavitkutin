**Date:** 2026-05-25
**Phase:** SRC
**Scope:** HILA-RUNTIME-C2 base select_approach source CL. One source edit replacing the `todo!()` body with the three-branch heuristic. One test module addition exercising four fixtures.
**Source TOPIC:** `mock/design_rounds/202605250600_topic.hila-c2-select-approach-base.md`.
**Source DOC:** `mock/design_rounds/202605250600_changelist.doc.lock.md`.

# Source changes

## CHANGE: `select_approach` body FROM `todo!()` TO three-branch heuristic in dispatch/mod.rs
File: `mock/crates/hilavitkutin/src/dispatch/mod.rs`
Verification: `! grep -A 2 'pub fn select_approach' mock/crates/hilavitkutin/src/dispatch/mod.rs | grep -q 'todo!' && grep -q 'fn select_approach' mock/crates/hilavitkutin/src/dispatch/mod.rs`.

The `todo!()` body at line 40 is replaced with the three-branch heuristic per the DOC CL's algorithm. `record_count.0 >= 10_000` returns `DispatchApproach::ScheduleMega`. Otherwise (`record_count.0 < 10_000`): `fiber_count.0 <= 1` returns `TrunkMega`; else returns `IndirectPerFiber`. The `10_000` threshold matches the `<10K target` already named in the type doc comments. Benchmark-tuned thresholds land in the follow-up named in BACKLOG.

## CHANGE: tests module ADDED to dispatch/mod.rs covering four fixtures
File: `mock/crates/hilavitkutin/src/dispatch/mod.rs`
Verification: `grep -q '#\[cfg(test)\]' mock/crates/hilavitkutin/src/dispatch/mod.rs && grep -q 'mod select_approach_tests' mock/crates/hilavitkutin/src/dispatch/mod.rs`.

Four tests:

1. `large_record_count_picks_schedule_mega`: 50_000 records, 8 fibers expect `ScheduleMega`.
2. `small_records_single_fiber_picks_trunk_mega`: 1_000 records, 1 fiber expect `TrunkMega`.
3. `small_records_many_fibers_picks_indirect_per_fiber`: 1_000 records, 8 fibers expect `IndirectPerFiber`.
4. `threshold_boundary_picks_schedule_mega`: exactly 10_000 records, 8 fibers expect `ScheduleMega` (the boundary is inclusive for ScheduleMega per the heuristic).

# Lock criteria

- Both `## CHANGE:` verification predicates pass.
- `cargo build` clean.
- `cargo test -p hilavitkutin select_approach` returns all four tests passing.
- `cargo test --workspace` clean (modulo pre-existing trybuild flake).
- `cargo mock --lint-only --commit` clean.
