# Src changelist: three allows the lint reads

**Date:** 2026-08-10
**Phase:** IMPL
**Doc CL:** `202608101300_changelist.doc.lock.md`

## CHANGE: the cast inside `origin_base`

File: `mock/crates/hilavitkutin-sym/src/shape.rs`

The signature's allow holds; the lint fires on the body's `as u32`, which is
arvo's own `to_raw` boundary. **An allow on a signature does not cover a body**,
and round thirteen placed its comment on the line its reasoning was about rather
than the line the lint reads.

## CHANGE: the two verdict predicates' `bool` returns

Same file. `origins_map_to_distinct_indices` and
`origin_indices_fit_origin_bits` return `bool` because a verdict function is
what lets one law serve both the shipped shapes and a deliberately broken one.
Test-local, never on a public surface.

## Verification

`mock --lint-only --strict --scope hilavitkutin-sym,hilavitkutin-str` reports
zero `no-bare-numeric` and zero `arvo-types-only`, measured after the autofix
against what landed rather than against the working tree.
