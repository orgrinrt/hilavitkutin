# Src changelist: fiber_plan_idx mapping catalogue test

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606201900

## NEW TEST: catalogue `fiber_plan_idx` CSR-vs-dispatch mapping
File: `mock/crates/hilavitkutin/tests/a1_fiber_morsel_size.rs`
Verification: `cargo test -p hilavitkutin --test a1_fiber_morsel_size -- --ignored` lists `fiber_plan_idx_records_csr_index_under_dispatch_reorder`.

A `#[ignore]` catalogue test asserting `fiber_plan_idx == f` (the CSR index) and
`morsel_size == plan.morsel_windows[f]` for a multi-trunk plan whose dispatch
order reorders fibers (`fd != f`). Red/ignored until slice A2 builds the
multi-trunk fixture + a `__fiber_plan_idx` accessor; the single-fiber A1 test
collapses `fd == f == 0`, leaving the mapping unasserted.
