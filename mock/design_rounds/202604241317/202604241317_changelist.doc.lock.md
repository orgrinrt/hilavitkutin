**Date:** 2026-04-24
**Phase:** DOC
**Scope:** doc-level artifacts for the severity ramp-up round (round 202604241317). No public-surface design change; BACKLOG housekeeping only.

**Source topic:** 202604241317_topic.severity-ramp-up.md

# Purpose

The topic records the decision to restore ten previously-downgraded lints to their intended error severity. The SRC CL applies the severity flip and fixes each resulting violation.

This DOC CL covers only documentation housekeeping: BACKLOG entries for the one task introduced by this round (#206 FFI descriptor boundary allow policy) and a note about the ramp-up being landed.

# D1. Update `hilavitkutin-extensions/BACKLOG.md.tmpl`

Add an entry under "Designed, deferred" noting that `CapabilityId` / `ExtensionVersion` / `ExtensionAbiStatus` / `ExtensionDescriptor` fields are FFI-boundary primitives by design, tracked as #206. Future design-round work on the descriptor respects the boundary constraint.

# No DESIGN edits

None needed. The DESIGN.md.tmpl for `hilavitkutin-extensions` already documents the `#[repr(C)]` contract; source conformance to that contract is what triggered the lint violations, and the fix is to acknowledge the boundary explicitly via tracked `lint:allow`, not to redesign.

# No mockspace.toml edits in DOC phase

The severity flip in `mock/mockspace.toml` was applied by the human overseer (per the workspace rule `.claude/rules/mockspace-toml-edits.md`). This CL does not re-edit it.

# Sequencing

1. Apply D1 BACKLOG edit.
2. Commit doc changes.
3. Lock via `cargo mock lock`.
4. Write SRC CL.

# No new design content

This CL introduces no new public types, methods, attributes, or constants. The sole effect is BACKLOG housekeeping.
