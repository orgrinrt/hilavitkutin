**Date:** 2026-05-05
**Phase:** SRC
**Round:** 202605050500

# Source changelist: em-dash polish (round-202605050400 follow-up)

6 grammatical fixes addressing senior-review findings on PR #57. End-of-line bare em-dashes that round 202605050400's mechanical comma-substitution rule mishandled get the appropriate clause separator: semicolon for tightly coupled continuations.

## CHANGE: axis.rs:34 trailing-comma to semicolon

File: `mock/crates/hilavitkutin-build/src/axis.rs:34`
From: `Optimisation tier. Additive per DESIGN §Optimisation tiers ,`
To: `Optimisation tier. Additive per DESIGN §Optimisation tiers;`

## CHANGE: guards.rs:10 trailing-comma to semicolon

File: `mock/crates/hilavitkutin-build/src/guards.rs:10`
From: `The macro always expands to a hard error ,`
To: `The macro always expands to a hard error;`

## CHANGE: ctx/lib.rs:123 trailing-comma to semicolon

File: `mock/crates/hilavitkutin-ctx/src/lib.rs:123`
From: `(e.g. `where R: Contains<...>`) ,`
To: `(e.g. `where R: Contains<...>`);`

## CHANGE: str/interner.rs:12 trailing-comma to semicolon

File: `mock/crates/hilavitkutin-str/src/interner.rs:12`
From: `Only handles runtime strings ,`
To: `Only handles runtime strings;`

## CHANGE: thread/class.rs:5 in-paren trailing-comma to semicolon

File: `mock/crates/hilavitkutin/src/thread/class.rs:5`
From: `(CPUID leaf 0x1A / sysfs / IOKit ,`
To: `(CPUID leaf 0x1A / sysfs / IOKit;`

## CHANGE: thread/mod.rs:57 trailing-comma to semicolon

File: `mock/crates/hilavitkutin/src/thread/mod.rs:57`
From: `T: Executor once the trait ships in a follow-up round ,`
To: `T: Executor once the trait ships in a follow-up round;`

## Verification

`! rg ', $' mock/crates/ --type rust` returns zero matches.
`cargo +nightly check` clean.
