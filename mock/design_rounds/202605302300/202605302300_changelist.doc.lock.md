**Date:** 2026-05-30
**Phase:** DOC
**Round:** 202605302300 columnstorage-overflow-fix

## Intent

Record the second review finding (the `!Send` / `!Sync` raw pointer) in the providers BACKLOG so the migration round that wires `ArenaColumnStorage` into the multi-core scheduler does not rediscover it. No DESIGN type-name change: `StorageError` is not backticked in any DESIGN, so adding its `LengthOverflow` variant is a pure source change handled in the SRC CL.

## CHANGE: providers `BACKLOG.md.tmpl` records the Send/Sync caveat
File: `mock/crates/hilavitkutin-providers/BACKLOG.md.tmpl`
Add a note: `ArenaColumnStorage` holds a raw `*mut u8` per column, so it is `!Send` / `!Sync`; wiring it into the multi-core scheduler needs an `unsafe impl Send`/`Sync` with a provenance justification, landing with the migration that threads the store. Names unbacktick'd (BACKLOG convention).
Verification: `grep -i "Send" mock/crates/hilavitkutin-providers/BACKLOG.md.tmpl` returns the new note.

## Not in scope (DOC)

The overflow guard (`checked_mul` + `StorageError::LengthOverflow`) and its test are the SRC CL. The Send/Sync `unsafe impl` itself lands with the migration.
