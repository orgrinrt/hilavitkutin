# Doc changelist: plan-inputs projection post-review fixes

**Round:** 202605300720
**Phase:** DOC
**Scope:** Source-only. No `*.md.tmpl` changes.

## No doc-surface changes

Both fixes (the bundle integration test and the `MAX_STORES <= 64` compile-time guard on the projection entry points) are verification and correctness hardening of internal plan-stage machinery. They do not change the projection's shape or any documented surface, so no `*.md.tmpl` is touched. The source changes are recorded in the src changelist under the `## CHANGE:` grammar.
