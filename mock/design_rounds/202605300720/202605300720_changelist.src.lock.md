# Src changelist: plan-inputs projection post-review fixes

**Round:** 202605300720
**Phase:** IMPL
**Scope:** Two source changes closing PR #108 review findings. No shapes change.

## CHANGE: `plan_inputs_from_bundle` + `project_access_set` ADD compile-time `MAX_STORES <= 64` guard
File: `mock/crates/hilavitkutin/src/plan/project.rs`
Both projection entry fns gain an inline `const { assert!(cap_size(MAX_STORES) <= 64, ...) }` so a store list wider than the skeleton `AccessMask` single-word backing fails at compile time rather than silently dropping high bits. Mirrors the sibling `DirtyMask::_ASSERT_FITS_IN_USIZE`.
Verification: `grep "cap_size(MAX_STORES) <= 64" mock/crates/hilavitkutin/src/plan/project.rs` returns two matches.

## CHANGE: NEW test `tests/plan_inputs_from_bundle.rs` ADD
File: `mock/crates/hilavitkutin/tests/plan_inputs_from_bundle.rs`
Integration test exercising `plan_inputs_from_bundle` over a two-work-unit bundle with overlapping access sets, using a permissive `TestCtx` / `Stub` shim (same shape as `tests/scheduler_builder.rs`). Asserts per-unit read/write/access masks, `unit_count`, `record_count`, the commutativity copy, and the write/read overlap (dependency edge) between the two units. Restores the test file name the round-202605300651 src changelist named, closing that changelist-vs-source drift.
Verification: `test -f mock/crates/hilavitkutin/tests/plan_inputs_from_bundle.rs`.
