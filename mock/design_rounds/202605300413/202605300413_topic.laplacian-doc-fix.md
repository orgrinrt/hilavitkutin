**Date:** 2026-05-30
**Phase:** TOPIC
**Scope:** correct the SymmetricLaplacian module doc to match the shipped live_dim
**Source topics:** post-review fix for the spectral-width-gate round (PR #107)

## Problem

The `plan/laplacian.rs` module doc (from C1d-1) describes the loose-CSR-aware live-`node_count` path as a future that "lands if the C1d bench adopts spectral". The spectral-width-gate round landed exactly that: `SymmetricLaplacian` now implements `live_dim` returning `node_count()`, and the width-gate feeds spectral on loose graphs. The doc contradicts the code below it.

## Resolution

Rewrite the module doc's closing paragraph so it states the live-node-count path is in effect through `live_dim`, rather than describing it as a future. Source-only edit (the `//!` comment in `laplacian.rs`); no behaviour change.
