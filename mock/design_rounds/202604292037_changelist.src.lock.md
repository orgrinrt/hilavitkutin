# Changelist (src): hilavitkutin arvo Round D adoption

**Date:** 2026-04-29
**Phase:** SRC
**Source topics:** `202604292037_topic.hilavitkutin-arvo-round-d-adoption.md`

## Status

SRC phase. Mechanical sweep across five crates plus one Cargo.lock bump. Patterns A through G as enumerated in the topic. The expected end state is `cargo check --workspace` green from `mock/`.

## hilavitkutin-api (Patterns A + B)

### Source files

`crates/hilavitkutin-api/src/column_value.rs`

```diff
-use arvo::newtype::{FBits, IBits};
-use arvo::strategy::Hot;
-use arvo::ufixed::UFixed;
-use arvo::USize;
+use arvo::strategy::Hot;
+use arvo::ufixed::UFixed;
+use arvo::{fbits, ibits, USize};
```

```diff
-impl ColumnValue for UFixed<{ IBits(1) }, { FBits::ZERO }, Hot> { ... }
-impl ColumnValue for UFixed<{ IBits(2) }, { FBits::ZERO }, Hot> { ... }
-impl ColumnValue for UFixed<{ IBits(4) }, { FBits::ZERO }, Hot> { ... }
+impl ColumnValue for UFixed<{ ibits(1) }, { fbits(0) }, Hot> { ... }
+impl ColumnValue for UFixed<{ ibits(2) }, { fbits(0) }, Hot> { ... }
+impl ColumnValue for UFixed<{ ibits(4) }, { fbits(0) }, Hot> { ... }
```

`crates/hilavitkutin-api/src/context.rs`

```diff
-use arvo::newtype::USize;
+use arvo::USize;
```

`crates/hilavitkutin-api/src/id.rs` (path import + doc comment)

```diff
-use arvo::newtype::USize;
+use arvo::USize;
```

```diff
-/// Wraps `arvo::newtype::USize` so boundary types stay inside the
-/// arvo newtype family. Transparent repr means no wrapping cost at
-/// runtime.
+/// Wraps `arvo::USize` so boundary types stay inside the arvo
+/// newtype family. Transparent repr means no wrapping cost at
+/// runtime.
```

`crates/hilavitkutin-api/src/platform.rs`

```diff
-use arvo::newtype::{Bool, FBits, IBits, USize};
-use arvo::strategy::Hot;
-use arvo::ufixed::UFixed;
+use arvo::strategy::Hot;
+use arvo::ufixed::UFixed;
+use arvo::{fbits, ibits, Bool, USize};
```

```diff
-pub type Nanos = UFixed<{ IBits(64) }, { FBits::ZERO }, Hot>;
+pub type Nanos = UFixed<{ ibits(64) }, { fbits(0) }, Hot>;
```

`crates/hilavitkutin-api/src/store.rs`

```diff
-use arvo::newtype::Cap;
+use arvo::Cap;
```

`crates/hilavitkutin-api/src/work_unit.rs`

```diff
-use arvo::newtype::Bool;
+use arvo::Bool;
```

`crates/hilavitkutin-api/src/hint.rs`

```diff
-use arvo::{FBits, IBits, UFixed, strategy::Hot};
+use arvo::{fbits, ibits, strategy::Hot, UFixed};
```

```diff
-pub type Urgency      = UFixed<{ IBits(2) }, { FBits::ZERO }, Hot>;
-pub type Divisibility = UFixed<{ IBits(2) }, { FBits::ZERO }, Hot>;
-pub type Significance = UFixed<{ IBits(3) }, { FBits::ZERO }, Hot>;
+pub type Urgency      = UFixed<{ ibits(2) }, { fbits(0) }, Hot>;
+pub type Divisibility = UFixed<{ ibits(2) }, { fbits(0) }, Hot>;
+pub type Significance = UFixed<{ ibits(3) }, { fbits(0) }, Hot>;
```

### Test files

Same path migration in `crates/hilavitkutin-api/tests/{column_value, work_unit, store_types, providers}.rs`. The test in `column_value.rs` also rewrites the literal forms to match the new const-generic spelling.

## hilavitkutin-str (Patterns A + C + D + E + F)

### `crates/hilavitkutin-str/src/handle.rs`

```diff
-use arvo::newtype::Bool;
-use arvo_bits::{bitfield, Bit, Bits, Hot};
+use arvo::{bitfield, Bool};
+use arvo_bits::{Bit, Bits, Hot};
```

`StrLayout` declared via the `arvo::bitfield!` macro (Pattern C). The macro emission paths through to `Bits::from_raw(...)` after Round D, which the dependent constructor sites must follow (Pattern D).

```diff
-Self(StrLayout::new().with_id(id).with_origin(Bit::<Hot>::new(1)))
+Self(StrLayout::new().with_id(id).with_origin(Bit::<Hot>::from_raw(1)))
```

The `StrLayout::new()` call is the macro-generated default constructor; if Round D's macro emission renamed it, follow whatever the bitfield macro emits. The `Bit::<Hot>::from_raw(1)` call replaces the removed `Bit::<Hot>::new(1)`.

### `crates/hilavitkutin-str/src/interner.rs`

Patterns D, E, F applied to the `Bits<28, Hot>` construction and accessor sites:

```diff
-Str::__runtime((id as u64).into())
+Str::__runtime(Bits::<28, Hot>::from_raw(id as u32))
```

(Container width for `N=28, S=Hot` is `u32` per arvo's dispatch table; verify at SRC time and adjust if wrong. The lint:allow comment trailing each site stays as-is; the reason text already describes the semantic.)

```diff
-Maybe::Is(self.arena.arena_resolve(s.id().bits() as u32))
+Maybe::Is(self.arena.arena_resolve(s.id().to_raw() as u32))
```

```diff
-let want = Str::__make((const_fnv1a(s) & 0x0FFF_FFFF).into());
+let want = Str::__make(Bits::<28, Hot>::from_raw((const_fnv1a(s) & 0x0FFF_FFFF) as u32));
```

The `use arvo_bits::Bits;` import that the `unused_imports` warning flagged becomes load-bearing again under Pattern F (explicit narrow-cast call sites name `Bits`).

### Test files

`crates/hilavitkutin-str/tests/*.rs` enumerated at SRC time; same patterns apply to any matching constructor / accessor sites.

## hilavitkutin-persistence (Patterns D + E + F + G via ContentHash)

`ContentHash = Bits<64, Hot>`, container width `u64` per the dispatch table. Every site that constructs or destructures a `ContentHash` follows the same recipe:

```diff
-let h = ContentHash::new(value);          // or ContentHash::from(value as u64)
+let h = ContentHash::from_raw(value);     // value already u64
```

```diff
-let raw = h.bits();
+let raw = h.to_raw();
```

```diff
-let h: ContentHash = (value as u64).into();
+let h = ContentHash::from_raw(value as u64);
```

### Files (enumerated at SRC time)

- `crates/hilavitkutin-persistence/src/archive_str.rs` (~4 sites known from abandoned-round investigation).
- `crates/hilavitkutin-persistence/src/manifest.rs`.
- `crates/hilavitkutin-persistence/src/sieve.rs`.
- `crates/hilavitkutin-persistence/src/cold_store.rs`.
- Tests under each file's matching test module.

## hilavitkutin-ctx (scope TBD; investigated at SRC time)

ctx is the provider framework. Expected to be light (path-only Pattern A migrations if any). Enumerate at SRC time via:

```bash
grep -rn 'arvo::newtype\|arvo_bits::bitfield\|Bits::new\|\.bits()\|: From<u64>' \
    mock/crates/hilavitkutin-ctx/src mock/crates/hilavitkutin-ctx/tests
```

Apply matching patterns. If any unexpected pattern surfaces, capture in a SHAME entry rather than expanding scope mid-round.

## hilavitkutin (engine; scope TBD; investigated at SRC time)

The engine consumes arvo-bitmask, arvo-graph, arvo-sparse, arvo-spectral, arvo-comb. Algorithm-crate API changes from Round D may apply additional pattern-shapes. Enumerate at SRC time via:

```bash
grep -rn 'arvo::newtype\|arvo_bits::bitfield\|Bits::new\|\.bits()\|: From<u64>' \
    mock/crates/hilavitkutin/src mock/crates/hilavitkutin/tests
```

If the engine's surface is large (more than ~10 sites of new patterns), capture findings and decide whether to carve into a SHAME entry. The aim is to keep this round mechanical; algorithm-crate redesign work belongs in a follow-up round, not here.

## Cargo.lock bump

After all source edits land:

```bash
cargo update \
  --manifest-path mock/Cargo.toml \
  -p arvo -p arvo-bits -p arvo-bitmask -p arvo-graph \
  -p arvo-sparse -p arvo-spectral -p arvo-comb -p arvo-hash
```

This bumps the eight arvo crates from `a98a3f49` to `bcc38304` (current arvo dev). The new arvo facade sub-crates (`arvo-bits-contracts`, `arvo-numeric-contracts`, `arvo-storage`, `arvo-strategy`, `arvo-transparent`) get added automatically. mockspace and notko also bump to current dev tips as a side effect.

## Verification

After all edits and the lock bump:

```bash
# 1. Pattern A: zero arvo::newtype references in source.
grep -rn 'arvo::newtype' mock/crates/hilavitkutin-* mock/crates/hilavitkutin/src
# Expected: zero matches.

# 2. Pattern B: zero literal forms in const-generic positions.
grep -rnE 'IBits\([0-9]+\)|FBits\([0-9]+\)|FBits::ZERO|IBits::ZERO|Width\([0-9]+\)' \
    mock/crates/hilavitkutin-* mock/crates/hilavitkutin/src
# Expected: zero matches.

# 3. Pattern C: zero references to the old bitfield macro path.
grep -rn 'arvo_bits::bitfield' mock/crates/hilavitkutin-* mock/crates/hilavitkutin/src
# Expected: zero matches.

# 4. Pattern D + E: zero references to the removed constructor / accessor.
grep -rnE 'Bits::new\b|::bits\(\)' mock/crates/hilavitkutin-* mock/crates/hilavitkutin/src
# Expected: zero matches (verify any false-positive is an unrelated `bits()` on
# a different type).

# 5. Workspace check passes.
cd mock && cargo check --workspace
# Expected: green.
```

If any verification step fails, the failure is in scope (the round's job is to make the workspace compile). Iterate on the specific files; recommit; re-verify. The round closes only when all five verification steps pass.

## Comparison to deprecated changelist

The abandoned round 202604291932's src CL covered patterns A and B for hilavitkutin-api source plus its tests, plus the lock bump. Its scope claim ("7 src files in 2 crates plus 4 tests") was contradicted by the lock-bump cascade (str/persistence/ctx/engine each carrying additional patterns C through G). That round was abandoned cleanly per the workspace rule on frozen-topic vs scope-mismatch.

This round's src CL covers the full scope. The mechanical content of patterns A and B is identical to what the abandoned round spelled. Patterns C through G are new in scope but identical in spirit (mechanical token replacement). The lock bump is a single atomic step at the end.

The deleted task split (#284 through #288) framed each crate as its own round. That ran into the pre-commit `cargo check` workspace-wide gate (tracked separately as #289). Until that gate is loosened, one wide round is the working shape; this CL is the SRC-side execution plan for that wide round.

## Round close

After all verification commands pass:

1. `cargo mock lock` to lock the src CL (SRC -> DONE).
2. Commit the lock rename.
3. `cargo mock close` to archive the round into `mock/design_rounds/202604292037/`.
4. Commit the archive move (if `cargo mock close` doesn't auto-commit).
5. Push the branch and open the PR against `dev` per the workspace branch + PR flow.
6. Dispatch `pr-reviewer-senior`; address findings if any; merge.
7. Mark #283 completed.
