# Src changelist: C1c post-review fixes

**Date:** 2026-05-30
**Phase:** SRC
**Round:** 202605300006 (C1c post-review fixes, PR #104)

Source-comment fixes from the pr-reviewer-senior review of PR #104. No behaviour change.

## CHANGE: doc comments on `PlanError::PhaseAlignmentMismatch` + `FeasibilityCheckFailed` FROM `claim block_diagonalise returns Bool::FALSE` TO `marked reserved for the later column-classification check`
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: `grep -c "Reserved:" mock/crates/hilavitkutin/src/plan/steps.rs` returns 2; the stale `Bool::FALSE` claim no longer appears on either variant.

## CHANGE: runner trunk-projection loop FROM `no note of the id-width cap` TO `comment noting the PhaseId (Uint<5>, 32 phases) / TrunkId (Uint<6>, 64 trunks) width caps as the engine phase/trunk-count limit`
File: `mock/crates/hilavitkutin/src/plan/mod.rs`
Verification: `grep -n "Uint<6>" mock/crates/hilavitkutin/src/plan/mod.rs` matches near the projection loop comment.
