---
round: 202605090144
phase: DOC
scope: hilavitkutin-str
---

# Doc CL — str linker-section rationale

One template edit. Adds a subsection `## Linker-section exemption
rationale` to `mock/crates/hilavitkutin-str/DESIGN.md.tmpl`,
immediately after the existing `## StaticStrEntry, linker-section
registration` section. Records the five reasons the linker-section
technique is permitted in this crate despite the workspace rule
forbidding linker-magic registration patterns elsewhere.

## File-by-file plan

### `mock/crates/hilavitkutin-str/DESIGN.md.tmpl`

Insert a new `## Linker-section exemption rationale` section between
the existing `## StaticStrEntry, linker-section registration` block
and whatever currently follows it. Section content is the
condensed five-point argument from the topic file.

## Lock criteria for doc phase

- DESIGN.md.tmpl gains the subsection.
- Rendered output (regenerated by `cargo mock`) reflects the
  addition.
- `cargo mock --lint-only --commit` green.

This round has no src CL because there are no source changes.

## Cross-references

- Topic file: `202605090144_topic.str-linker-rationale.md`.
- Workspace rule: `hilavitkutin/.claude/CLAUDE.md` (no linker-magic
  registration).
- Task #384.
