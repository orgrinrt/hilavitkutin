# Doc changelist: morsel-relative iteration indices

**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041720 (morsel-relative-iteration, branch `feat/hilavitkutin-column-data-plane`)

No `*.md.tmpl` changes. The fix is a source-contract correction in the
per-WorkUnit Context iteration accessors (`EachApi` / `ReduceApi` /
`BatchApi`), with the rationale documented in the iteration accessors'
own `///` doc comments (source, edited in the SRC phase). The DESIGN
prose describes the per-WU Context and the morsel windowing at the level
of `read(i)` / `write(i)` indexing `base + (morsel.start + i)`, which is
unchanged and already correct; it does not describe the iteration
accessors' index basis, so no template prose needs editing.
