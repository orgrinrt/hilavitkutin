**Date:** 2026-05-30
**Phase:** DOC
**Round:** 202605300321 fiber-trunk-projection
**Scope:** DESIGN.md.tmpl step 7/8 prose reflects the per-block fiber->trunk-component projection

## Intent

Update the plan-stage step list so the documented contract matches the source the src CL lands: step 8 now forms fibers per-block and populates `Trunk.components`, and step 7's "projection lands later" clause points at step 8.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

### Change 1: step 7 (spectral) prose

Drop the "projecting the chosen grouping onto trunk components land in later slices" clause; the projection now lands in step 8. Keep the spectral-former description; note the width-gated spectral arm for wide blocks is a follow-on slice.

### Change 2: step 8 (fiber grouping) prose

Rewrite to describe per-block fiber formation projected onto `Trunk.components`: each block (trunk) forms its fibers within the block's units in topo order so fibers nest in trunks; the greedy former lands now, the width-gated spectral former for wide blocks follows. `TrunkComponent::Fiber` entries carry id and units; columns, head+tail, and dispatch shape fill in at later steps.
