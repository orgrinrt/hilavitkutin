---
round: 202605090047
phase: DRAFT
scope: hilavitkutin, hilavitkutin-persistence
---

# Src CL — test rehab #291

Test-only migration. Five files:

1. `mock/crates/hilavitkutin-persistence/tests/sieve.rs`
2. `mock/crates/hilavitkutin-persistence/tests/cold_store.rs`
3. `mock/crates/hilavitkutin-persistence/tests/manifest.rs`
4. `mock/crates/hilavitkutin-persistence/tests/archive_str.rs`
5. `mock/crates/hilavitkutin/tests/plan_types.rs`

Each file gains `Identity` import where needed, replaces
`assert!(<Bool>)` with `assert!(<Bool>.0)`, replaces `Some(...)` with
`Maybe::Is(...)` where the function returns `Maybe<T>`, and wraps
integer literals at `USize`-typed sites. No production-source
edits.

## Changes

### CHANGE: file `tests/sieve.rs` ADDS `Identity` import (resolves USize::ZERO)

File: `mock/crates/hilavitkutin-persistence/tests/sieve.rs`
Verification: `grep "use arvo::.*Identity" mock/crates/hilavitkutin-persistence/tests/sieve.rs` returns one match.

### CHANGE: file `tests/cold_store.rs` ADDS `Identity` import

File: `mock/crates/hilavitkutin-persistence/tests/cold_store.rs`
Verification: `grep "use arvo::.*Identity" mock/crates/hilavitkutin-persistence/tests/cold_store.rs` returns one match.

### CHANGE: file `tests/manifest.rs` ADDS `Identity` import

File: `mock/crates/hilavitkutin-persistence/tests/manifest.rs`
Verification: `grep "use arvo::.*Identity" mock/crates/hilavitkutin-persistence/tests/manifest.rs` returns one match.

### CHANGE: file `tests/archive_str.rs` MIGRATES Bool / Maybe / USize::ZERO sites

File: `mock/crates/hilavitkutin-persistence/tests/archive_str.rs`
Verification: `grep "use arvo::.*Identity" mock/crates/hilavitkutin-persistence/tests/archive_str.rs` returns one match. `! grep "Some(\"" mock/crates/hilavitkutin-persistence/tests/archive_str.rs` (no `Some("...")` literals at Maybe<&str> sites).

### CHANGE: file `tests/plan_types.rs` MIGRATES Bool / USize literal sites

File: `mock/crates/hilavitkutin/tests/plan_types.rs`
Verification: `cargo test -p hilavitkutin --test plan_types --no-run` returns 0.

## Lock criteria

- All `## CHANGE:` blocks above verify against current source.
- `cargo test --workspace --no-run` returns 0.
- `cargo test -p hilavitkutin -p hilavitkutin-persistence` passes.
- `cargo mock --lint-only --commit` green.
