# Doc changelist 202605011000 - post-review polish

**Date:** 2026-05-01
**Phase:** DOC
**Source topic:** `202605011000_topic.post-review-polish.md` (frozen)

This CL is intentionally empty on the doc-template side. Round 202605010900's locked doc CL already specified the engine surface with `W: WorkUnit` plus platform-API bounds (see `mock/crates/hilavitkutin/DESIGN.md.tmpl` "Method shapes" section). The shipped source drifted from that spec; the source CL in this round amends the source to match the existing docs.

## Files to create

None.

## Files to edit

None at the doc-template level.

## SRC-phase work (named for scope completeness)

- `mock/crates/hilavitkutin/src/scheduler/mod.rs`: tighten three method bounds, add two `use` lines.
- `mock/crates/hilavitkutin/tests/scheduler_builder.rs`: add Stub + TestCtx + 3 WU-bearing tests; update file doc-comment header.

## Verification

After SRC apply:

- Engine source surface for `add` / `memory` / `threads` / `clock` matches the locked engine DESIGN of round 202605010900 (no further drift).
- Test count goes from 8 to 11; the three new tests cover the cons-list-depth path through `Buildable` and `Contains`.
- `cargo mock --lint-only --commit` clean.
