# Src changelist — round 202605232020 (runtime megaround deferrals)

**Date:** 2026-05-23
**Phase:** SRC
**Scope:** Three deferrals from the megaround that the locked Pass 7 src CL named but did not ship: trybuild fixture, MIRI flag documentation, and `ema_update` slice-type pivot.
**Source topics:** `202605232020_topic.runtime-megaround-deferrals.md`.
**Prior rounds:** `202605101036` (megaround, locked, closed) + `202605232000` (post-review fixes, locked, closed).

## CHANGE: trybuild fixture for double-derive rejection

File: `mock/crates/hilavitkutin-api/tests/trybuild/double_derive_runcfg.rs` (NEW)
Plus: `mock/crates/hilavitkutin-api/tests/trybuild.rs` (NEW, the harness runner) and `mock/crates/hilavitkutin-api/Cargo.toml` (adds `trybuild` to dev-dependencies).
Topic A1. The fixture combines `#[derive(Resource)] + #[derive(RunCfg)]` on the same type and expects a coherent compile error per Topic 3 S2 + audit-2 M5. The expected `.stderr` lands alongside it. Pass 7 of round 202605101036 locked this verification but the file did not ship.
Verification: `test -f mock/crates/hilavitkutin-api/tests/trybuild/double_derive_runcfg.rs` returns success.

## CHANGE: MIRI flag documentation

File: `mock/crates/hilavitkutin/MIRI.md` (NEW)
Topic A2. The canonical `MIRIFLAGS="-Zmiri-strict-provenance -Zmiri-tree-borrows"` invocation, the test files that exercise atomic-ordering paths, and the pointer to mockspace task #203 for CI integration timing. Pass 7 named this file and named CI integration as deferred.
Verification: `test -f mock/crates/hilavitkutin/MIRI.md` returns success.

## CHANGE: `ema_update` slice-type pivot from USize to Sample

File: `mock/crates/hilavitkutin-providers/src/adapt_ema.rs`
Topic A3. Replace `&mut [USize]` / `&[USize]` with `&mut [Sample]` / `&[Sample]` where `Sample` is a new `pub type Sample = UFixed<{ibits(64)}, {fbits(0)}, Hot>` alias declared alongside `BlendFactor`. The 64-bit unsigned shape is the canonical EMA-sample width: large enough for nanosecond timing, record counts, and bytes-allocated metrics. The body remains the structural stub; the bench-validated kernel still lands with the executor wiring in a subsequent round.
Verification: `grep 'pub type Sample = UFixed' mock/crates/hilavitkutin-providers/src/adapt_ema.rs` returns one match.

## Lock criteria

1. `cargo +nightly check --workspace --tests` clean.
2. `cargo mock` lint gate green.
3. All three `## CHANGE:` verification commands return their stated result.
