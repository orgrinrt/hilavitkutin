# Src CL: Catalogue the Aliasing Finding as a Test

**Date:** 2026-07-19
**Round topic:** `202607201700_topic.pre-freeze-evidence-followups.md`
**Doc CL:** `202607201800_changelist.doc.lock.md`

## CHANGE: `tests/replace_swap.rs` gains the #689 catalogue entry

File: `mock/crates/hilavitkutin/tests/replace_swap.rs`
One ignored test, `parallel_swap_pattern_is_aliasing_clean`, cataloguing
the audit-confirmed aliasing hole (record `202607201600`): the intended
`run_parallel; replace_value; run_parallel` pattern materialises a
`&mut Scheduler` while a parked worker holds a live `&Scheduler`. The
entry documents the pattern and its resolution (the scheduler-plane
relocation round resolving GATE-2 deviations 1, 5, and 6 together) and
unignores as a Miri-gated check when that round lands. Not natively
observable, so the body carries the intended Miri assertion in prose;
per the catalogue standard the ignore reason names the gap and the
tracked task.
Verification: `grep -c "tracked #689" mock/crates/hilavitkutin/tests/replace_swap.rs` returns 1.
