# Topic: Pre-Freeze Evidence Follow-Ups

**Date:** 2026-07-19
**Scope:** the small source-side artifacts the pre-freeze evidence batch
produces as it runs: catalogued tests for findings the evidence memos
record, and nothing larger. Substantive correctives (the scheduler-plane
relocation resolving GATE-2 deviations 1, 5, and 6; the spectral role
restoration) are their own future rounds gated on op's
evidence-then-bless rulings; this round only keeps the catalogue-as-test
discipline honest while the batch runs.

## Current slice

The #689 aliasing audit (record `202607201600`) confirmed the
between-frames `&mut Scheduler` versus parked-worker `&Scheduler`
aliasing hole; the finding is catalogued as an ignored test in
`tests/replace_swap.rs` naming the pattern and its resolution (the
plane relocation round), unignoring as a Miri-gated check when that
round lands.
