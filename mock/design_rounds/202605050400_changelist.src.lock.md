**Date:** 2026-05-05
**Phase:** SRC
**Round:** 202605050400

# Source changelist: em-dash sweep across hilavitkutin source

Pure trivial mechanical text-replacement sweep. 109 em-dash and en-dash occurrences pre-sweep, zero post-sweep, across approximately 30 source files spanning hilavitkutin, hilavitkutin-api, hilavitkutin-build, hilavitkutin-ctx, hilavitkutin-extensions, hilavitkutin-extensions-macros, hilavitkutin-linking, hilavitkutin-persistence, hilavitkutin-providers, hilavitkutin-str.

Per `cl-claim-sketch-discipline.md`: trivial mechanical sweeps do not need per-line `## CHANGE:` blocks. The claim is bulk and verifiable.

## CHANGE: bulk-replace em-dash and en-dash variants in all hilavitkutin source

Replacement rules applied:

- ` — ` (space + U+2014 + space): replaced with `: ` (colon + space).
- bare `—` (no spaces): replaced with `, ` (comma + space).
- ` – ` (space + U+2013 + space): replaced with ` to ` (range word).
- bare `–`: replaced with `-` (ASCII hyphen).

Verification: `! rg '—|–' mock/crates/ --type rust` returns zero matches post-sweep.

## Verification commands

```sh
# No remaining em-dash or en-dash in any Rust source:
! rg '—|–' mock/crates/ --type rust

# Workspace compiles:
cargo +nightly check
```

## Out of scope

- Em-dash sweep across mock/agent/ templates is gated by the writing-style lint already; not a goal for this round.
- Em-dash sweep across DESIGN.md.tmpl files is gated by the same lint; not in this round.
- Round artefact files (mock/design_rounds/*) are not gated but follow the rule by discipline; this round's own artefacts are em-dash clean.
