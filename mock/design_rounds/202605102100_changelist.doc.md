**Date:** 2026-05-10
**Phase:** DOC
**Round:** 202605102100 (PR #73 review fixups)

# Doc CL — sweep extensions-macros DESIGN + add BACKLOG entry

## File: mock/crates/hilavitkutin-extensions-macros/DESIGN.md.tmpl

Sweep stale Capability → Provider references that round-202605091700 missed. Identifiers to rewrite:

- `CapabilityEntry` → `ProviderEntry`
- `CapabilityId` → `ProviderId`
- `CapabilityExport` → `ProviderExport`
- `required_host_caps` → `required_host_providers`
- `capabilities_len` → `providers_len`
- `capabilities_ptr` → `providers_ptr`
- `CAPABILITIES` → `PROVIDERS` (the static const name in the emit shape — also tracked in src CL)
- `CAP_A`, `CAP_B` → `PROVIDER_A`, `PROVIDER_B` (illustrative const names in attribute parameter prose)
- `CapabilityId::from_name` → `ProviderId::from_name`
- "capability table" / "capability lookup" / "Per-capability trampolines" / "shared-capability helpers" → "provider table" / "provider lookup" / "Per-provider trampolines" / "shared-provider helpers"
- Method spellings (`fn capability(`) → `fn provider(`

The macro source (`crates/hilavitkutin-extensions-macros/src/lib.rs`) already emits the new names since the prior round's source-side rename. The DESIGN drift is doc-only.

## File: mock/crates/hilavitkutin-api/BACKLOG.md.tmpl

Append a new section `### Wire Resource::new to the scheduler-owned resource registry`. Body explains that `Resource::new(value: T)` currently consumes T but drops it at construction (D1 in `mock/research/notes/202605091700_src_apply_decisions.md`); when HILA-RUNTIME-C6 ships the persistence spine, the constructor will route the value into scheduler-owned storage. The transitional drop matches the prior `add_resource(_init: T)` shape and is documented in the constructor's doc comment, but a BACKLOG signal makes the obligation discoverable when C6 lands.

## Lock criteria

`cargo mock --lint-only --commit` shows no `design-doc-source-mismatch` violation in `hilavitkutin-extensions-macros`.
