# Changelist (src): round D adoption review fixes

**Date:** 2026-04-29
**Phase:** SRC
**Source topics:** `202604292117_topic.round-d-adoption-review-fixes.md`

## Status

SRC phase. Three source-side adjustments addressing reviewer findings.

## Per-file edits

`crates/hilavitkutin-str/src/interner.rs` — `intern` and `intern_static`. Add a comment block at each call site explaining that `Bits::<28, Hot>::from_raw(id)` is transparent (no masking) and that the 28-bit invariant is enforced by `StrLayout::with_id` (the bitfield-generated setter masks input to the field width). Cite `tests/handle.rs::make_masks_to_28_bits` as the contract anchor.

`crates/hilavitkutin-persistence/src/archive_str.rs:34`. Parenthesise the `as u64` cast: `const_fnv1a(s) & (Str::ID_MASK.to_raw() as u64)`. The expression's parse is correct under Rust's precedence (`as` binds tighter than `&`), but the parenthesised form removes any ambiguity for readers not holding the precedence table in head.

`crates/hilavitkutin-persistence/tests/archive_str.rs:50`. Same parenthesisation in the `content_hash` helper.

## Verification

`cargo check --workspace` from `mock/`: green.
`grep -n 'as u64)' mock/crates/hilavitkutin-persistence/src/archive_str.rs mock/crates/hilavitkutin-persistence/tests/archive_str.rs` shows the parenthesised form at the two sites.
`grep -n 'StrLayout::with_id' mock/crates/hilavitkutin-str/src/interner.rs` shows the invariant carrier referenced in the comment.

## Comparison to deprecated changelist

Not a deprecation continuation.

## Round close

After verification: `cargo mock lock` to lock the src CL, commit the rename, `cargo mock close` to archive the round, push.
