# Topic: P1 R3b post-review fixes

**Date:** 2026-06-20
**Crate:** hilavitkutin
**Tracked:** #341, #691, completion-arc R3b (PR #144 review)

## Fixes

PR #144 review surfaced three points:

1. The DESIGN.md.tmpl sentence on `change_seen_count` lacked a single-core qualifier; the impl increments only in the single-core `run` fold, not `run_parallel`/`run_fused`. Qualify the prose so the doc (the oracle) matches what ships.
2. The parallel/fused divergence (those folds reset `store_dirty` without counting it, so `change_seen_count` reads stuck-at-zero beside live siblings there) is tracked as #691 and recorded in BACKLOG.
3. The new test omitted the bare-numeric module-doc note its sibling `r3c_throughput_record_count.rs` carries. Add it for parity.
