**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** engine-resource-sweep source CL

# `mock/crates/hilavitkutin/src/resource/accumulator.rs`

Add import: `use arvo::USize;`.

- `pub struct ConvergenceBuffer<T: Copy, const N: usize>` — trailing
  inline lint:allow for `const N: usize`.
- `impl<T: Copy, const N: usize> ConvergenceBuffer<T, N>` — trailing
  inline lint:allow.
- `pub fn get(&self, i: usize) -> T` → `pub fn get(&self, i: USize) -> T`,
  body derefs `self.slots[*i].value`.
- `pub fn set(&mut self, i: usize, v: T)` → `pub fn set(&mut self, i: USize, v: T)`,
  body derefs `self.slots[*i].value = v`.

# `mock/crates/hilavitkutin/src/resource/cache.rs`

Add import: `use arvo::USize;`.

- `pub struct ResourceCache<'a, R: AccessSet, const N: usize>` —
  trailing inline lint:allow.
- `impl<'a, R: AccessSet, const N: usize>` — trailing inline lint:allow.
- `pub fn get(&self, i: usize) -> Slot` → `pub fn get(&self, i: USize) -> Slot`;
  forwards `self.snapshot.get(i)` unchanged (snapshot's `get` will
  accept `USize` after that file's update).
- `pub fn set(&mut self, i: usize, v: Slot)` → `pub fn set(&mut self,
  i: USize, v: Slot)`; forwards unchanged.

# `mock/crates/hilavitkutin/src/resource/snapshot.rs`

Add imports:
```rust
use arvo::newtype::{FBits, IBits};
use arvo::strategy::Hot;
use arvo::ufixed::UFixed;
use arvo::USize;
```

## `Slot` payload rewrap

Before:
```rust
#[derive(Copy, Clone, Default)]
#[repr(transparent)]
pub struct Slot(pub u64);
```
After:
```rust
#[derive(Copy, Clone, Default)]
#[repr(transparent)]
pub struct Slot(pub UFixed<{ IBits(64) }, { FBits::ZERO }, Hot>);
```

Initialiser substitution: `Slot(0)` → `Slot(UFixed::from_raw(0))` at
every call site in this file.

## `ResourceSnapshot`

- `pub struct ResourceSnapshot<const N: usize>` — trailing inline
  lint:allow.
- `impl<const N: usize> Default for ResourceSnapshot<N>` — trailing
  inline lint:allow.
- `impl<const N: usize> ResourceSnapshot<N>` — trailing inline
  lint:allow.
- `pub fn get(&self, i: usize) -> Slot` → `pub fn get(&self, i: USize) -> Slot`,
  body `self.slots[*i]`.
- `pub fn set(&mut self, i: usize, v: Slot)` → `pub fn set(&mut self,
  i: USize, v: Slot)`, body `self.slots[*i] = v`.

# Validation

- `cargo check` clean across all crates.
- `cargo mock --lint-only | grep resource/` returns nothing.
