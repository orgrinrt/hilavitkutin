# Src changelist: per-fiber morsel A3a

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606202500

## CHANGE: add `StoreElemBytes` + `StoreSizes` + `store_sizes` to plan/project.rs
File: `mock/crates/hilavitkutin/src/plan/project.rs`
Verification: `grep "pub trait StoreElemBytes\|pub trait StoreSizes\|pub fn store_sizes" mock/crates/hilavitkutin/src/plan/project.rs` returns three matches.

Per-marker `StoreElemBytes { const BYTES }` (Column/Resource/Accum/StagedResource
over `T: ColumnValue` -> `(BIT_WIDTH+7)/8`; Virtual -> ZERO); `StoreSizes<CS>` fold
over the Stores cons-list; `store_sizes::<Stores, CS>()` entry. Mirrors the
existing `StoreAccumKind`/`AccumStoresMask`/`accum_stores_mask`.

## NEW TEST: `tests/a3a_store_sizes.rs`
File: `mock/crates/hilavitkutin/tests/a3a_store_sizes.rs`
Verification: `cargo test -p hilavitkutin --test a3a_store_sizes` passes.

A known 4-store carrier (`Column<Uint<14>>`, `Column<Uint<11>>`, `Column<Uint<27>>`,
`Resource<Bool>`) asserts `store_sizes` yields each store's `size_of` element bytes
and a zero slack tail.
