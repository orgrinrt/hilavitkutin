# Doc changelist: hilavitkutin-api-macros crate + ResourceFootprint derive

**Date:** 2026-06-20
**Phase:** DOC
**Round:** 202606210300

Adding the 10th crate (`hilavitkutin-api-macros`, 2nd proc-macro crate) makes the
"nine crates / sole proc-macro crate" claims stale. Update the authored sources:

- `mock/DESIGN.md.tmpl`: add the `hilavitkutin-api-macros` row to the crate table;
  fix the `hilavitkutin-extensions-macros` row's "Only proc-macro crate" wording.
- `mock/agent/MAIN.md.tmpl`: crate count + crate table + the "sole proc-macro crate"
  phrasing.
- `mock/agent/rules/cargo.md.tmpl`: "Nine crates ... one proc-macro" -> "Ten crates ...
  two proc-macro"; add the crate to the crate-structure list + dependency-direction
  list; update the "exactly one proc-macro crate" forbidden-crate-types bullet to "two".

The new crate's purpose: ships `#[derive(ResourceFootprint)]`, the derive that lets
every resource value type report its L1-morsel write-collection footprint (canonical
R5). Companion to `hilavitkutin-api` as `hilavitkutin-extensions-macros` is to
`hilavitkutin-extensions`.
