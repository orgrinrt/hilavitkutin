# Src changelist: hilavitkutin-api-macros crate + ResourceFootprint derive

**Date:** 2026-06-20
**Phase:** SRC
**Round:** 202606210300

## NEW CRATE: hilavitkutin-api-macros
Files: `mock/crates/hilavitkutin-api-macros/Cargo.toml` (new, `[lib] proc-macro = true`, proc-macro2 + syn + quote), `src/lib.rs` (new).
Verification: `cargo build -p hilavitkutin-api-macros` succeeds.

Ships `#[proc_macro_derive(ResourceFootprint)]`. Syntactic field-walk: for each
named/unnamed struct field whose type path's last segment is `Field`/`Seq`/`Map`,
emit `<#ty as ::hilavitkutin_api::footprint::CollectionBytes>::BYTES.0`; sum into
`impl ResourceFootprint for #name { const L1_BYTES = ::arvo::USize(Σ) }`. Plain
fields contribute 0 (skipped). Carries the input's generics.

## CHANGE: workspace registration
File: `mock/Cargo.toml`
Verification: `grep "hilavitkutin-api-macros" mock/Cargo.toml` returns matches in members + [workspace.dependencies].

## CHANGE: mockspace.toml proc_macro_crates + forbidden-imports scope
File: `mock/mockspace.toml`
Verification: `grep "hilavitkutin-api-macros" mock/mockspace.toml` returns matches in proc_macro_crates + a new [lints.*] forbidden-imports scope (error level; emission references ::hilavitkutin_api::* only). ADDITIVE new-crate boundary, not a severity change.

## NEW TEST: derive_resource_footprint
File: `mock/crates/hilavitkutin-api-macros/tests/derive_resource_footprint.rs`
Verification: `cargo test -p hilavitkutin-api-macros` passes; a fixture resource with Field/Seq/Map fields + `#[derive(ResourceFootprint)]` asserts `L1_BYTES == N_seq*elem + N_map*(k+v)` (Field 0).
