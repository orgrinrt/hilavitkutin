**Date:** 2026-05-25
**Phase:** SRC
**Scope:** HILA-RUNTIME-C4 base assign_cores source CL. One source edit replacing the `todo!()` body with the round-robin assignment. One test module addition exercising three fixtures.
**Source TOPIC:** `mock/design_rounds/202605250545_topic.hila-c4-assign-cores-base.md`.
**Source DOC:** `mock/design_rounds/202605250545_changelist.doc.lock.md`.

# Source changes

## CHANGE: `assign_cores` body FROM `todo!()` TO round-robin assignment in thread/mod.rs
File: `mock/crates/hilavitkutin/src/thread/mod.rs`
Verification: `! grep -A 2 'pub fn assign_cores' mock/crates/hilavitkutin/src/thread/mod.rs | grep -q 'todo!' && grep -q 'fn assign_cores' mock/crates/hilavitkutin/src/thread/mod.rs`.

The `todo!()` body at line 70 is replaced with a round-robin assignment per the DOC CL's algorithm: compute `max_trunks_needed` as the max over `plan.phases[0..plan.phase_count].trunk_count`, set `width = min(max_trunks_needed, core_count.0, MAX_LANES)`, populate `trunk_index[0..width] = [USize(0)..USize(width-1)]` leaving the rest at `NO_TRUNK`, set `assigned_count = USize(width)`. `fiber_assignments` and `morsel_size_multiplier` keep their `CoreAssignment::new()` defaults (FiberId::ZERO and USize(100)); both wait for follow-up slices.

## CHANGE: tests module ADDED to thread/mod.rs covering three fixtures
File: `mock/crates/hilavitkutin/src/thread/mod.rs`
Verification: `grep -q '#\[cfg(test)\]' mock/crates/hilavitkutin/src/thread/mod.rs && grep -q 'mod assign_cores_tests' mock/crates/hilavitkutin/src/thread/mod.rs`.

Three tests:

1. `empty_plan_returns_default_assignment`: `phase_count = 0` plan; expect `assigned_count == USize::ZERO` and every `trunk_index[i] == NO_TRUNK`.
2. `single_phase_single_trunk_4_cores`: plan with `phase_count = 1`, phase[0].trunk_count = 1, 4 cores; expect `trunk_index[0] == USize(0)`, `trunk_index[1..3] == NO_TRUNK`, `assigned_count == USize(1)`.
3. `three_phases_max_3_trunks_4_cores`: plan with three phases of `[2, 3, 1]` trunks, 4 cores; expect `assigned_count == USize(3)`, `trunk_index = [USize(0), USize(1), USize(2), NO_TRUNK]`.

The tests construct `ExecutionPlan` directly (all fields are `pub`); helper closure builds the plan with the per-phase trunk counts.

# Lock criteria

- Both `## CHANGE:` verification predicates pass.
- `cargo build` clean.
- `cargo test --workspace` clean.
- Three new assign_cores tests pass.
- `cargo mock --lint-only --commit` clean.
