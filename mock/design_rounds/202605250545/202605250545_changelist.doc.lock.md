**Date:** 2026-05-25
**Phase:** DOC
**Scope:** HILA-RUNTIME-C4 base assign_cores DOC CL. Adds a DESIGN section recording the base assignment shape. Adds a BACKLOG entry naming the CoreClass-aware follow-up.
**Source TOPIC:** `mock/design_rounds/202605250545_topic.hila-c4-assign-cores-base.md`.

# Doc changes

## CHANGE: doc section `## assign_cores base implementation` ADDED to hilavitkutin DESIGN.md.tmpl
File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`
Verification: `grep -q "^## assign_cores base implementation" mock/crates/hilavitkutin/DESIGN.md.tmpl`.

New DESIGN section documenting the base `assign_cores` body. Pipeline-wide round-robin assignment: `width = min(max_trunks_per_phase, core_count, MAX_LANES)`, `trunk_index[0..width] = [USize(0), USize(1), ..., USize(width-1)]`, slots beyond width stay `NO_TRUNK`. `fiber_assignments` stays at `FiberId::ZERO` default and `morsel_size_multiplier` stays at `USize(100)` (1.0x); both wait for follow-up slices to populate. Names the lock criteria: `cargo build` + `cargo test --workspace` clean, three new tests pass.

## CHANGE: BACKLOG entry `CoreClass-aware assign_cores follow-up` ADDED to hilavitkutin/BACKLOG.md.tmpl
File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Verification: `grep -q "CoreClass-aware assign_cores" mock/crates/hilavitkutin/BACKLOG.md.tmpl`.

New entry naming the follow-up: extend `assign_cores` to consume `classify_cores()` output, prefer P-cores for the first `width` trunks (so the critical-path slots land on performance cores), and set `morsel_size_multiplier[i] = USize(200)` for P-core slots and `USize(100)` for E-core slots. Sequencing: lands after `classify_cores` returns non-stub data on at least one platform (today every detect_into stubs out, so the heterogeneous path is a no-op until the detection sysfs/sysctl/CPUID probes land).

# Verification target

After the round ships: `cargo build` clean, `cargo test --workspace` clean, three new assign_cores tests pass. `cargo mock --lint-only --commit` clean.
