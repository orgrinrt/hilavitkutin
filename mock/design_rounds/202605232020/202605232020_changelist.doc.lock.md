# Doc changelist — round 202605232020 (runtime megaround deferrals)

**Date:** 2026-05-23
**Phase:** DOC
**Scope:** No template-side changes. The three deferrals close locked Pass 7 src CL claims from the prior round that did not land in source; bringing source up to match those claims requires no DESIGN/BACKLOG/SHAME edits.
**Source topics:** `202605232020_topic.runtime-megaround-deferrals.md`.

## Order of operations

1. No `.md.tmpl` edits.
2. `cargo mock lock` to freeze the doc CL.
3. Open src CL with the three deferral fixes.

## Mock root templates

No changes.

## Per-crate `DESIGN.md.tmpl` updates

None. The prior round's DESIGN entries for the trybuild fixture, MIRI flag documentation, and `ema_update` slice type already describe the intended state; this round's src changes bring source to match.

## Per-crate `BACKLOG.md.tmpl` updates

None.

## Why this doc CL is empty

The reviewer's deferrals are source-state items the locked Pass 7 src CL named but did not ship. The intended design surfaces (trybuild test harness, MIRI flag invocation, `ema_update`'s sample-shape) were locked in the prior round. This round just ships them.

Two trivial files outside `mock/` (`crates/hilavitkutin/MIRI.md`, `crates/hilavitkutin-api/tests/trybuild/double_derive_runcfg.rs`) fall outside the mockspace template gate; they need only the src CL to authorise the change.
