# Doc changelist: the spawn-gate lint accepts a forcing that gates nothing

**Date:** 2026-09-19
**Phase:** DOC
**Topic:** `202609191200_topic.spawn-gate-lint-forcing.md`

N1 changes no design document and is source-only.

## CHANGE: the thread-count paragraph FROM ending on the engine's current status TO the rule alone

File: `mock/PRINCIPLES.md.tmpl` (Pre-allocated thread pool).

The sentence "The engine does not honour this yet: it hands out one closure per worker whatever the plan's width." is removed. The gap stays recorded where implementation status belongs, the `FIXME:` at the `runnable_worker_count` call in `scheduler/run_parallel.rs` and the ignored catalogue test in `tests/gate2_worker_count.rs`.

## CHANGE: the generated documents FROM stale TO regenerated

Files: `docs/` at the repository root, by `cargo mock`.
