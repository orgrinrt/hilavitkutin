**Date:** 2026-05-04
**Phase:** SRC
**Scope:** hilavitkutin-extensions src/host.rs + src/extension.rs
**Source topics:** 202605041629 (D1 shutdown observer)

# Source changelist: shutdown observer

Mechanical execution of the locked doc CL.

## CHANGE: type `ShutdownObserverFn` ADD pub type alias in `host.rs`

File: `mock/crates/hilavitkutin-extensions/src/host.rs`

Add a new `pub type ShutdownObserverFn = fn(name: &[u8], status: ExtensionAbiStatus);` alongside `FailurePolicyFn`, with a doc comment matching the topic file's prose.

Verification: `grep -c "pub type ShutdownObserverFn" mock/crates/hilavitkutin-extensions/src/host.rs` returns 1.

## CHANGE: struct `ExtensionHost` ADD field `observer: Maybe<ShutdownObserverFn>`

File: `mock/crates/hilavitkutin-extensions/src/host.rs`

Append `observer: Maybe<ShutdownObserverFn>` to the `ExtensionHost` struct fields. Initialise to `Maybe::Isnt` in `ExtensionHost::new`. Add inherent method `pub fn with_shutdown_observer(mut self, observer: ShutdownObserverFn) -> Self` that sets it.

Verification: `grep -c "with_shutdown_observer" mock/crates/hilavitkutin-extensions/src/host.rs` returns at least 1, and `grep -c "observer: Maybe<ShutdownObserverFn>" mock/crates/hilavitkutin-extensions/src/host.rs` returns 1 (the field declaration).

## CHANGE: fn `ExtensionHost::load` THREAD `self.observer` INTO `Extension::from_parts`

File: `mock/crates/hilavitkutin-extensions/src/host.rs`

Update the success-path call site to pass `self.observer` as the new fourth argument.

Verification: `grep -n "Extension::from_parts" mock/crates/hilavitkutin-extensions/src/host.rs` shows the call passes four arguments and the last one references `self.observer`.

## CHANGE: struct `Extension` ADD field `observer: Maybe<ShutdownObserverFn>`

File: `mock/crates/hilavitkutin-extensions/src/extension.rs`

Append `observer: Maybe<ShutdownObserverFn>` to the `Extension` struct fields. Update `Extension::from_parts` signature to accept `observer: Maybe<ShutdownObserverFn>` and store it. Update the `close()` body to fire the observer with `(self.name(), status)` after the shutdown call (passing `ExtensionAbiStatus::Ok` if no `shutdown_fn` was declared). Update the `Drop::drop` body to fire the observer in the same shape.

Verification: `grep -c "observer: Maybe<ShutdownObserverFn>" mock/crates/hilavitkutin-extensions/src/extension.rs` returns 1, and `grep -c "observer(" mock/crates/hilavitkutin-extensions/src/extension.rs` returns at least 2 (one in `close()`, one in `Drop`).

## What is NOT changed

- `ExtensionHost::new` public signature. Builder method is additive.
- `ExtensionRequirement`, `PolicyVerdict`, `FailurePolicyFn`, `default_policy`. Unchanged.
- `Extension::descriptor`, `capability`, `capabilities`, `name`, `version` accessors. Unchanged.
- `Extension::close` public signature. Still `fn(self) -> Outcome<(), ExtensionError>`. Observer fires inside it before any error propagates.
- The `ExtensionDescriptor` and `ExtensionAbiStatus` types. Unchanged.
- No tests added. The observer firing is exercised under an integration scenario that would require a built cdylib extension; this round adds the API surface only.

## Lock criteria

- [ ] All four verification greps above succeed.
- [ ] `cargo check -p hilavitkutin-extensions` passes on host platform.
- [ ] `cargo test -p hilavitkutin-extensions` passes (existing tests still run; no regressions).
- [ ] `cargo mock --lint-only --build` passes scoped to `hilavitkutin-extensions`.
