# Src CL: P1 R3b change_class

**Round:** 202606200000
**Phase:** IMPL

## CHANGE: ADD field `change_seen_count` to `SchedulerMetrics`
File: `mock/crates/hilavitkutin-api/src/meta.rs`
`pub change_seen_count: Cell<USize>` after `last_record_count`, plus Default `Cell::new(USize(0))`.
Verification: `grep -c "change_seen_count" mock/crates/hilavitkutin-api/src/meta.rs` >= 2.

## CHANGE: increment `change_seen_count` in the single-core `run` fold when store_dirty was non-empty
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Capture `let stores_changed = !self.store_dirty.get().is_empty().0;` before the store_dirty reset (~1368); after the EMA fold, `if stores_changed { m.change_seen_count.set(USize(m.change_seen_count.get().0 + 1)); }`.
Verification: `grep -c "change_seen_count.set" mock/crates/hilavitkutin/src/scheduler/mod.rs` == 1.

## CHANGE: ADD `#[doc(hidden)] pub fn __mark_store_dirty` white-box test accessor
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Sets `store_dirty` to a non-empty mask (`AccessMask::empty().set(USize(0))`) so a test can trigger the change signal without the sealed `PlanAffecting` path. Consistent with the existing `__bindings`/`__ptr`/`__len_cell` doc-hidden test accessors.
Verification: `grep "__mark_store_dirty" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns a match.

## CHANGE: ADD test `tests/r3b_change_class_count.rs`
File: `mock/crates/hilavitkutin/tests/r3b_change_class_count.rs`
An `OnMeta<ScheduleEnd>` consumer appends `change_seen_count`. Frame 1: `__mark_store_dirty` then run (consumer reads pre-increment 0; fold increments to 1). Frame 2: clean run (consumer reads 1; no increment). Asserts the frame-2 read is 1 (the frame-1 change counted through the bridge). White-box via the accessor since `store_dirty`'s only public trigger is sealed.
Verification: `cargo test -p hilavitkutin --test r3b_change_class_count` passes.

Single-core `run` only; parallel/fused follow.
