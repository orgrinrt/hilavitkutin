# Doc CL: P1 R3b change_class

**Round:** 202606200000
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

One sentence: `SchedulerMetrics` carries a `change_seen_count` cell, incremented
each `run` frame whose `store_dirty` was non-empty, so an `OnMeta` hook derives
the input-change rate (change_class axis) from existing dirty state.
