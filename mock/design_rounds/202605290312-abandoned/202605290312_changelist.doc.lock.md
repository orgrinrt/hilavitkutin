**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605290312 DependencyGraph to CsrBidirectional adapter (C1a)

# Doc changelist

## No design-document edits

C1a adds one method (`DependencyGraph::to_csr_bidirectional`) plus a Cargo dep
(`arvo-tensor`). It introduces no new type. The `design-doc-source-mismatch`
lint checks backticked type names in `DESIGN.md.tmpl` exist as pub items; a
method on an existing type is not in scope, and the plan-stage DESIGN does not
enumerate per-method surface. The adapter is internal plumbing the later C steps
consume, not a new consumer-facing design concept, so the topic file plus the
src changelist CHANGE claims carry the record.

The arvo dependency addition is a Cargo manifest change, recorded as a CHANGE in
the src changelist.
