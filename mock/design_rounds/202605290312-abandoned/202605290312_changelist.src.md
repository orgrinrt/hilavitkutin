**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605290312 DependencyGraph to CsrBidirectional adapter (C1a)

# Source changelist

## CHANGE: add `arvo-tensor` dependency
Files: `mock/Cargo.toml` (workspace.dependencies), `mock/crates/hilavitkutin/Cargo.toml`
Add `arvo-tensor = { git = ..., branch = "dev", default-features = false }` to `[workspace.dependencies]` and `arvo-tensor.workspace = true` to the engine crate. `cargo update -p arvo-tensor` so the arvo git source resolves to a commit including `cap` (arvo dev `1840531`).
Verification: `grep "arvo-tensor" mock/crates/hilavitkutin/Cargo.toml` returns a match; `cargo build -p hilavitkutin` resolves `arvo_tensor::cap`.

## CHANGE: add `DependencyGraph::to_csr_bidirectional`
File: `mock/crates/hilavitkutin/src/plan/graph.rs`
Add `pub fn to_csr_bidirectional(&self) -> arvo_sparse::CsrBidirectional<{cap(MAX_UNITS)}, {cap(MAX_EDGES)}, EdgeKind>` with `where [(); cap_size(cap(MAX_UNITS))]:, [(); cap_size(cap(MAX_EDGES))]:`. Build a `Csr` (row_ptr from row_offsets within unit_count; col_idx from each col_indices[e] as `NodeId::new(uid.index())` and values from edge_kinds[e] within edge_count), then `.with_transpose()`. Uses `arvo_tensor::{cap, cap_size}`, `arvo_sparse::{Csr, CsrBidirectional}`, `arvo_bitmask::NodeId`.
Verification: `grep "pub fn to_csr_bidirectional" mock/crates/hilavitkutin/src/plan/graph.rs` returns one match; `cargo test -p hilavitkutin` builds it.

## CHANGE: test the adapter
File: `mock/crates/hilavitkutin/src/plan/graph.rs` (inline `#[cfg(test)]`) or `mock/crates/hilavitkutin/tests/`
Build a small known DAG, call `to_csr_bidirectional`, assert per-row `nnz` / `row_col_indices` / `row_values` match the source edges and the transpose carries the reverse adjacency.
Verification: `cargo test -p hilavitkutin` runs the new adapter test green.
