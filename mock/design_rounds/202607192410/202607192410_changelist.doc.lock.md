# Doc changelist: the docs already describe the result

**Date:** 2026-07-19
**Phase:** DOC
**Topic:** `202607192400_topic.delete-the-n-way-record-split.md`

No template changes. Round `202607192100` already rewrote every passage that described the branch,
in advance of removing it:

- `hilavitkutin/DESIGN.md.tmpl:299-307` now says single-trunk phases get head+tail convergence, that
  the mechanism is not built yet so such phases run serially, and that an earlier engine split them
  N ways and this paragraph wrongly called that convergence.
- `:23` marks head+tail convergence designed-but-not-built in the domain table.
- `:553-560` describes the accumulator regioning on its own terms, citing accumulator canon rather
  than convergence canon.

That ordering was deliberate: the prose was wrong independently of when the code changed, and
leaving it would have kept confirming the drift to anyone checking the source against the design.

The consequence for this round is that the doc phase has nothing to do, and the src phase brings the
code into line with docs that already describe it. Recording the empty changelist rather than
skipping the phase, so the round's shape stays legible: the design moved first, the source follows.

## Verification after the src phase

`grep -c "head+tail" mock/crates/hilavitkutin/DESIGN.md.tmpl` is unchanged by this round, and no
backticked type named in the templates disappears from source.
