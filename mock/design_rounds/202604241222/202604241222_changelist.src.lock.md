**Date:** 2026-04-24
**Phase:** SRC-PLAN
**Scope:** `ExtensionDescriptor` function-pointer slots — `Option<fn>` to `MaybeNull<fn>`
**Source topics:** 202604241222_topic.maybenull-fn-at-descriptor-boundary.md

# Source edits

`mock/Cargo.lock` — bump the notko git revision to the dev tip that carries `notko::MaybeNull<T: NicheFilled>`.

`mock/crates/hilavitkutin-extensions/src/lib.rs` — add a `#[doc(hidden)] pub use notko::MaybeNull;` re-export so the proc-macro's emitted path resolves without forcing consumer crates to declare a direct `notko` dep purely for the emission.

`mock/crates/hilavitkutin-extensions/src/descriptor.rs` — `use notko::MaybeNull;` and swap both field types:

- `pub init_fn: Option<unsafe extern "C" fn(...) -> ExtensionAbiStatus>` becomes `pub init_fn: MaybeNull<unsafe extern "C" fn(...) -> ExtensionAbiStatus>`.
- `pub shutdown_fn: ...` same swap.

Drop both `// lint:allow(no-bare-option) reason: ...; tracked: #206` annotations on those fields.

`mock/crates/hilavitkutin-extensions/src/host.rs` — rewrite the init dispatch:

- `if let Some(init) = descriptor.init_fn` becomes `if let Maybe::Is(init) = descriptor.init_fn.into_maybe()`.

The surrounding `unsafe { init(host_ctx) }` body stays identical. `Maybe` already imported.

`mock/crates/hilavitkutin-extensions/src/extension.rs` — rewrite the two shutdown dispatches:

- `if let Some(shutdown_fn) = shutdown` (in `close`) becomes `if let Maybe::Is(shutdown_fn) = shutdown.into_maybe()`.
- `if let Some(shutdown) = self.descriptor.shutdown_fn` (in `Drop`) becomes `if let Maybe::Is(shutdown) = self.descriptor.shutdown_fn.into_maybe()`.

`mock/crates/hilavitkutin-extensions-macros/src/lib.rs` — macro emission swap:

- `quote! { Some(#fn_ident) }` becomes `quote! { ::hilavitkutin_extensions::MaybeNull::new(#fn_ident) }` at both init and shutdown call sites.
- `quote! { None }` becomes `quote! { ::hilavitkutin_extensions::MaybeNull::isnt() }` at both sites.

The emission routes through `hilavitkutin_extensions::MaybeNull` rather than `::notko::MaybeNull` so consumers do not need a direct `notko` dep purely for the proc-macro output.

# Validation

- `cargo build` across the mock workspace: clean.
- `cargo test -p hilavitkutin-extensions --lib`: 10/10 green.
- `cargo mock --lint-only`: clean.
- Two `lint:allow(no-bare-option)` annotations removed from `descriptor.rs`.

# Follow-up

Notko PR #2 renames `MaybeNull::isnt` to `MaybeNull::null` (and the predicates) for pointer-vocabulary clarity. Once that lands on notko `dev`, a follow-up round here syncs the emission path: `MaybeNull::isnt()` becomes `MaybeNull::null()`. The `doc(hidden)` re-export stays; only the method name changes.
