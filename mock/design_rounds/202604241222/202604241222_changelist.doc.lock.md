**Date:** 2026-04-24
**Phase:** DOC
**Scope:** `ExtensionDescriptor` function-pointer slots — `Option<fn>` to `MaybeNull<fn>`
**Source topics:** 202604241222_topic.maybenull-fn-at-descriptor-boundary.md

# Doc template edits

`mock/crates/hilavitkutin-extensions/DESIGN.md.tmpl` — Lifecycle contract section, two prose tweaks reflecting the `MaybeNull<fn>` shape:

- "If `init_fn` is `None`, init is a no-op." becomes "If `init_fn` is absent (`MaybeNull::isnt()`), init is a no-op."
- "If `None`, shutdown is a no-op." becomes "If absent (`MaybeNull::isnt()`), shutdown is a no-op."

No other doc templates reference these field types. `BACKLOG.md.tmpl` does not need changes; the `ExtensionHost::probe` deferral entry reads "without calling `init_fn`" without spelling the type.

# Validation

- `cargo mock --lint-only` clean.
