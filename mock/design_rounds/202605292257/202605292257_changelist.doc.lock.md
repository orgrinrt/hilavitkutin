# Doc changelist: plan rcm reorder wiring

**Date:** 2026-05-29
**Phase:** DOC
**Round:** 202605292257 (C1b, #639)

## Current state

`DESIGN.md.tmpl:45` lists plan step 5 (RCM) as "RCM reordering, fiber grouping order plus arena layout (arvo-sparse)". The consumption is named, but the doc does not say where the reorder result is carried, because step 4 (`steps::rcm_reorder`) is a pass-through stub whose output the runner discards.

## Changes proposed

### `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Refine the step-5 line to state that the RCM pass produces a renumber permutation persisted on `ExecutionPlan`, feeding arena layout now and fiber grouping order in a later sub-round. This reflects the source change landing in the src CL (a new `rcm_order` field on `ExecutionPlan`, populated by the runner instead of discarded). No other doc edits: the RCM consumption framing at line 45 is otherwise already accurate.
