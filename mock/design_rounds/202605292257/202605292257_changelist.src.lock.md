# Src changelist: plan rcm reorder wiring

**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605292257 (C1b, #639)

Wires plan step 4 to the real Reverse Cuthill-McKee reordering through the merged C1a adapter, and persists the resulting renumber permutation on the plan instead of discarding it. The permutation is a renumber for arena layout, not the dispatch order; dispatch stays topological.

## CHANGE: fn `rcm_reorder` FROM `pass-through stub taking (graph, topo) and returning *topo` TO `real RCM taking (graph) and returning the NodeId-to-UnitId-converted rcm_reorder_via permutation`
File: `mock/crates/hilavitkutin/src/plan/steps.rs:209`
Verification: `grep -n "rcm_reorder_via" mock/crates/hilavitkutin/src/plan/steps.rs` returns one match; the `topo` parameter is gone (rcm_reorder takes only `&DependencyGraph`).

## CHANGE: field `ExecutionPlan.rcm_order` FROM `absent` TO `pub rcm_order: [UnitId; cap_size(MAX_UNITS)]`
File: `mock/crates/hilavitkutin/src/plan/mod.rs`
Verification: `grep -n "pub rcm_order" mock/crates/hilavitkutin/src/plan/mod.rs` returns one match; `ExecutionPlan::new` initialises it to `[UnitId::ZERO; cap_size(MAX_UNITS)]`.

## CHANGE: runner `compute_execution_plan` step 4 FROM `let _reordered = steps::rcm_reorder(&dag, &topo)` TO `plan.rcm_order = steps::rcm_reorder(&dag)`
File: `mock/crates/hilavitkutin/src/plan/mod.rs:344`
Verification: `! grep -n "_reordered" mock/crates/hilavitkutin/src/plan/mod.rs` and `grep -n "plan.rcm_order = steps::rcm_reorder" mock/crates/hilavitkutin/src/plan/mod.rs` returns one match.

## CHANGE: test `tests/rcm_reorder.rs` FROM `absent` TO `strict chain-DAG RCM permutation test`
File: `mock/crates/hilavitkutin/tests/rcm_reorder.rs`
Verification: `cargo +nightly-2026-05-28 test -p hilavitkutin --test rcm_reorder` passes; the test asserts the chain 0->1->2->3 reorders to [3,2,1,0] over the live range.

## Doc-comment updates (mechanical, not source-state claims)

`steps.rs` module doc drops the `*Stub*` marker for step 4 and narrows the "Steps 4 to 6 are stubs" block to steps 5-6. `mod.rs` runner comment updates to reflect step 4 is wired while steps 5-6 remain stubs.
