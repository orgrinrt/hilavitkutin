**Date:** 2026-05-05
**Phase:** SRC
**Round:** 202605050600

# Source changelist: str test rehab

## CHANGE: str_const! macro brings BitsRefitCtor into scope

File: `mock/crates/hilavitkutin-str/src/macros.rs`
Adds `use ::arvo_bits::BitsRefitCtor as _;` at the start of the macro's expansion block. Resolves the `from_narrowed` method call without requiring consumers to import the trait.
Verification: `grep "use ::arvo_bits::BitsRefitCtor as _;" mock/crates/hilavitkutin-str/src/macros.rs` returns one match.

## CHANGE: feature(const_trait_impl) on three test files

Files:
- `mock/crates/hilavitkutin-str/tests/str_const.rs:1`
- `mock/crates/hilavitkutin-str/tests/ergonomics.rs:1`
- `mock/crates/hilavitkutin-str/tests/interner.rs:1`

Adds `#![feature(const_trait_impl)]` as the first line. Required because `from_narrowed` is on a `pub const trait` and the macro expands into a `static` initialiser, which is a const context.

Verification: `head -1 mock/crates/hilavitkutin-str/tests/{str_const,ergonomics,interner}.rs` shows the feature attribute on each.

## CHANGE: interner test uses Maybe::Is in place of Option::Some

File: `mock/crates/hilavitkutin-str/tests/interner.rs`
Adds `use notko::Maybe;` import.
Replaces three `interner.resolve(...), Some(...)` assertions with `Maybe::Is(...)` matching the `Maybe<&str>` return type of `StringInterner::resolve`.
Verification: `! grep "Some(" mock/crates/hilavitkutin-str/tests/interner.rs` returns zero matches in resolve assertions.

## Verification commands

`cargo +nightly check --tests -p hilavitkutin-str` returns clean.
