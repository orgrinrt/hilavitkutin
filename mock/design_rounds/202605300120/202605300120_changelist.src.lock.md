# Src changelist: spectral partition wiring (C1d-1)

**Date:** 2026-05-30
**Phase:** SRC
**Round:** 202605300120 (C1d-1, #642)

Wires plan step 6 to spectral partitioning via an engine-local symmetric Laplacian over arvo-spectral. No behaviour reaches the plan yet (the runner still discards the spectral output until C1d-3); this slice makes step 6 produce a real spectral `FiberGrouping` so the C1d-2 bench can compare it against greedy `group_fibers`.

## CHANGE: module `plan::laplacian` with `SymmetricLaplacian` FROM `absent` TO `engine-local LinearOperator over CsrBidirectional (symmetric unit-weight matvec)`
File: `mock/crates/hilavitkutin/src/plan/laplacian.rs`
Verification: `grep -n "pub struct SymmetricLaplacian" mock/crates/hilavitkutin/src/plan/laplacian.rs` returns one match; an `impl ... LinearOperator` for it; declared in `plan/mod.rs`.

## CHANGE: fn `spectral_partition` FROM `(graph) -> FiberGrouping returning new()` TO `(graph) -> FiberGrouping via SymmetricLaplacian + k_way_partition`
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
Verification: `grep -n "k_way_partition" mock/crates/hilavitkutin/src/plan/steps.rs` returns one match; `SymmetricLaplacian` is used; `[(); cap_size(MAX_FIBERS)]:` is added to the where-clause.

## CHANGE: test `tests/spectral_partition.rs` FROM `absent` TO `strict two-cluster spectral split test`
File: `mock/crates/hilavitkutin/tests/spectral_partition.rs`
Verification: `cargo +nightly-2026-05-28 test -p hilavitkutin --test spectral_partition` passes.

## Doc-comment updates (mechanical, not source-state claims)

`steps.rs` module doc drops the step-6 stub framing: steps 4 to 6 are now wired to arvo-sparse / arvo-spectral; the runner consumes steps 4 and 5, and step 6's output awaits C1d-3.
