**Date:** 2026-04-24
**Phase:** DOC
**Scope:** docs-level deliverables for round 202604241201. Source follows DESIGN; this CL records the BACKLOG housekeeping that falls out of implementing what DESIGN already declared.

**Source topic:**
- 202604241201_topic.extensions-conformance-and-macros-emission.md

# Purpose

The topic phase records the global decision: source follows DESIGN for every listed mismatch. The DESIGN for both `hilavitkutin-extensions` and `hilavitkutin-extensions-macros` already declares the target shapes; this round's SRC CL brings source into conformance and implements the macro emission body the DESIGN already specifies.

Consequently, no DESIGN.md.tmpl edits are required in this CL. The DOC CL covers BACKLOG updates only.

# D1. Update `hilavitkutin-extensions-macros/BACKLOG.md.tmpl`

Remove entries whose scope is now covered by this round's SRC CL:

- "Designed, deferred" section drops no entries wholesale; the emission itself is covered by DESIGN, not BACKLOG. The deferred items listed ("Compile-time check of capability completeness", "Enhanced error messages", "Shared capability-id interning", "Host-ABI-version build-time check", "`impl`-block mode", "Darling integration") remain. Add a leading paragraph noting that v1 emission lands in round 202604241201 per the src CL.

# D2. Update `hilavitkutin-extensions/BACKLOG.md.tmpl`

Promote to "Shipped in round 202604241201" any prior entries whose scope is satisfied by the conformance fixes and tests in this round. Items about hot-reload, manifest walkers, sandboxing, and cross-extension channels remain deferred.

# D3. Add a "Conformance sweep" note to `hilavitkutin-extensions/DESIGN.md.tmpl` cross-references

Extend the `Cross-references` section with a link to `design_rounds/202604241201/` once the round closes. Deferred to the close commit; no edit in this DOC CL beyond noting the intent.

# No other doc edits

`MAIN.md.tmpl`, `rules/cargo.md.tmpl`, `mockspace.toml`, `mock/DESIGN.md.tmpl`, and the crate `README.md.tmpl`s remain correct from round 202604240615. No re-generation needed for doc layer.

# Sequencing

1. Apply D1 (extensions-macros BACKLOG tweak).
2. Apply D2 (extensions BACKLOG promotion).
3. Commit doc changes.
4. Lock via `cargo mock lock`.
5. Write SRC CL.

# No new design content

Every shape the SRC CL implements appears in the already-locked DESIGN.md.tmpl files from round 202604240615. This CL introduces no new public types, methods, attributes, or constants. The sole effect is BACKLOG housekeeping.
