# Doc changelist: none, and the reason is the point

**Date:** 2026-07-19
**Phase:** DOC
**Topic:** `202607192300_topic.accumulator-region-sizing-under-trunk-rank.md`

No design template changes. The round exists to answer a question by experiment, and the answer is
not known yet, so there is nothing to write down as design.

Recording the empty changelist rather than skipping the phase, because the phase is where the
decision about what the docs will say gets made, and the decision here is "wait for the result".
Writing the prose first would mean writing the conclusion first, which is the failure this round
exists to avoid.

## What lands in docs once the answer is in

If accumulators turn out to be confined to single-trunk phases by construction, that constraint
belongs in `hilavitkutin/DESIGN.md.tmpl` beside the accumulator-region paragraph, since it is what
makes the region arithmetic sound and it is currently implicit.

If they are not, the region sizing rule changes and the same paragraph changes with it.

Either way the doc edit belongs to the round that acts on the result, not this one.
