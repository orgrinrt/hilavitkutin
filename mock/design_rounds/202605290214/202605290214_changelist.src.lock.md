**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605290214 engine-id index round-trip test

# Source changelist

## CHANGE: add `tests/engine_id_index.rs`
File: `mock/crates/hilavitkutin-api/tests/engine_id_index.rs` (new)
Adds one test per engine id asserting `Id::from_index(USize(k)).index() == USize(k)` at in-range values including each id's `Uint<N>` width ceiling (`UnitId` 65535, `FiberId` 127, `PhaseId` 31, `TrunkId` 63).
Verification: `cargo test -p hilavitkutin-api --test engine_id_index` passes; the four `*_index_roundtrips` tests run.
