**Date:** 2026-04-21
**Phase:** TOPIC
**Scope:** hilavitkutin::plan module — #72 final engine slice

# Current state

`plan/` has 139 warnings across 9 files (access, column, dirty, fiber,
graph, inputs, mod, phase, steps). Distribution:

| File | Warnings |
|------|---------:|
| steps.rs | 30 |
| graph.rs | 28 |
| inputs.rs | 17 |
| access.rs | 17 |
| dirty.rs | 15 |
| phase.rs | 14 |
| fiber.rs | 12 |
| mod.rs | 6 |

Same categorical pattern as prior engine slices:

- 65 `no-bare-numeric` / 65 `arvo-types-only` — count/index fields and
  params.
- 9 `no-public-raw-field` — `pub field: u*` positions inside structs.

All follow the established USize-substitution + const-generic
lint:allow pattern.

# Changes proposed

- Count/index fields across all plan submodules swap `u8`/`u16`/`u32`/
  `u64`/`usize` → `arvo::USize`.
- Struct inits `0` → `USize(0)`.
- Const-generic `const MAX_*: usize` sites carry trailing `lint:allow`
  tracked `#121`.
- Body-level primitive casts / raw constants use `.0` field access
  where const-Deref isn't stable.
- Derives `Default`/`Hash`/`Debug` dropped where arvo newtype gap
  blocks them; manual `impl Default` where the type needs one for
  ergonomics.
- No Option/Result sites expected; spot-check during apply.

# Non-goals

- No exact-width refinement (`UFixed<16, 0, Hot>` vs `USize`) —
  uniform `USize` matches prior rounds; revisit when real plan body
  lands (5a2).
- No semantic alias creation (e.g., `LaneId`, `FiberId`) — those stay
  as existing domain-aliased newtypes over arvo primitives.

# Validation

- `cargo check` clean.
- `cargo mock --lint-only | grep plan/` returns nothing.
