**Date:** 2026-04-21
**Phase:** SRC-PLAN
**Scope:** engine-plan-sweep source CL

Uniform pattern across 9 files in `src/plan/`:

- Every bare numeric field / param / return type swaps to `arvo::USize`
  (count/index semantics); boolean becomes `arvo::newtype::Bool`.
- Every `const MAX_*: usize` const-generic carries trailing inline
  `lint:allow(no-bare-numeric) lint:allow(arvo-types-only) reason:
  const-generic array size; rust grammar requires usize; tracked: #121`.
- Literal `0`/`1` initialisers in struct construction become `USize(0)`
  etc.
- Method bodies that compare `x < n` / add `a + b` work via `arvo::USize`
  `Deref<Target=usize>` where runtime; `.0` field access where const.
- Derives trimmed to `Copy, Clone, Eq, PartialEq` where arvo newtype
  gap blocks `Debug`/`Default`/`Hash`. Manual `impl Default` added
  where helper ergonomics require.

## Files

- `access.rs` — 17 warnings
- `column.rs` — (already lint-clean; skip)
- `dirty.rs` — 15
- `fiber.rs` — 12
- `graph.rs` — 28
- `inputs.rs` — 17
- `mod.rs` — 6
- `phase.rs` — 14
- `steps.rs` — 30

Each file edited in place; no new files. No signature semantics change
at any site; the swap is type-surface only.

# Validation

- `cargo check` clean.
- `cargo mock --lint-only | grep plan/` returns nothing.
