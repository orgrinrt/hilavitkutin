# Doc changelist: A3b morsel-window budget source

**Date:** 2026-07-16
**Phase:** DOC (this changelist opens it)
**Topic:** `202607161400_topic.a3b-l1-morsel-formula.md`

## Changes proposed

### `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The plan step-9 bullet gains the budget source: the window formula's
`L1_usable` / `MIN_MORSEL` / `MAX_MORSEL` come from consumer-tunable `RunCfg`
consts (defaults per domain 12: the 32 KB detection-fallback times 0.75, one
micro-morsel interval, 8192), and the per-fiber sum walks the write-mask union
against the per-store size fold (`Field` scalars 0, `Seq`/`Map` collections
`N * elem`, accumulators 0). One tightening of an existing line, no section
restructure.
