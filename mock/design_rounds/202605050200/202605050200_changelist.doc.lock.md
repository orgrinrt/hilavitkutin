**Date:** 2026-05-05
**Phase:** DOC
**Round:** 202605050200

# Doc changelist: USize(0) → USize::ZERO

No documentation template changes. The replacement is value-only and produces no public-API or DESIGN.md.tmpl semantic changes. Consumers reading rustdoc see the same `Default for StoreId` shape, the same `CountingSink::new()` shape, the same `AccessSet for ()` shape.

The src CL records the three concrete edits per `cl-claim-sketch-discipline.md`.
