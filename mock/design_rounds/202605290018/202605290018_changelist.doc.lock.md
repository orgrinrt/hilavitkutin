# Doc changelist — resource arena (B2a)

Round: 202605290018. Phase: DOC. Updates the engine DESIGN builder paragraph to the B2a shape (the `StoreValues` store-aligned list replaces B1's `Staged`, and `build()` now allocates the arena), and notes the `Scheduler` reshape.

Scope note: the engine DESIGN builder section below the param paragraph still names the registration trait `Provider` and a GAT-form `Dispatch`, while shipped source is `BuilderInput` with parameter-form `Dispatch`. That is pre-existing drift unrelated to B2a (flagged in the B1 round); reconciling the whole section is a separate doc pass, not folded here. B2a edits only the param paragraph its change makes inaccurate plus a short `Scheduler`-reshape note. The new B2a types (`RouterKind`, `Place`, `StoreValues`/`Sv`/`SvEmpty`, `ArenaFor`, `ArenaResourceNode`/`ArenaColumnNode`/`ArenaVirtualNode`, `DrainStores`, `DropArena`, `BuildError`) are source-only; the `design-doc-source-mismatch` lint flags only first-cell backtick names in DESIGN table rows, so no DESIGN table entry is required.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The builder typestate paragraph documents the fourth `Staged` parameter as a value-carrying cons-list holding every registered value. B2a replaces it with `StoreValues`, a `Stores`-aligned list holding only store carriers, populated at `.with` via a `RouterKind` tag on the dispatch routers plus a `Place` view (store carriers retained; workunit and platform values dropped, their type still tracked in `Wus` / `Platform`). The single `.with` verb is unchanged. State that `build(memory_provider)` now walks `Stores` and `StoreValues` in lockstep, allocates one block per `Resource<T>` via the supplied `MemoryProvider`, and returns an `Outcome` (allocation can fail). Add a short note that `Scheduler` gains `Stores` and `M: MemoryProviderApi` parameters, owns the arena plus the provider, and deallocates every block on `Drop`. Column buffers and the per-WorkUnit Context are later rounds.

## Not changed

The broader `Provider` / GAT-`Dispatch` drift in the engine builder section and the api crate's registration-contract section is left for a dedicated reconciliation pass. No `.md.tmpl` table rows reference the new types, so no lint surface is touched.
