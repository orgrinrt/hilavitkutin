---
round: 202605041228
phase: DOC
status: open
---

# Doc changelist: arvo Rounds 1-8 adoption + typed-const completion

Two topic files frame this round:

1. `202605041228_topic_arvo_rounds_1_8_adoption.md`: pin update plus
   the two consumer-side breakage fixes (Mask64 to Mask, the
   `const_trait_impl` feature gate for hilavitkutin-str's
   `bitfield!` use).
2. `202605041228_topic_typed_const_completion.md`: closes the
   typed-const stubs PR #46 left at `builder.rs:165`, `:168`, and
   `:102`, plus the parallel `const_trait_impl` feature gate for
   hilavitkutin-api so the const-Add and Identity surface from arvo
   is reachable from `Depth` impls.

The two scopes ride one PR because their shared root cause is the
arvo dev-pin advance and the typed-const surface that advance
ships. Splitting them into two PRs would mean either (a) the first
PR adopts breakage fixes against an arvo pin that already exposes
USize::ZERO without consuming it, or (b) the second PR sits idle
until the first lands.

## Doc template edits

### `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`

Update the `Depth` trait code sample at lines 298 to 301 so it
matches the typed-const shape the source will carry after this
round's source CL applies:

Before:

```rust
impl Depth for () { const D: USize = 0; }
impl<H, R: Depth> Depth for (H, R) {
    const D: USize = R::D + 1;
}
```

After:

```rust
impl Depth for () { const D: USize = USize::ZERO; }
impl<H, R: Depth> Depth for (H, R) {
    const D: USize = R::D + USize::ONE;
}
```

The surrounding prose (lines 304 onward, "Two impls. No
specialization. ...") stays. The change captures the typed-const
adoption.

## Doc template edits not needed

The other affected sites are all source-side (builder.rs Rust doc
comments, lib.rs feature attributes, id.rs use statement,
hilavitkutin-str/src/lib.rs feature attribute, Cargo.lock pin).
They live in the source CL, not here.

Hilavitkutin-side BACKLOG entries do not currently mention the
deferred typed-const sites; PR #46's deferral was tracked through
the locked SRC CL and the reviewer's nit, not through a BACKLOG
entry. No BACKLOG removals or additions in this round.

## Lock criteria

This round's doc CL is ready to lock when the DESIGN.md.tmpl edit
above lands and `cargo mock --lint-only --commit` is green at the
infra scope.

## Cross-references

- Topic 1: `202605041228_topic_arvo_rounds_1_8_adoption.md`
- Topic 2: `202605041228_topic_typed_const_completion.md`
- Source CL (to be written post-DOC-lock): `202605041228_changelist.src.md`
- Origin of typed-const drift: `mock/design_rounds/202605011500/202605011500_changelist.src.lock.md:158-170,294`
- arvo USize const surface: `arvo/mock/crates/arvo-storage/src/platform.rs:108-111` (Identity), `:148-226` (const arith)
