# Doc changelist: GATE-2 const grouping

**Round:** 202606070900 (topic: gate2-const-grouping)
**Phase:** DOC
**Scope:** `mock/crates/hilavitkutin/BACKLOG.md.tmpl` only.

## Design recorded this round

The GATE-2 carrier mechanism computes the per-unit grouping (phase + trunk assignment) at compile time from the registered work-unit bundle, reusing the existing const `MaskProject` fold (`plan::project`) for per-unit store-bit masks and const fns for the read-after-write phase depth and within-phase column-disjoint trunk components. The grouping is exposed as a const-evaluable structure the later const-gated dispatch walk indexes. Full design: the round topic (`202606070900_topic.gate2-const-grouping.md`) and roadmap r4 (`mock/research/202606070700_engine-roadmap-r4-gate2-mechanism.md`).

## Why no DESIGN.md.tmpl change this round

The const grouping is internal `plan`-module machinery with no observable consumer-facing behavior on its own. It becomes observable only when the next round re-points `Scheduler::run` through the const-gated per-trunk dispatch. Per the documentation tiers, internal plan machinery that does not change a shipped contract lands in BACKLOG until the behavior it enables ships. The DESIGN.md.tmpl prose for GATE-2 parallel dispatch (the trunk/phase/waist model becoming the actual run path) is authored in the dispatch-wiring round, when the backticked types it would name exist in source and the behavior is real.

## CHANGE: add a GATE-2 grouping note to BACKLOG

File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Add a short entry under the engine's backlog recording the GATE-2 const-grouping design as in-progress (designed, landing across the GATE-2 build rounds). Names stay unbackticked per BACKLOG convention so the design-doc-source-mismatch lint does not require them in source yet.
