# Doc CL: accumulator over-capacity hard error

**Round:** 202606200300
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Add a sentence to the accumulator-dispatch section: appending past an
accumulator's reserved capacity is a contract violation that panics (the assert
fires before the write, so no out-of-bounds access), not a silent saturating drop.
Consumers size the accumulator for the maximum per-frame appends.
