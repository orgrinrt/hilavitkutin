# Src CL: accumulator over-capacity hard error

**Round:** 202606200300
**Phase:** IMPL

## CHANGE: resolve_append asserts live < cap instead of saturating
File: `mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs` (~1399-1406)
Replace the `if live.0 >= acc.cap.0 { return; }` silent-saturate guard with an `assert!(live.0 < acc.cap.0, "<static msg>")` that panics before the write. The assert fires ahead of the out-of-bounds write, so the soundness floor (no OOB) is preserved; the silent drop becomes a loud abort.
Verification: `grep "assert!(live.0 < acc.cap.0" mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs` returns one match; the saturating `return` is gone.

## CHANGE: update the AccumColPtr doc comment
File: `mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs:135`
The "the append saturates at" comment becomes "the append asserts against".

## CHANGE: ADD over-append should_panic test
File: `mock/crates/hilavitkutin/tests/r3b_change_class_count.rs`
A WorkUnit appends `cap + 1` times in one single-core `execute`; `#[should_panic(expected = "reserved capacity")]` asserts the contract violation is caught. Keeps the RECORDS=64 happy single-core and parallel tests.
Verification: `cargo test -p hilavitkutin --test r3b_change_class_count` runs 3 tests, all pass.
