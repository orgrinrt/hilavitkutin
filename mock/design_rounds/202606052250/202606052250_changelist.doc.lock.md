**Date:** 2026-06-05
**Phase:** DOC
**Scope:** hilavitkutin engine, transitive arvo bump + one call-site fix (#666). No design-doc edits.
**Source topics:** `mock/design_rounds/202606052250_topic.arvo-bump-waist-detect.md`

# DOC CL: no doc-template edits

This round is a mechanical transitive dependency bump (arvo `dev` HEAD) plus the single `waist_detect` call-site fix the new generic arity forces. It changes no design, no contract, and no public surface, so it edits no `*.md.tmpl` template.

`DESIGN.md.tmpl` already describes `waist_detect` as the step-4 waist-detection call into arvo-graph without pinning its generic arity, so the signature change needs no design-doc edit. The engine's 64-node row cap (`Bits<64, Hot, Unsigned>`) is unchanged by this round and is therefore still accurately described; lifting it is the separate follow-up named in the topic's Out of scope (#668).

No `README.md.tmpl`, `BACKLOG.md.tmpl`, `DESIGN.md.tmpl`, or other template edits.

## Basis

arvo #663 (merged to arvo `dev`) parameterized arvo-graph over the bit-row type; `waist_detect` gained a second generic argument. The engine's call supplied one. The fix is to supply the second argument (`Bits<64, Hot, Unsigned>`, matching the engine's existing `adj` row type) and bump the `Cargo.lock` arvo pin to `dev` HEAD.

## Increment plan (for the SRC CL)

1. Bump the arvo git source in `Cargo.lock` from `549628` to `dev` HEAD.
2. Add the second generic argument to the `waist_detect` call at `plan/steps.rs:272`. Build and existing plan tests green.
