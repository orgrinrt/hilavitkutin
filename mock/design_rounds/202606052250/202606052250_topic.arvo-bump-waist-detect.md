**Date:** 2026-06-05
**Phase:** TOPIC
**Scope:** hilavitkutin engine, transitive dependency bump (arvo `dev` HEAD) plus the one call-site fix it forces. No design change.
**Source:** task #666; arvo #663 (completed, merged to arvo `dev`): arvo-graph DAG algorithms parameterized over row-width `W`, which changed `waist_detect`'s generic arity.

# Topic: bump arvo to dev HEAD, fix the waist_detect call

## Current state

The mock workspace `Cargo.lock` pins the arvo git source at `549628` (pre-#663). On that pin `arvo_graph::waist_detect` is `waist_detect<C: Capacity>(dag, topo_order)`. The engine calls it at `plan/steps.rs:272` with one generic argument.

arvo #663 (merged to arvo `dev`) parameterized the arvo-graph DAG algorithms over the bit-row type to lift the fixed 64-node cap. `waist_detect` is now `waist_detect<C: Capacity, B>(dag: &BitMatrix<B, C>, topo_order: &C::Array<NodeId>) -> Mask<B>`. The engine does not build against arvo `dev` HEAD because the call supplies one generic argument where two are required.

## Change

Bump the arvo git source in `Cargo.lock` from `549628` to `dev` HEAD (`ff514a7`), and update the `waist_detect` call to supply the second generic argument. The engine's `adj` is `BitMatrix<Bits<64, Hot, Unsigned>, D::Units>`, so the second argument is `Bits<64, Hot, Unsigned>` and the `Mask<Bits<64, Hot, Unsigned>>` return type is unchanged. This is a mechanical compile fix.

## Out of scope

Lifting the engine's own 64-node cap (widening the `adj` / waist row type from the hardcoded `Bits<64, Hot, Unsigned>` to a width derived from `D::Units` capacity, consuming #663's multi-limb bit matrix) is a separate GATE-1 slice, tracked as its own follow-up. This round only restores the build against arvo `dev` HEAD; it does not change the engine's node-count ceiling.

## Test plan

`cargo build -p hilavitkutin` and the existing plan tests green against arvo `dev` HEAD.
