**Date:** 2026-06-05
**Phase:** SRC
**Scope:** hilavitkutin engine, `plan/steps.rs` + `Cargo.lock` (#666).
**Source topics:** `mock/design_rounds/202606052250_topic.arvo-bump-waist-detect.md`

# SRC CL: bump arvo dev HEAD, fix waist_detect call

Mechanical transitive update. Bumps the arvo git source to `dev` HEAD and supplies the second generic argument the new `waist_detect` arity requires. No design, contract, or public-surface change. The engine's 64-node row cap is unchanged (lifting it is #668).

## CHANGE: arvo git source in `Cargo.lock` FROM `549628` TO `dev` HEAD (`ff514a7`)
File: `mock/Cargo.lock`
Verification: `grep "arvo.git?branch=dev#ff514a7" mock/Cargo.lock` returns matches; `! grep "549628815f5b4e6df34c0eb77c731377b28bd7b5" mock/Cargo.lock`.

## CHANGE: `waist_detect` call FROM 1 generic argument TO 2
File: `mock/crates/hilavitkutin/src/plan/steps.rs:272`
Verification: `grep "waist_detect::<D::Units, Bits<64, Hot, Unsigned>>" mock/crates/hilavitkutin/src/plan/steps.rs` returns one match; `cargo build -p hilavitkutin` succeeds.

## Test plan

`cargo build -p hilavitkutin` green against arvo `dev` HEAD; existing plan tests (`compute_waists` coverage in the plan tests) stay green.
