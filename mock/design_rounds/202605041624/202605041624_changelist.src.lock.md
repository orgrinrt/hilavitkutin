**Date:** 2026-05-04
**Phase:** SRC
**Scope:** hilavitkutin-linking tests/basic.rs (one match arm added)
**Source topics:** 202605041624

# Source changelist: extend reject_path_without_null_terminator match

## CHANGE: test `reject_path_without_null_terminator` ADD match arm `Outcome::Err(LinkError::PathEncodingUnsupported) => {}`

File: `mock/crates/hilavitkutin-linking/tests/basic.rs:128-134`

Insert a new match arm between the existing `PathNotFound` arm and the catch-all `_` arm so the test passes on both Unix (returns `PathNotFound`) and Windows (returns `PathEncodingUnsupported` post-round-202605041618).

Verification: `grep -c "PathEncodingUnsupported" mock/crates/hilavitkutin-linking/tests/basic.rs` returns 1.

## What is NOT changed

- Public API. No source file under `src/` is touched.
- DESIGN.md.tmpl. The variant is already documented from round 202605041618.
- Other test functions in `tests/basic.rs`.

## Lock criteria

- [ ] Verification grep above succeeds.
- [ ] `cargo test -p hilavitkutin-linking` passes on macOS host (10 prior tests + this one keeps passing on Unix branch since the new arm is additive).
- [ ] `cargo mock --lint-only --build` passes scoped to `hilavitkutin-linking`.
