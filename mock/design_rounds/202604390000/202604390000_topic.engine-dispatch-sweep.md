**Date:** 2026-04-21
**Phase:** TOPIC
**Scope:** hilavitkutin::dispatch module — #72 slice

# Current state

`dispatch/` has 43 warnings across 6 files. Like `thread/`, the module
is skeleton-stage for 5a3: public surface is complete; every
code-emit function stubs to `todo!()`. Real LLVM / ExpandedLto wiring
lands as follow-up rounds.

Three patterns:

1. **Counts / ranges** — `u8`/`u16`/`u32`/`u64` fields and parameters:
   `fiber_count`, `phase_count`, `boundary_count`, `sync_point_count`,
   `record_count`, `start`/`len` of MorselRange, `min_records` of
   SyncPoint. All become `USize` (same approach as strategy/adapt/
   thread rounds).
2. **Option in dispatch field** — `FiberDispatch::body: Option<WuFn<Ctx>>`
   needs `Maybe<WuFn<Ctx>>` per notko-macros primitive vocabulary.
3. **ProgressCounter** — `#[repr(transparent)]` wrapper around
   `AtomicUsize`. The method sigs (`new(start: usize)`, `store(value:
   usize)`, `load() -> usize`) take raw usize. Public surface switches
   to `USize` with internal deref; the internal `AtomicUsize` stays
   as-is (represents the concrete atomic-memory ABI).

# Non-goals

- No new traits / refactored method signatures beyond primitive
  swaps.
- No changes to `approach.rs` or `wu_fn.rs` — already clean.
- No changes to the skeleton-body `todo!()` markers.
- Derives may need trimming where arvo `USize` gap blocks them.

# Validation

- `cargo check` clean.
- `cargo mock --lint-only | grep dispatch/` returns nothing.
