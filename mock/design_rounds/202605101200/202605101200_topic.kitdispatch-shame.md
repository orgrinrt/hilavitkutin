**Date:** 2026-05-10
**Phase:** TOPIC
**Round:** 202605101200 (KitDispatch SHAME entry)

# Topic: silence the KitDispatch source-mismatch lint via SHAME

Round 202605091700 introduced four `Dispatch` routers in api/DESIGN.md.tmpl. Three (`UnitDispatch`, `StoreDispatch`, `PlatformDispatch`) live in `hilavitkutin-api/src/provider.rs`. The fourth (`KitDispatch`) lives in `hilavitkutin-kit/src/lib.rs` because its `Dispatch` impl projects `K::Units` and `K::Owned`, which require the `Kit` trait that lives in the kit crate; the api crate is the contract layer and cannot depend on kit.

The `design-doc-source-mismatch` lint at push-gate severity fires on `KitDispatch` documented in api but not present in api source. Silence it via a SHAME entry that records the cross-crate decision (D7 in the prior round's research note).

## Decision

Add `mock/crates/hilavitkutin-api/SHAME.md.tmpl` with a `## KitDispatch` header and a 50+ word explanation of the cross-crate location. No source changes. No DESIGN edits.
