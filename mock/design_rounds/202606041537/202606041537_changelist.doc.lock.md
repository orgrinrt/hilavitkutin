**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041537 (rename resource arena to bindings)

# Doc changelist

`BACKLOG.md.tmpl` describes the unified-storage arc and names the resource
data plane "the resource arena, ArenaFor / DrainStores". This round renames
that data plane to "bindings" in source, so the backlog prose follows: it
names the resource bindings and the `BindingsFor` trait instead. `DrainStores`
keeps its name (it is the drain trait, not renamed).

## File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`

### CHANGE: the unified-storage-arc paragraph naming the resource data plane

The sentence "The resource data plane (the resource arena, ArenaFor /
DrainStores) collapses onto ColumnStorage" updates to "The resource data plane
(the resource bindings, BindingsFor / DrainStores) collapses onto
ColumnStorage". Same meaning, current names. No backticked identifier is
introduced or removed (the names are prose, not code spans).

## Verification

`grep -n "resource arena\|ArenaFor" BACKLOG.md.tmpl` returns no match after the
edit. The `design-doc-source-mismatch` lint passes at push: the prose names
`BindingsFor` (which exists as a pub trait in source once the src round lands,
both in the same PR).
