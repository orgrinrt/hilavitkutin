**Date:** 2026-06-04
**Phase:** SRC
**Round:** 202606041537 (rename resource arena to bindings)

# Source changelist

Pure rename of the resource data-plane index from "arena" to "bindings". No
behaviour changes, no signature shapes change. The existing test suite stays
green throughout (the REFACTOR step under green tests); no new test is added,
since no new behaviour lands. The `ArenaColumnStorage` provider, the
morsel/progress scratch arena, the `'arena` lifetimes, and `AdaptArena` keep
their names (different concepts, named in the topic's scope boundary).

## CHANGE: module `resource::arena` RENAMES TO `resource::bindings`
File: `mock/crates/hilavitkutin/src/resource/arena.rs` -> `src/resource/bindings.rs`
File: `mock/crates/hilavitkutin/src/resource/mod.rs` (`pub mod arena;` -> `pub mod bindings;`, re-export path)
Verification: `! test -e src/resource/arena.rs && test -e src/resource/bindings.rs`; `grep "pub mod bindings" src/resource/mod.rs` returns one match.

## CHANGE: struct `ArenaResourceNode<T, Tail>` RENAMES TO `ResourceBinding<T, Tail>`
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`, `src/dispatch/engine_ctx.rs` (Selector / Project impl targets), tests
Verification: `! grep -rn "ArenaResourceNode" src/ tests/`; `grep -rn "ResourceBinding" src/` returns matches.

## CHANGE: struct `ArenaColumnNode<T, Tail>` RENAMES TO `ColumnBinding<T, Tail>`
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`
Verification: `! grep -rn "ArenaColumnNode" src/`; `grep -rn "ColumnBinding" src/` returns matches.

## CHANGE: struct `ArenaVirtualNode<T, Tail>` RENAMES TO `VirtualBinding<T, Tail>`
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`
Verification: `! grep -rn "ArenaVirtualNode" src/`; `grep -rn "VirtualBinding" src/` returns matches.

## CHANGE: struct `ArenaTail` RENAMES TO `BindingNil`
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`, `src/scheduler/mod.rs` (Default impl)
Verification: `! grep -rn "ArenaTail" src/`; `grep -rn "BindingNil" src/` returns matches.

## CHANGE: trait `ArenaFor` / assoc `type Arena` RENAME TO `BindingsFor` / `type Bindings`
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`, `src/scheduler/mod.rs` (bounds + field type)
Verification: `! grep -rn "ArenaFor\|type Arena\b" src/`; `grep -rn "BindingsFor\|type Bindings" src/` returns matches.

## CHANGE: `Scheduler` field `arena` RENAMES TO `bindings`; `__arena()` RENAMES TO `__bindings()`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep "bindings:" src/scheduler/mod.rs` and `grep "fn __bindings" src/scheduler/mod.rs` return matches; `! grep "fn __arena" src/scheduler/mod.rs`.

## CHANGE: test file `tests/resource_arena.rs` RENAMES TO `tests/resource_bindings.rs`
File: `mock/crates/hilavitkutin/tests/resource_arena.rs` -> `tests/resource_bindings.rs`
Verification: `! test -e tests/resource_arena.rs && test -e tests/resource_bindings.rs`; `cargo test -p hilavitkutin` green.

## CHANGE: projection-source param `arena` RENAMES TO `bindings` (trivial)
File: `mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs`, `src/dispatch/fiber_walk.rs`, `src/dispatch/fiber_codegen.rs`, and the test locals
The bindings cons-list is the source the `EngineCtx` / fiber walk projects
resources from (columns arrive through a separate param, so the projected
source is specifically the bindings). The `arena: &A` param and its doc-comments
rename to `bindings: &A` for consistency. No signature shape changes (param
name only); the generic `A` is unchanged. The genuine scratch arena
(`dispatch::progress`, `morsel`, the `'arena` lifetimes) is NOT touched.
Verification: `! grep -rn "arena" src/dispatch/engine_ctx.rs src/dispatch/fiber_walk.rs src/dispatch/fiber_codegen.rs` (case-sensitive; the scratch-arena files are out of scope).

## Verification (whole round)

`cargo test -p hilavitkutin` is green with the same test count as before the
rename (no test added or removed; `resource_arena` is renamed to
`resource_bindings`, not dropped). `cargo mock check` lints pass.
