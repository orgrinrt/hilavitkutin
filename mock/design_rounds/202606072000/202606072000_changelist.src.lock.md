# Src changelist: GATE-2 R4c round B1, extract run_core_phase

**Date:** 2026-06-07
**Phase:** SRC
**Scope:** `hilavitkutin/src/scheduler/mod.rs`

## CHANGE: add private method `Scheduler::run_core_phase`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Detail: extract the per-(core,phase) walk (compute `core_phase_mask`, then the
morsel loop / empty-morsel walk via `wu_values.run_gated`) from `run_parallel`'s
inline double loop into `fn run_core_phase<Witnesses>(&self, phase, trunk, n,
core: USize, p: USize, ncores: USize, total: usize, msize: usize) where WuVals:
RunFiber<<Vals as BindingsFor>::Bindings, Witnesses>`.
Verification: `grep "fn run_core_phase" mock/crates/hilavitkutin/src/scheduler/mod.rs`
returns one match.

## CHANGE: rewire `run_parallel` to call `run_core_phase`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Detail: the phase-outer / core-inner loop body becomes a single
`self.run_core_phase::<Witnesses>(&phase, &trunk, n, USize(core), USize(p),
ncores, total, msize)` call. Behaviour unchanged (pure extraction).
Verification: `cargo test -p hilavitkutin --test gate2_run_parallel` passes
(equivalence guard); full engine suite green.
