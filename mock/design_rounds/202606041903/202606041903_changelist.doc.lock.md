# Doc changelist: resource-arena-partial-failure

**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041903 (resource-arena-partial-failure, branch `test/hilavitkutin-resource-arena-partial-failure`)

No documentation change. This round is test-only (a regression guard for the resource-arena partial-failure cleanup). No `*.md.tmpl` surface describes the test-internal `CountingProvider` counter semantics or the partial-failure assertion, so no design-doc edit is warranted. The source-state invariant (no leak on the `Err` arm) is already reflected by the existing `resource/bindings.rs` drain prose.

## Out of scope

The test and the `CountingProvider` count-on-success adjustment belong to the src changelist of this round.
