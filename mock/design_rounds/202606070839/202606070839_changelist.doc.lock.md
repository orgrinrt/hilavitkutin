**Date:** 2026-06-07
**Phase:** DOC
**Scope:** hilavitkutin engine
**Round:** gate2-waist-bounded-phase (202606070839)

## Doc changes

One file: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`, the `## GATE-2 const
grouping and per-trunk dispatch` section. It currently says "read-after-write
edges between masks give a phase depth". That is the non-canonical depth axis the
course-correction superseded. Reword to the canonical waist-bounded phase: the
read-after-write edges form the unit dependency DAG, and phases are its
waist-delimited sections (concurrency minima, via `arvo_graph::waist_detect_const`),
so a producer to consumer chain with no interior narrowing is one phase. The
within-phase column-disjoint trunk wording is unchanged.

No backticked-type changes (the section is prose / unbackticked promissory note),
so the design-doc-source-mismatch lint is unaffected. No other doc template
changes.
