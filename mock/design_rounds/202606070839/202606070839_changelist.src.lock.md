**Date:** 2026-06-07
**Phase:** SRC
**Scope:** hilavitkutin engine
**Round:** gate2-waist-bounded-phase (202606070839)

Replace the const grouping's depth phase axis with a waist-bounded one fed by
`arvo_graph::waist_detect_const`. Thread the adjacency row word `Adj` (=
`D::AdjRow`) and `CU: ConstCapacity` through the grouping fns and the round-2a
dispatcher. Mirrors the runtime `compute_waists` mapping. S2-proven mechanism.

## CHANGE: trait `PlanDims::Units` bound GAINS `ConstCapacity`
File: `mock/crates/hilavitkutin/src/plan/dims.rs`
Verification: `grep -n "type Units:.*ConstCapacity" mock/crates/hilavitkutin/src/plan/dims.rs` returns a match; `DefaultPlanDims` (`Units = Dim<64>`) compiles unchanged.

## CHANGE: const fn `compute_phases_waist` ADDED; `compute_phases` (depth) REMOVED
File: `mock/crates/hilavitkutin/src/plan/grouping.rs`
Verification: `grep "compute_phases_waist" mock/crates/hilavitkutin/src/plan/grouping.rs` matches; `! grep -n "fn compute_phases\b" mock/crates/hilavitkutin/src/plan/grouping.rs` (depth fn gone, no legacy shim).
Shape: `compute_phases_waist<CU: [const] ConstCapacity, CS: Capacity, Adj: [const] BitAccess + Identity>(reads, writes, n, phase_out)` builds the unit adjacency `CU::Array<Adj>` from `reads[j].overlaps(writes[i])`, runs `waist_detect_const::<CU, Adj>`, and prefix-counts waist flags into `phase_out` (canonical: phase[k] = waist flags before k).

## CHANGE: grouping fns `phase_of` / `trunk_of` / `phase_count` / `is_member` THREAD `Adj` + `CU: ConstCapacity`
File: `mock/crates/hilavitkutin/src/plan/grouping.rs`
Verification: `grep -n "Adj" mock/crates/hilavitkutin/src/plan/grouping.rs` shows the new param on those fns; `group_n` is unchanged (counts only).
The fns call `compute_phases_waist` in place of `compute_phases`; `compute_trunks` is unchanged in shape and keys on the waist-phase.

## CHANGE: dispatcher `Member` / `RunGatedTrunk` THREAD `Adj` + `CU: ConstCapacity`
File: `mock/crates/hilavitkutin/src/dispatch/trunk_gate.rs`
Verification: `grep -n "Adj" mock/crates/hilavitkutin/src/dispatch/trunk_gate.rs` shows the param on `Member`, `RunGatedTrunk`, and the `is_member::<..>` call. `Adj` is fixed through the recursion (like `CU` / `CS`), so no `{POS+1}`-style const arithmetic is reintroduced.

## CHANGE: `Scheduler::run_one_trunk` passes `Adj = D::AdjRow`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep -n "AdjRow" mock/crates/hilavitkutin/src/scheduler/mod.rs` shows `run_one_trunk` supplying `D::AdjRow` to the gated walk.

## CHANGE: test `tests/gate2_const_grouping.rs` REWRITTEN to a waist-valid hourglass fixture
File: `mock/crates/hilavitkutin/tests/gate2_const_grouping.rs`
Verification: `cargo test -p hilavitkutin --test gate2_const_grouping` passes; asserts waist-bounded `phase = [0,0,0,0,1,1]` (NOT depth `[0,1,1,2,3,3]`) and `trunk = [0,0,0,0,4,5]`.
The prior diamond fixture is topo-invalid for waist (a WAW second-writer registered after the reader is a back edge); the hourglass (every producer before its consumers) is topo-valid.

## CHANGE: test `tests/gate2_gated_walk.rs` passes `Adj`
File: `mock/crates/hilavitkutin/tests/gate2_gated_walk.rs`
Verification: `cargo test -p hilavitkutin --test gate2_gated_walk` passes with `run_one_trunk` resolving `Adj` (member selectivity unchanged).

## Out of scope / unchanged
`tests/morsel_outer.rs` stays green (a producer to consumer chain has no interior waist, so it is one phase). `>64`-unit support is the consumer's `D::AdjRow` width choice (wide-`Bits` const `BitAccess` = a separate arvo round), not on this path.
