# Doc changelist: accumulator append saturates at capacity

**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041529 (accumulator-append-saturates-at-capacity, branch `feat/hilavitkutin-accumulator-columns`)

One edit, naming the append overflow contract in the engine design doc so the prose matches the saturating clamp the src changelist lands.

## `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The build/drain paragraph describes the `Accum<T>` drain arm and the live-length advance. Add the overflow contract: a WorkUnit advancing the live length by appending saturates at the reserved capacity (the append is dropped once the live length reaches capacity), so an append never runs past the reserved buffer. This names the Fork D decision from round 202606041800 in the shipping contract.
