# Doc CL: CtxFor computed Context type

**Round:** 202606191400
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Add one paragraph after the `EngineCtx` description (the "only the engine
names `EngineCtx<'frame, W::Read, W::Write>`" sentence) documenting `CtxFor`:
the engine-side computed alias a consumer assigns to its `Ctx<'frame>` GAT
instead of hand-spelling the nine `EngineCtx` parameters. Name the four pure
fold traits (`ResourceBundleOf`, `ColBundleOf`, `AccumBundleOf`,
`VirtBundleOf`) that compute the projected-bundle parameters from the Read /
Write access sets by per-store-kind dispatch (the same kind dispatch the
`Project` family uses, no specialization), and that the meta pointer keys off
the schedule via the shipped `MetaPtrFor`. State it is additive (no api or
dispatch change; the `RunFiber` projection-equality bounds already normalize
against the computed form) and that hand-spelling stays valid.
