# Src CL: CtxFor computed Context + fold traits

**Round:** 202606191400
**Phase:** IMPL

## CHANGE: ADD four fold traits + `CtxFor` alias to `dispatch/engine_ctx.rs`
File: `mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs`
Add `pub trait ResourceBundleOf { type Out; }`, `pub trait ColBundleOf { type
Out; }`, `pub trait AccumBundleOf<'frame> { type Out; }`, `pub trait
VirtBundleOf<'frame> { type Out; }`, each with five impls (the `Empty` leaf +
the four `Cons<Resource/Column/Accum/Virtual<T>, Tail>` head kinds: the
contributing kind conses its projected node, the other three pass the tail).
Add `pub type CtxFor<'frame, R, W, S = Always> = EngineCtx<'frame, R, W,
<R as ResourceBundleOf>::Out, <R as ColBundleOf>::Out, <W as ColBundleOf>::Out,
<W as AccumBundleOf<'frame>>::Out, <W as VirtBundleOf<'frame>>::Out,
<S as MetaPtrFor<'frame>>::Ptr>`. Verbatim the shapes proven by sketch
`202606111430_ctxfor-computed-ctx` (WORKS).
Verification: `grep "pub type CtxFor" mock/crates/hilavitkutin/src/dispatch/engine_ctx.rs` returns one match; `grep -c "BundleOf" ...` covers the four traits.

## CHANGE: re-export `CtxFor` + the four fold traits from the engine crate root
File: `mock/crates/hilavitkutin/src/lib.rs` (or the dispatch re-export module)
Alongside the existing `EngineCtx` re-export.
Verification: `grep "CtxFor" mock/crates/hilavitkutin/src/lib.rs` returns a match (or the re-export module).

## CHANGE: ADD identity test `tests/ctxfor_identity.rs`
File: `mock/crates/hilavitkutin/tests/ctxfor_identity.rs`
A `fn assert_same<T>()`-style type-identity assertion (compiles only when the
two types are literally equal, generic over `'frame`) pinning `CtxFor` output
against the hand-spelled `EngineCtx` aliases across the store kinds + the three
schedule kinds (`Always`/`On<V>`->MetaNil, `OnMeta<V>`->MetaRef). TDD: written
to fail-compile before the traits exist.
Verification: `cargo test -p hilavitkutin --test ctxfor_identity` passes.

Additive: no api change, no dispatch change (RunFiber projection-equality bounds
already normalize against the computed form). Hand-spelling `EngineCtx` stays
valid. In-repo WU migration off hand-spelled aliases is follow-up. Full suite
stays green.
