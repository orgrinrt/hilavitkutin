# Src changelist: per-fiber morsel slice A1

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606201800

## CHANGE: `FiberDispatch` gains `morsel_size: USize` + `fiber_plan_idx: USize`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep "pub morsel_size: USize" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match; Default sets both to `USize::ZERO`.

## CHANGE: `derive_phase_dispatch_order` populates the per-fiber size + plan index
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs` (the fiber-loop descriptor build)
Verification: `grep -n "fiber_plan_idx: USize(f)" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

Reads `plan.morsel_windows`; at CSR index `f` sets `fiber_plan_idx: USize(f)` and
`morsel_size: morsel_windows[f]` (bounds-guarded, ZERO fallback). No dispatch
behaviour change (field populated, not consumed).

## CHANGE: white-box `Scheduler::__fiber_morsel_size`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep -n "pub fn __fiber_morsel_size" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## NEW TEST: `tests/a1_fiber_morsel_size.rs`
File: `mock/crates/hilavitkutin/tests/a1_fiber_morsel_size.rs`
Verification: `cargo test -p hilavitkutin --test a1_fiber_morsel_size` passes.

A single-fiber carrier with N records: `__fiber_morsel_size(0)` equals the plan's
per-fiber `morsel_windows[0]` (the placeholder partition value `N` until A3).
