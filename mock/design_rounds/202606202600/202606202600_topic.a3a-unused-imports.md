# Topic: A3a test unused-import cleanup

**Date:** 2026-06-19
**Phase:** TOPIC
**Scope:** `hilavitkutin` test (`tests/a3a_store_sizes.rs`)
**Source topics:** pr-reviewer-senior on PR #160

## Current state

`tests/a3a_store_sizes.rs` imports `arvo::USize` and `arvo_tensor::Capacity` but
uses neither directly (USize values are accessed via `.0`; `Capacity` is not
named, only `Dim`). Two unused-import warnings.

## Change proposed

Drop the two unused imports.

## Not in scope

A3b.
