# Doc changelist: morsel-loop windowing

**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041609 (morsel-loop-windowing, branch `feat/hilavitkutin-morsel-loop`)

Two prose edits in the engine design doc so the `Scheduler::run` description matches the windowed dispatch the src changelist lands. Both currently say `run` dispatches "over a single morsel"; the run-loop now windows the record range into morsels.

## `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The two `Scheduler::run` descriptions (the overview paragraph and the `CollectFiber`-path paragraph) say dispatch happens "over a single morsel covering the build-time record count". Update both to state that `run` windows the record range into morsels of `RunCfg::MORSEL_SIZE` (guarded to a minimum of one record) and dispatches each unit once per morsel in topological order (unit-outer), noting that the per-fiber phase-sequential morsel loop is a later arc. Name `RunCfg::MORSEL_SIZE` so the design-doc-source-mismatch lint ties it to the new const.
