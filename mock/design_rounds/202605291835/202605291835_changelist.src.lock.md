# Source changelist: regenerate trybuild goldens

**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291835 (regenerate extensions-macros trybuild goldens)

## Summary

Realign the `hilavitkutin-extensions-macros` trybuild goldens with the macro
diagnostics PR #73 introduced (Capability to Provider rename), fixing the
long-standing red `trybuild_fixtures` test on dev. Goldens regenerated via
`TRYBUILD=overwrite` on the pinned `nightly-2026-05-28`, then verified by a
plain `cargo test --test trybuild`.

## CHANGE: add `provider_type_missing_impl.stderr` golden
File: `mock/crates/hilavitkutin-extensions-macros/tests/fixtures/fail/provider_type_missing_impl.stderr`
Verification: file exists and contains `the trait bound \`NotACap: ProviderExport\` is not satisfied`; `cargo +nightly-2026-05-28 test -p hilavitkutin-extensions-macros --test trybuild` passes.

## CHANGE: update `unknown_attr.stderr` to the renamed supported keys
File: `mock/crates/hilavitkutin-extensions-macros/tests/fixtures/fail/unknown_attr.stderr`
Verification: the supported-keys line names `required_host_providers, providers` (the post-#73 keys), not `required_host_caps, capabilities`.

## CHANGE: remove orphaned `capability_type_missing_impl.stderr`
File: `mock/crates/hilavitkutin-extensions-macros/tests/fixtures/fail/capability_type_missing_impl.stderr`
Verification: file no longer exists; no `capability_type_missing_impl.rs` fixture exists to match it.

## Lock criteria

- `cargo +nightly-2026-05-28 test -p hilavitkutin-extensions-macros --test trybuild` passes (no overwrite).
- The three goldens in the intended end state (provider added, unknown_attr updated, orphan removed).
