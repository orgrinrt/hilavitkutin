# Doc CL: PlanDims gains AccumsPerCore + PlanAffecting for cap lifting

**Round:** 202606191200
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Extend the documented `PlanDims` trait with two capacity dimensions so the
hardcoded grouping/scheduler caps become consumer-tunable (the s1 shape from
sketch `202606111450`, which concluded the lift extends `PlanDims` rather than
adding a separate trait, since `Units` and `Cores` already exist):

- `AccumsPerCore: Capacity` replaces the hardcoded `GATE2_MAX_ACCUMS = 16`
  (and feeds the per-(core,accum) publish array as the nested
  `Cores::Array<AccumsPerCore::Array<_>>` composition).
- `PlanAffecting: Capacity` replaces the hardcoded `plan_dirty: [_; 256]`.

The grouping `GATE2_MAX_UNITS = 256` scratch migrates onto the existing
`D::Units`, conditioned on the round's first src step (the P0.1b in-engine
trait-solver-overflow probe): if the GAT-projected grouping scratch compiles
under the real compounded `BundleMasks`/`MaskProject`/`GateWith` obligations,
`Units` lifts the grouping cap fully; if it overflows, the grouping cap stays
at the documented 256 ceiling while the other caps still lift. Update the
`PlanDims` count ("thirteen"/"fourteen" dimensions) and trait block, and add a
paragraph naming the two new dimensions and the probe-conditional grouping
lift. Defaults stay 256/16/etc so existing pipelines are byte-identical.
