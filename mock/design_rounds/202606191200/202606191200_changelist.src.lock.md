# Src CL: lift engine caps onto PlanDims

**Round:** 202606191200
**Phase:** IMPL

## CHANGE: P0.1b probe — `masks_of` fill scratch FROM fixed `[_; GATE2_MAX_UNITS]` TO `<CU as ConstCapacity>::Array<_>`
File: `mock/crates/hilavitkutin/src/plan/grouping.rs`
Build the reads/writes/ranks fill scratch as `CU`-capacity GAT arrays
(`ConstCapacity::filled`), slice into the slice-based `BundleMasks::fill` via
the `ConstCapacity::slice_mut` bridge. First step; `cargo check` at the real
call sites is the in-engine overflow probe. Bound `CU: Capacity` gains
`+ [const] ConstCapacity`.
Verification: `cargo build -p hilavitkutin` succeeds (probe green = no
trait-solver overflow under the compounded BundleMasks/MaskProject/GateWith
obligations).

FINDING (2026-06-19): PROBE GREEN. `cargo build -p hilavitkutin` compiles with
the GAT scratch at the real call sites (CU = `DefaultPlanDims::Units` =
`Dim<256>`) after `cargo update`-ing the arvo dep to the P0.1a const-slice
HEAD. No overflow, as predicted (slice-based `fill` keeps the recursion
capacity-blind). `group_n` also gained `+ [const] ConstCapacity` on its CU.
=> caps lift fully; the probe-conditional CHANGE below proceeds. The probe's
copy-back into the fixed-cap return is a checkpoint artifact; the return-type
migration onto `<Units as ConstCapacity>::Array` replaces it (no copy-back
ships).

## CHANGE: trait `PlanDims` GAINS `AccumsPerCore: Capacity` + `PlanAffecting: Capacity`
File: `mock/crates/hilavitkutin/src/plan/dims.rs`
Two associated types; `DefaultPlanDims` binds `Dim<16>` / `Dim<256>` (the
former GATE2_MAX_ACCUMS / plan_dirty literals). Every `impl PlanDims` site
(src + tests) gains the two lines.
Verification: `grep "type AccumsPerCore" mock/crates/hilavitkutin/src/plan/dims.rs` returns at least one match.

## CHANGE: const `GATE2_MAX_ACCUMS` USE-sites FROM literal TO `<D::AccumsPerCore as Capacity>` — DEFERRED to follow-up #690
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
The `AccumsPerCore` capacity type ships in this round (on `PlanDims` + all
impls), but threading it through `gate2_accum_live` requires nesting the flat
`[AtomicUsize; MAX_CORES * GATE2_MAX_ACCUMS]` publish array into
`<D::Cores>::Array<<D::AccumsPerCore>::Array<AtomicUsize>>`, which in turn
requires redesigning `Bindings::merge_accums` (6 impls in
`resource/bindings.rs`) off its flat `live: &[USize]` + stride interface onto
the nested per-core storage. That is its own design unit (the merge interface,
not just an index rewrite) and is tracked as follow-up #690. The flat
`GATE2_MAX_ACCUMS` array is the existing shipped-correct code, not a stopgap;
`AccumsPerCore` stays defined and ready for #690. This round lifts the other
two caps (`plan_dirty`->`PlanAffecting`, grouping scratch->`Units`).

## CHANGE: field `plan_dirty: [AtomicBool; 256]` FROM literal TO `<D::PlanAffecting as Capacity>::Array<AtomicBool>`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Init via `Capacity::from_fn` (AtomicBool not Copy). Reads/writes through
`as_ref()`/`as_mut()`.

## CHANGE (probe-conditional): grouping `GATE2_MAX_UNITS` scratch + return FROM literal TO `<D::Units as Capacity>` projections
File: `mock/crates/hilavitkutin/src/plan/grouping.rs`
Only if the P0.1b probe compiles: migrate `masks_of`/`final_phases_of` return
arrays and the `phase_of`/`trunk_of`/`phase_count` callers off the fixed cap
onto `Units` GAT projections. If the probe overflows, `GATE2_MAX_UNITS` stays
the documented 256 ceiling (tracked) and only the non-grouping caps lift.

No behavioural change: defaults stay 256/16/etc, existing pipelines
byte-identical. Full engine+api suite stays green.
