---
round: 202605090144
phase: DRAFT
scope: hilavitkutin-str
---

# Src CL — str linker-section rationale (no source changes)

This round is doc-only. The DESIGN.md.tmpl edit landed under the
doc CL (now locked). No `src/*.rs` files change. This empty src CL
exists to step the phase machine through DRAFT to CLOSED.

## Changes

(none)

## Lock criteria

- No source-side state to verify; the doc CL's lock criteria already
  passed.
- `cargo check -p hilavitkutin-str` green (no-op verification, no
  source change).
- `cargo mock --lint-only --commit` green.
