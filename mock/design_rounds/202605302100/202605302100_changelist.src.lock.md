**Date:** 2026-05-30
**Phase:** SRC
**Round:** 202605302100 unified-columnar-storage

## Intent

Ship the unified columnar storage contract in `hilavitkutin-api`: the `ColumnStorage` backing trait and the `Decompose` SoA mechanism, plus the re-exports. This is the contract round (phase 1 of the arc): traits only, no engine migration. A conforming `ColumnStorage` stub and a hand-written `Decompose` impl in the test crate prove the shapes are implementable and the associated types resolve as the DESIGN specifies.

The DOC CL (`202605302100_changelist.doc.lock.md`) pins the contract shape and the two design resolutions (cardinality-1 retains scalar promotion; trait-only first, derive deferred). This SRC CL ships the source those backticked names bind to.

## Scope

Two trait declarations in a new `storage` module of `hilavitkutin-api`, their re-exports, and a conformance test. No impl ships in a shipping crate this round (the naive arena-backed `ColumnStorage` impl is the next round on this branch; the storage lint is the round after; the engine migrations are follow-on PRs). The test crate carries a stub impl and a hand-written `Decompose` impl to drive the contract test-first.

## CHANGE: add `trait ColumnStorage` (new) in `hilavitkutin-api`
File: `mock/crates/hilavitkutin-api/src/storage.rs`
Add `pub trait ColumnStorage` with associated `type Error`; methods `reserve<T: ColumnValue>(&mut self, StoreId, USize) -> Outcome<(), Self::Error>`, `unsafe fn column_ptr<T: ColumnValue>(&self, StoreId) -> *const T`, `unsafe fn column_ptr_mut<T: ColumnValue>(&self, StoreId) -> *mut T`, `count(&self, StoreId) -> USize`, `release(&mut self, StoreId)`. Raw-pointer access (R6), MemoryProvider-backed, type-native stride, 64-byte aligned (documented in the trait rustdoc; the alignment is an impl obligation, not a signature). `#[diagnostic::on_unimplemented]` per the platform-trait convention.
Verification: `grep "pub trait ColumnStorage" mock/crates/hilavitkutin-api/src/storage.rs` returns one match; `cargo +nightly-2026-05-28 test -p hilavitkutin-api --test column_storage` exercises a conforming impl.

## CHANGE: add `trait Decompose` (new) in `hilavitkutin-api`
File: `mock/crates/hilavitkutin-api/src/storage.rs`
Add `pub trait Decompose { type Leaves: StoreBundle; }`. The cons-list of `Field<S>` scalar leaves an aggregate occupies, in field order; `Empty` for a unit aggregate. Trait-only (the derive is the deferred BACKLOG shape).
Verification: `grep "pub trait Decompose" mock/crates/hilavitkutin-api/src/storage.rs` returns one match; the test asserts `<TwoField as Decompose>::Leaves` resolves to the expected cons-list.

## CHANGE: `lib.rs` declares the storage module and re-exports the two traits
File: `mock/crates/hilavitkutin-api/src/lib.rs`
Add `pub mod storage;` to the module list and `pub use storage::{ColumnStorage, Decompose};` to the re-export block.
Verification: `grep "pub use storage::{ColumnStorage, Decompose}" mock/crates/hilavitkutin-api/src/lib.rs` returns one match.

## Tests

`mock/crates/hilavitkutin-api/tests/column_storage.rs`: written first (red before the traits exist). A stub `ColumnStorage` impl proves the trait is implementable and that `reserve` / `count` interact sanely. A `TwoField` aggregate with a hand-written `Decompose` impl plus a `fn assert_leaves<D: Decompose<Leaves = ...>>()` proves the leaf set resolves to the exact specified cons-list (a compile-time type-equality assertion). The behavioral arena test (reserve, write, read back over real memory) ships with the naive impl next round, where backing memory exists.

## Not in scope (SRC)

The naive arena-backed `ColumnStorage` impl (phase 2, `hilavitkutin-providers`), the storage lint (phase 3), the decomposition derive, the `evict` / `inject` persistence boundary, and the engine store migrations (resource arena, store-backed plan, adapt, dispatch buffers; the #647 rework) are follow-on rounds and PRs on this arc.
