# Src changelist: A3-redo-1 footprint traits

**Date:** 2026-06-20
**Phase:** SRC
**Round:** 202606210200

## CHANGE: add `arvo-tensor` dep to hilavitkutin-api
File: `mock/crates/hilavitkutin-api/Cargo.toml`
Verification: `grep "arvo-tensor" mock/crates/hilavitkutin-api/Cargo.toml` returns one match.

## CHANGE: add `footprint` module (CollectionBytes + ResourceFootprint)
File: `mock/crates/hilavitkutin-api/src/footprint.rs` (new) + `src/lib.rs` (`pub mod footprint;`)
Verification: `grep "pub trait CollectionBytes\|pub trait ResourceFootprint" mock/crates/hilavitkutin-api/src/footprint.rs` returns two matches.

`CollectionBytes { const BYTES }`: Seq<U,N> -> cap_size(N)*ceil(U::BIT_WIDTH/8);
Map<K,V,N> -> cap_size(N)*(ceil(K)+ceil(V)); Field<T> -> 0. `ResourceFootprint
{ const L1_BYTES }` trait definition (impls/derive in A3-redo-2). StoreElemBytes
untouched (no scaling break).

## NEW TEST: `tests/a3_resource_footprint.rs`
File: `mock/crates/hilavitkutin-api/tests/a3_resource_footprint.rs`
Verification: `cargo test -p hilavitkutin-api --test a3_resource_footprint` passes.

A fixture resource (Field/Seq/Map fields, hand ResourceFootprint impl) asserts
`L1_BYTES == N_seq*elem + N_map*(k+v)`, Field contributing 0.
