**Date:** 2026-04-21
**Phase:** DOC
**Scope:** hilavitkutin-api primitive sweep — doc changelist
**Source topic:** `202604330000_topic.api-primitive-sweep.md`

# Summary

Source-only round. No `*.md.tmpl` template changes required in this
slice:

- Trait associated consts and the clock alias are documented inline
  via rustdoc on the items themselves. The generated crate-level
  `docs/*.md` regenerates from source at the end of the round.
- `Nanos` is a new public alias; its rustdoc is authored in `platform.rs`
  alongside the trait. It will appear in `docs/STRUCTURE.md` after
  regeneration without a separate DESIGN.md.tmpl edit.
- No principle, workflow, or agent-rule template updates are required —
  this round does not introduce or retire a principle, only applies
  the existing primitive-vocabulary discipline to code.

# Per-crate plan

- `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`: no changes. The
  existing text references the traits by name and does not pin their
  exact const types.
- `mock/crates/hilavitkutin-api/BACKLOG.md.tmpl`: no changes.
- `mock/crates/hilavitkutin-api/SHAME.md.tmpl`: no changes.
- All other crates: unaffected by this slice.

# Validation

Nothing to validate in DOC phase — the CL is intentionally empty.
Lock and proceed directly to SRC-PLAN.
