# Doc CL: parallel between-frame field interior-mutability discipline

**Round:** 202606191000
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

The DESIGN already states the cross-thread discipline ("the main thread
writes the block only between frames, while every worker is parked"). Add the
aliasing-model reason it is sound: because every worker holds a live shared
reference to the scheduler across the park, the between-frame writes to the
worker-visible scalar fields (the virtual epoch, the cold-start flag, the
change seed) go through interior mutability (Relaxed atomics ordered by the
frame barriers, and a `Cell` for the change seed), never a plain field
write, so no live shared reference is invalidated. One sentence appended to
the existing parked-worker discipline paragraph.
