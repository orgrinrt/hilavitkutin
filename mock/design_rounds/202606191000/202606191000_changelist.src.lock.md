# Src CL: parallel between-frame field interior-mutability

**Round:** 202606191000
**Phase:** IMPL
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`

## CHANGE: field `Scheduler::first_frame` FROM `Bool` TO `AtomicBool`
The cold-start flag. Reads become `.load(Ordering::Relaxed)`, writes
`.store(false, Ordering::Relaxed)`, init `AtomicBool::new(true)`. The
`run_parallel` between-frame write now goes through a shared reference.
Verification: `grep "first_frame: AtomicBool" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: field `Scheduler::virtual_epoch` FROM `USize` TO `AtomicUsize`
The virtual-fire epoch. Per-pass advance becomes `.fetch_add(1, Relaxed)`
(new value = prev + 1), reads `USize(self.virtual_epoch.load(Relaxed))`, init
`AtomicUsize::new(0)`.
Verification: `grep "virtual_epoch: AtomicUsize" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: field `Scheduler::store_dirty` FROM `AccessMask<D::Stores>` TO `Cell<AccessMask<D::Stores>>`
The per-store change seed, main-only (worker-invisible). Accesses become
`.get()` / `.set(..)`; the parallel between-frame reset goes through the
shared reference via `Cell::set`. `Cell` matches the existing `meta_block`
interior-mutability precedent.
Verification: `grep "store_dirty: Cell<AccessMask" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

Ordering rationale: all atomic accesses use `Relaxed`; the per-frame
publish/await Release/Acquire establishes the happens-before that orders
these fields across the worker park, exactly as for the existing
`plan_dirty: [AtomicBool; 256]` and `gate2_accum_live: [AtomicUsize; ..]`.

No behavioural change: values are identical, only the storage gains interior
mutability so the between-frame writes no longer invalidate the parked
workers' live shared reference. Full engine+api suite stays green.
