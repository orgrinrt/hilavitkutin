# Src CL: WAKE_SPIN_BUDGET Default Follows the wake_policy Bench

**Date:** 2026-07-19
**Round topic:** `202607202350_topic.wake-budget-default-zero.md`
**Doc CL:** `202607202351_changelist.doc.lock.md`

## CHANGE: const `RunCfg::WAKE_SPIN_BUDGET` default FROM `USize(128)` TO `USize(0)`

File: `mock/crates/hilavitkutin-api/src/run_cfg.rs`
The doc comment updates to cite the wake_policy bench ruling
(park-immediately wins 1.64x / 1.34x / 1.11x at n = 64 / 1024 / 8192;
evidence memo `202607202340_wake-policy-evidence.md`): the default is
the measured winner, the non-zero middle tier stays a consumer
override, and the canonical 128 remains named as the Topic 6 axis K
budget a host with a costlier wake path would pick.
Verification: `grep "WAKE_SPIN_BUDGET: USize = USize(0)" mock/crates/hilavitkutin-api/src/run_cfg.rs` returns one match; `cargo test -p hilavitkutin` green.
