# Topic: generalized ASM-emission contract tests (backlog note)

**Date:** 2026-06-19
**Phase:** TOPIC
**Scope:** `hilavitkutin` crate BACKLOG (promissory note)
**Source topics:** op directive 2026-06-19; roadmap `202606201900`

## Current state

The `asm_gate` (`mock/benches/asm_gate_fixtures/` + `cargo run --bin asm_gate`,
round 202606111600) disassembles four `#[no_mangle]` fixtures and asserts zero
indirect calls. It catches devirtualisation regression but only that one property,
on four fixed fixtures.

## Change proposed (backlog promissory note)

Record the generalized ASM-emission contract discipline in BACKLOG: extend the gate
into a standing per-typestate harness that, for each codegen-load-bearing typestate
shape, exports a stable named symbol, disassembles it (the existing nm/objdump/otool
fallback), scopes to that symbol's region, and asserts the instructions we EXPECT
present and the ones we DON'T absent. General (property assertions, not fragile
exact-match); each typestate-bearing slice adds its fixture. Catches the codegen
REASON the perf bench only shows as a symptom; a standing red oracle until codegen
matches, alongside the perf gate.

## Not in scope

Implementation; this is the backlog note. The discipline is in roadmap 202606201900;
fixtures land with each typestate slice (per-fiber morsel, RCM-row, head+tail,
phase-overlap).
