# Doc changelist: plan-dims-id-width-guard

**Date:** 2026-06-04
**Phase:** DOC
**Round:** 202606041836 (plan-dims-id-width-guard, branch `fix/hilavitkutin-plan-dims-id-width-guard`)

One sentence into `mock/crates/hilavitkutin/DESIGN.md.tmpl` recording the new up-front `compute_execution_plan` rejection, alongside the existing `PlanError::Cycle` rejection.

## CHANGE: document the id-width rejection in the plan-stage prose

File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

After the `PlanError::Cycle` sentence (the cyclic-dependency rejection at step 2), add a sentence: a registration whose `PlanDims` declares more phases or trunks than the fixed-width `PhaseId` / `TrunkId` can name is rejected up front as `PlanError::PhaseCapacityExceedsIdWidth` / `PlanError::TrunkCapacityExceedsIdWidth`, so an over-wide dims fails loudly instead of silently wrapping ids.

No backticked type that is removed or renamed; the new `PlanError` variants are additive. `PhaseId` / `TrunkId` already exist as pub items (the design-doc-source-mismatch lint stays satisfied).

## Out of scope

Source edits (the guard, the `ADDRESSABLE` consts, the `PlanError` variants, the test) belong to the src changelist of this round.
