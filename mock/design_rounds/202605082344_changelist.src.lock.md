**Date:** 2026-05-08
**Phase:** DRAFT
**Round:** 202605082344
**Source topic:** `202605082344_topic_hint_diagnostic_marker_lists.md`

# Source CL: correct hint diagnostic marker lists

## Scope

`mock/crates/hilavitkutin-api/src/hint.rs` only. Four `note = ...` strings in the four sealed-axis diagnostic attributes corrected per topic Decision 2.

## CHANGE: `UrgencyValue` `note` lists `Immediate, Steady, Relaxed, Deferred`

File: `mock/crates/hilavitkutin-api/src/hint.rs`

Replace the existing note containing `\`Immediate\`, \`Deferred\`, \`Steady\`, \`Adaptive\`, \`Relaxed\`, \`Opportunistic\`` with the corrected enumeration `\`Immediate\`, \`Steady\`, \`Relaxed\`, \`Deferred\``. The corrected list matches the four `impl UrgencyValue for X` lines (82, 90, 98, 106).

Verification:
- `grep -A3 'pub trait UrgencyValue' mock/crates/hilavitkutin-api/src/hint.rs | grep -F 'Available markers: \`Immediate\`, \`Steady\`, \`Relaxed\`, \`Deferred\`'` returns one match.
- `grep -c 'impl UrgencyValue for' mock/crates/hilavitkutin-api/src/hint.rs` returns `4` (matching the corrected list size).

## CHANGE: `DivisibilityValue` `note` lists `Atomic, Adaptive, Interruptible`

File: `mock/crates/hilavitkutin-api/src/hint.rs`

Replace the existing note containing `\`Atomic\`, \`Interruptible\`, \`Divisibility\`` with `\`Atomic\`, \`Adaptive\`, \`Interruptible\``. Drops the `Divisibility` (type alias, not a marker) and adds the omitted `Adaptive`.

Verification:
- `grep -A3 'pub trait DivisibilityValue' mock/crates/hilavitkutin-api/src/hint.rs | grep -F 'Available markers: \`Atomic\`, \`Adaptive\`, \`Interruptible\`'` returns one match.
- `grep -c 'impl DivisibilityValue for' mock/crates/hilavitkutin-api/src/hint.rs` returns `3` (matching the corrected list size).

## CHANGE: `SignificanceValue` `note` lists `Critical, Important, Normal, Opportunistic, Optional`

File: `mock/crates/hilavitkutin-api/src/hint.rs`

Replace the existing note containing `\`Critical\`, \`Important\`, \`Normal\`, \`Optional\`, \`Significance\`` with `\`Critical\`, \`Important\`, \`Normal\`, \`Opportunistic\`, \`Optional\``. Drops the `Significance` (type alias) and adds the omitted `Opportunistic`.

Verification:
- `grep -A3 'pub trait SignificanceValue' mock/crates/hilavitkutin-api/src/hint.rs | grep -F 'Available markers: \`Critical\`, \`Important\`, \`Normal\`, \`Opportunistic\`, \`Optional\`'` returns one match.
- `grep -c 'impl SignificanceValue for' mock/crates/hilavitkutin-api/src/hint.rs` returns `5` (matching the corrected list size).

## CHANGE: `SchedulingHint` `note` aggregates the three corrected per-axis enumerations

File: `mock/crates/hilavitkutin-api/src/hint.rs`

Replace the SchedulingHint note's parenthetical enumeration with the three corrected lists from CHANGES 1-3, in order: U axis, D axis, S axis.

Verification:
- `grep -F '\`Immediate\` / \`Steady\` / \`Relaxed\` / \`Deferred\` for U' mock/crates/hilavitkutin-api/src/hint.rs` returns one match.
- `grep -F '\`Atomic\` / \`Adaptive\` / \`Interruptible\` for D' mock/crates/hilavitkutin-api/src/hint.rs` returns one match.
- `grep -F '\`Critical\` / \`Important\` / \`Normal\` / \`Opportunistic\` / \`Optional\` for S' mock/crates/hilavitkutin-api/src/hint.rs` returns one match.

## Lock criteria

- All four CHANGE blocks land verbatim.
- `cargo check --workspace` passes clean.
- `cargo test -p hilavitkutin-api --test access_set` passes 7/7.
- No other source file edited.
