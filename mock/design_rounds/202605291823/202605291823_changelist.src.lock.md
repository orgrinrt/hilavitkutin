# Source changelist: drop redundant min_specialization gate

**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291823 (drop redundant min_specialization gate)

## Summary

Remove the redundant `#![feature(min_specialization)]` gate from
hilavitkutin-api (the retained full `specialization` gate subsumes it; nothing
uses the min subset specifically) and correct the `column_value.rs` doc which
inaccurately attributed the blanket-default-override to `min_specialization`.

## CHANGE: remove `#![feature(min_specialization)]` from `hilavitkutin-api`
File: `mock/crates/hilavitkutin-api/src/lib.rs`
Verification: `! grep -q "feature(min_specialization)" mock/crates/hilavitkutin-api/src/lib.rs`; `cargo +nightly-2026-05-28 check -p hilavitkutin-api -p hilavitkutin --all-targets` clean (empirically confirmed: full `specialization` covers the only spec use, `ColumnValue::BIT_WIDTH`).

## CHANGE: correct `column_value.rs` doc from min_specialization to full specialization
File: `mock/crates/hilavitkutin-api/src/column_value.rs`
Verification: the module doc now reads "blanket impl uses full `specialization`"; the prior inaccurate "uses `min_specialization`" attribution is gone. `grep -q "uses full" column_value.rs` matches. The two remaining `min_specialization` mentions are explanatory (naming the shape full spec is needed for / that min rejects), co-located with the #631 tracking note above the blanket impl. So `min_specialization` still appears as prose but never as a feature gate or a "uses min" claim.

## Lock criteria

- `#![feature(min_specialization)]` gone from hilavitkutin-api; full `specialization` gate unchanged (retained for column_value per #623, tracked-exception comment intact).
- `column_value.rs` doc accurate (names full specialization, references #631).
- `cargo +nightly-2026-05-28 check --workspace --all-targets`: 0 errors.
- `cargo +nightly-2026-05-28 test --workspace` green.
