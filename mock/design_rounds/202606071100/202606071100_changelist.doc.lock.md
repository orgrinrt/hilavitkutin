# Doc changelist: GATE-2 const-gated walk mechanism (round 2a)

**Round:** 202606071100 (topic: gate2-const-gated-dispatch)
**Phase:** DOC
**Scope:** `mock/crates/hilavitkutin/BACKLOG.md.tmpl` only.

## Slice this round

This round lands round 2a: the const-gated walk MECHANISM (`BundleMasks` over the value carrier `WuCons`, a `RunFiber::run_head` per-unit step, a `RunGatedTrunk` gated trait that runs only a `(phase, trunk)`'s members, and a `phase_count` const fn). It does not re-point `Scheduler::run`; the const-generic phase/trunk dispatcher + the `run` re-point is round 2b (a follow-on round on this branch). Splitting the risky const-gate-around-real-projection composition (2a) from the live-path integration (2b) keeps each round tractable.

## Why no DESIGN.md.tmpl change this round

The consumer contract (`Scheduler::run` signature and output) is unchanged: round 2a adds internal dispatch machinery that nothing drives yet, and round 2b's re-point is output-equivalent. The trunk/phase/waist model becomes consumer-observable only at G2-N (when parallelism is the point); the DESIGN prose for it lands then. This round records progress in BACKLOG, matching round 1.

## CHANGE: extend the GATE-2 BACKLOG note with the dispatch mechanism

File: `mock/crates/hilavitkutin/BACKLOG.md.tmpl`
Append to the existing "GATE-2 const grouping and per-trunk dispatch" section that round 2a lands the const-gated walk machinery (the mechanism that turns the round-1 grouping into per-(phase, trunk) member-only programs), with the dispatcher + run re-point following in 2b. Names stay unbackticked per BACKLOG convention.
