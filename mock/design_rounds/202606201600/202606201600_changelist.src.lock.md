# Src changelist: per-fiber morsel slice 1 review fixes

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606201600

## CHANGE: `PlanColumn::MorselSizes` FROM `MorselSizes` TO `MorselWindows`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep -n "MorselWindows" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns the variant + match arm + accessor use; `! grep "MorselSizes"`. The `=> 4` wire key is unchanged.

## CHANGE: correct stale "morsel-sizes" doc strings to "morsel-windows"
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `! grep -i "morsel-sizes\|morsel sizes" mock/crates/hilavitkutin/src/scheduler/mod.rs`.

`morsel_windows_id` doc (`:297`), the fiber-count doc (`:314`), and the two
column-pool docs (`:536`, `:619`).

## CHANGE: drop the em-dash in the blueprint research note
File: `mock/research/202606201400_per-fiber-morsel-blueprint.md`
Verification: `! grep "—" mock/research/202606201400_per-fiber-morsel-blueprint.md`.
