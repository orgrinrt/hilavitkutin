# Topic: per-fiber morsel slice 1 review fixes

**Date:** 2026-06-19
**Phase:** TOPIC
**Scope:** `hilavitkutin` scheduler (complete the rename: stale docs + the PlanColumn variant)
**Source topics:** pr-reviewer-senior on PR #151

## Current state

Slice 1 renamed `morsel_sizes` -> `morsel_windows` but left the rename incomplete:
the renamed `morsel_windows_id()` and surrounding docs still say "morsel-sizes",
and the `PlanColumn::MorselSizes` enum variant kept the old name. A rename slice
shipping its own symbol's stale docs / old variant name has not finished.

## Changes proposed

1. `PlanColumn::MorselSizes` -> `MorselWindows` (`scheduler/mod.rs:237`, match arm
   `:275`, accessor use `:299`). The `=> 4` wire-key position is unchanged.
2. Correct the stale "morsel-sizes" / "morsel sizes" doc strings (`:297`, `:314`,
   `:536`, `:619`) to "morsel-windows" / "morsel windows".
3. Drop the lone em-dash in the merged blueprint research note
   (`mock/research/202606201400_per-fiber-morsel-blueprint.md`, ungated but
   discipline).

No behaviour change (doc + identifier rename; the variant's wire key is stable).

## Not in scope

The per-fiber morsel slices 2-5 (descriptor field, loop inversion, L1 formula,
parallel). This is the slice-1 rename completion only.
