# Doc CL: api Executor sealed allow (no template change)

**Round:** 202606192200
**Phase:** DOC

A rustc `#[allow(private_bounds)]` on an internal sealed supertrait bound touches
no documented surface; no `*.md.tmpl` change.
