# Src CL: api Executor sealed private_bounds allow

**Round:** 202606192200
**Phase:** IMPL

## CHANGE: ADD `#[allow(private_bounds)]` to `Executor`
File: `mock/crates/hilavitkutin-api/src/platform.rs:322`
Add the attribute + a sealed-trait rationale comment above `pub trait Executor`.
Verification: `grep -B2 "pub trait Executor" mock/crates/hilavitkutin-api/src/platform.rs` shows the allow; `cargo build -p hilavitkutin-api` emits zero warnings.
