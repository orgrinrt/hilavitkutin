# Doc changelist: E7 incremental-skip (GATE-1)

**Round:** 202606111200 (E7 dirty-skip incremental)
**Phase:** DOC

## Scope

The engine `DESIGN.md.tmpl` already carries the E7 design at altitude: plan step 12 ("dirty propagation masks for incremental skip"), the `Scheduler::run()`-entry dirty bit (the `PlanAffecting`-keyed plan-recompute path), and "hilavitkutin owns generation counters". This round implements E7; the doc refinement makes two distinctions concrete so the implementation has an unambiguous design-of-record and a later reader does not conflate the two dirty mechanisms or expect the meta-WU loop in the GATE-1 run path.

## File: `mock/crates/hilavitkutin/DESIGN.md.tmpl`

Add a short paragraph after the plan-step list (near step 12) that states the incremental-skip mechanism concretely: plan step 12 produces a per-unit predecessor mask keyed by carrier position; at `Scheduler::run()` entry the engine seeds a dirty set from per-root change signals (a `PlanAffecting` resource swapped via `replace_resource`, a non-plan-affecting value swapped via `replace_value`, an input the consumer marked changed, or the cold first frame where all roots are dirty), propagates it forward over the predecessor masks in carrier order, and skips any unit whose entire predecessor set is clean. State that this domain-16 incremental-skip dirty (which units to skip) is distinct from the domain-22 `PlanAffecting` plan-recompute dirty already described above (whether to rebuild the plan), and that the latter's recompute consumption is sequenced later (E8 adapt).

Add one sentence noting the self-hosting meta-WU frame loop (`PlanStage` / `ScheduleReady` / `PassStart` / `ScheduleEnd`) is the N-core self-hosting form (E4), so at one core the run path is the static carrier walked over runtime record ranges plus the dirty gate, not a meta-WU loop.

Prose only; no new code-identifier backticks (the new types land in the src phase). Existing-type backticks (`Scheduler`, `replace_resource`, `replace_value`, `PlanAffecting`, the meta markers already named in this doc) only.

## Verification

`grep "incremental-skip\|predecessor mask" mock/crates/hilavitkutin/DESIGN.md.tmpl` returns the added paragraph; the `design-doc-source-mismatch` lint stays green (no new backticked type names introduced ahead of source).
