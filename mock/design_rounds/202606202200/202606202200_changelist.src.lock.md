# Src changelist: per-fiber morsel A2b-1

**Date:** 2026-06-19
**Phase:** SRC
**Round:** 202606202200

## CHANGE: add `Scheduler::run_one_trunk_windowed`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
Verification: `grep "pub fn run_one_trunk_windowed" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

Same `RunGatedTrunk` bounds as `run_one_trunk`; takes `window: USize`; loops
`[0, total)` in windows of `window.0.max(1)`, one whole-fiber `run_trunk` call per
window (all-ones dirty). The proven A2a fiber-outer/morsel-inner entry.

## NEW TEST: `tests/a2b_windowed_trunk.rs`
File: `mock/crates/hilavitkutin/tests/a2b_windowed_trunk.rs`
Verification: `cargo test -p hilavitkutin --test a2b_windowed_trunk` passes.

A one-WU carrier (one trunk); `run_one_trunk_windowed::<_,_,0>(USize(4))` over
`total=10` records the per-morsel start index in each `execute` and asserts the
sequence is `[0, 4, 8]` (windowed, three morsels), versus the single whole-range
call `run_one_trunk` would make.
