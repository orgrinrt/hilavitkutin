# Src changelist: GATE-2 R4c round A, frame wake-word protocol

**Date:** 2026-06-07
**Phase:** SRC
**Scope:** `hilavitkutin-api/src/platform.rs`, `hilavitkutin/src/thread/`, `hilavitkutin/tests/`

## CHANGE: add fields `seq` / `done` / `exited` to `PoolFrame`
File: `mock/crates/hilavitkutin-api/src/platform.rs`
Detail: three `AtomicU32` fields for per-frame orchestration: `seq` (frame
wake word), `done` (frame-completion counter), `exited` (worker-exit counter).
Pub fields, doc comments, additive (no construction site in src today).
Verification: `grep "pub seq: AtomicU32" mock/crates/hilavitkutin-api/src/platform.rs`
returns one match.

## CHANGE: add module `thread/frame.rs` with the frame protocol
File: `mock/crates/hilavitkutin/src/thread/frame.rs` (new)
Detail: lost-wakeup-safe helpers over `&PoolFrame`: `frame_publish`,
`frame_await_done`, `request_shutdown`, `await_exit` (main side); `frame_await`,
`frame_done_arrive`, `frame_exit_arrive` (worker side). Each park is
load-check-`atomic_wait(word, observed)`.
Verification: `grep "pub fn frame_publish" mock/crates/hilavitkutin/src/thread/frame.rs`.

## CHANGE: register + re-export `thread::frame`
File: `mock/crates/hilavitkutin/src/thread/mod.rs`
Detail: `pub mod frame;` plus a `pub use frame::{...}` line for the seven helpers.
Verification: `grep "pub mod frame" mock/crates/hilavitkutin/src/thread/mod.rs`.

## CHANGE: add engine test `tests/frame_protocol.rs`
File: `mock/crates/hilavitkutin/tests/frame_protocol.rs`
Detail: fail-first multi-thread test. Real `PoolFrame`, std-thread workers driven
through the helpers over several frames + shutdown; asserts formula-exact
per-frame output and exit-counter join, deadlock-clean. Fails before the helpers
exist, passes after.
Verification: `cargo test -p hilavitkutin --test frame_protocol` passes.
