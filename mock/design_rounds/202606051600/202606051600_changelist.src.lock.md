# Src changelist: post-review cleanup

**Date:** 2026-06-05
**Phase:** SRC
**Round:** 202606051600 (dispatch-post-review-cleanup, branch `feat/hilavitkutin-fiber-structured-dispatch`)

## CHANGE: DELETE trait `AccumFreeBindings` and its five impls
File: `mock/crates/hilavitkutin/src/resource/bindings.rs`
Round 202606051500 removed its only consumer (the whole-pipeline dispatch guard) when the per-fiber `morsel_local` bit replaced it; the dead declaration and its stale doc comment are deleted per `no-legacy-shims-pre-1.0`.
Verification: `! grep AccumFreeBindings mock/crates/hilavitkutin/src/resource/bindings.rs`; the crate still builds (the trait had no other reference).

## CHANGE: ADD fiber-count completeness assert in `derive_phase_dispatch_order`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
A `debug_assert_eq!(fd, plan.fiber_count.0, ...)` after the walk, matching the existing `next == plan.unit_count.0` unit-completeness assert, so a `fiber_count > fd_cap` truncation (unreachable for a valid plan) is loud in debug/test rather than silently skipping a fiber's units at dispatch.
Verification: `grep "debug_assert_eq!(fd" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match; the suite stays green.

No test change: the existing `fiber_structured_dispatch.rs` and `morsel_outer.rs` cover the behaviour; this round is dead-code removal plus a debug-only assert.
