# Changelist (doc): FFI-boundary primitive policy

**Date:** 2026-04-24
**Phase:** DOC
**Source topics:** `202604510000_topic.ffi-boundary-primitive-policy.md`

## Doc-side changes

### `mock/crates/hilavitkutin-extensions/DESIGN.md.tmpl`

Under "Public items" update the descriptor entry:

- `ExtensionDescriptor.name_len` / `capabilities_len` / `required_host_caps_len` typed as `USize` instead of `usize`.
- Add a short "FFI-wire primitive policy" subsection describing the two-tier rule (arvo transparent newtype where wire-identical; bare primitive with `lint:allow` tracked: #206 where not).
- `ExtensionVersion` entry notes bare `u16` fields are deliberate FFI-wire choices, not cop-outs.
- `CapabilityId` entry notes the `pub u64` field is the deliberate FFI constructor pattern.

### `mock/crates/hilavitkutin-extensions/BACKLOG.md.tmpl`

Add a BACKLOG item: when arvo ships `UWire<N>` (wire-stable N-bit unsigned at `#[repr(C)]` boundaries, no Strategy axis), flip `abi_version`, `ExtensionVersion` fields, and any other FFI-bare-numeric sites here to use it. Reference #206 and the arvo BACKLOG counterpart.

### Src-side changes deferred

- `src/descriptor.rs` field retypes + `lint:allow` annotations land with the src changelist.
