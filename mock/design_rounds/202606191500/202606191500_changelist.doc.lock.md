# Doc CL: Units-cap guard

**Round:** 202606191500
**Phase:** DOC
**Target:** `mock/crates/hilavitkutin/DESIGN.md.tmpl`

One sentence in the PlanDims section: the grouping unit cap currently requires
`Units::CAP <= GATE2_MAX_UNITS` because `run_parallel`'s `gate2_phase` /
`gate2_trunk` arrays are sized by that constant; a compile-time assertion in
`run_parallel` enforces it, and a follow-up (#690) lifts those arrays onto
`Units` to remove the ceiling.
