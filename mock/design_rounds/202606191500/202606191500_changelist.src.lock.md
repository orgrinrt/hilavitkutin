# Src CL: Units-cap compile-time guard

**Round:** 202606191500
**Phase:** IMPL

## CHANGE: import `cap_size` in `scheduler/mod.rs`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
`use arvo_tensor::{cap_size, Capacity, ConstCapacity};` (add `cap_size`).
Verification: `grep "use arvo_tensor::{cap_size" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match.

## CHANGE: ADD compile-time Units-cap guard at the top of `run_parallel`
File: `mock/crates/hilavitkutin/src/scheduler/mod.rs`
A `const { assert!(cap_size(<D::Units as Capacity>::CAP) <= GATE2_MAX_UNITS, "...") }`
at the start of `run_parallel`'s body, evaluated per-D monomorphisation. Fails
the build with a clear message when a consumer's `Units` capacity exceeds the
`GATE2_MAX_UNITS` ceiling that still sizes `gate2_phase`/`gate2_trunk`, instead
of an out-of-bounds panic at runtime. Closes the P0.1c capstone-review finding 1
latent hole (the full lift of the parallel scratch onto `Units` lands in #690).
Verification: `grep "cap_size(<D::Units as Capacity>::CAP) <= GATE2_MAX_UNITS" mock/crates/hilavitkutin/src/scheduler/mod.rs` returns one match; full suite stays green (every in-repo `PlanDims` has `Units <= Dim<128>`, well under 256, so the assert never fires for shipped configs).

Negative test (a `compile_fail` fixture for an over-256 `Units`) lands with the
#690 parallel-scratch lift; the positive proof is the green suite (the guard
does not false-fire on any shipped config). No runtime cost, no behavioural
change for valid configs.
