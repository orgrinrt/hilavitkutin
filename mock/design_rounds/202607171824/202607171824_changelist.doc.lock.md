# Doc changelist: soften the engine-reads-fourth-axis claim

**Date:** 2026-07-17
**Round:** `202607171824_topic.plannable-post-review-fixes`
**Phase:** DOC
**Scope:** One doc softening in `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`.

## CHANGE: soften "the engine reads urgency, divisibility, and significance and ignores the extension"

File: `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`
The sentence states an engine mechanism that is not implemented (the engine is untouched;
`SchedulingHint` has no extraction API; no `HintExt` markers ship). Restate as intent: a 4-tuple
hint schedules as its `(U, D, S)` prefix would, and reading a fourth dimension is a later gate for
whichever runner defines one. No claim of present engine behaviour.

## Verification

Writing-style gate passes (no em-dashes, no hype). The softened sentence names no shipped engine
mechanism.
