# Src changelist: blanket test + E0119 doc note + hint.rs softening

**Date:** 2026-07-17
**Round:** `202607171824_topic.plannable-post-review-fixes`
**Phase:** IMPL
**Scope:** Two source edits and one test addition. No trait, impl, or export changes.

## CHANGE: test the blanket Plannable impl on a real WorkUnit
File: `mock/crates/hilavitkutin-api/tests/work_unit.rs`
Add a compile-assert that `Integrate` (the file's existing `WorkUnit<Always>`) satisfies
`Plannable<Always>` with `Read`/`Write`/`Hint` recovered equal to its `WorkUnit` associated types.
This monomorphises the blanket forwarding for a real WorkUnit, which nothing in the workspace did
before. Closes the src-CL-vs-shipped gap from the prior round.
Verification: `grep "Plannable<Always>" mock/crates/hilavitkutin-api/tests/work_unit.rs` returns a
match; `cargo test -p hilavitkutin-api --test work_unit` passes.

## CHANGE: add the both-is-a-conflict note to the Plannable blanket doc
File: `mock/crates/hilavitkutin-api/src/work_unit.rs`
Add to the blanket impl's doc comment: a downstream type must not be both a `WorkUnit` and
hand-implement `Plannable`, because the hand impl then conflicts with the blanket (E0119). Stated so
it is read, not discovered at a red build.
Verification: `grep -i "must not be both" mock/crates/hilavitkutin-api/src/work_unit.rs` returns a
match.

## CHANGE: soften the hint.rs engine-ignores-fourth sentence
File: `mock/crates/hilavitkutin-api/src/hint.rs`
Restate "The engine reads the first three positions and ignores the fourth" as intent (a 4-tuple
schedules as its `(U, D, S)` prefix would; reading a fourth dimension is a later gate), matching the
DESIGN.md.tmpl softening.
Verification: `! grep "ignores the fourth" mock/crates/hilavitkutin-api/src/hint.rs`.
