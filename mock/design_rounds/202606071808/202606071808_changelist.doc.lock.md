# Doc changelist: gate2 worker-side waist barrier

**Round:** 202606071805 (gate2-worker-side-waist-barrier)
**Phase:** DOC

## Scope

No `*.md.tmpl` design-doc changes. The worker-side waist barrier is an internal
engine mechanism (the parallel dispatch path's inter-phase synchronisation); no
shipping-contract design doc names the barrier, `PoolFrame`, or `run_parallel`
surface (grep of `mock/crates/*/*.md.tmpl` + `mock/DESIGN.md.tmpl` returns no
references), so there is nothing to keep in sync here. The substance lives in
the source changelist.

## Files

(none)
