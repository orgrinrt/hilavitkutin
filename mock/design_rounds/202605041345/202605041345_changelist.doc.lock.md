**Date:** 2026-05-04
**Round:** 202605041345
**Phase:** DOC
**Source topic:** 202605041345_topic_providers_v0_1_kit_bridge.md (#329)

# Doc changelist

Per-file mechanical doc edits for round 202605041345. Three crates
touched: api (new sealed trait section), engine (one paragraph naming
the new BuilderExtending companion trait it impls), providers
(promote the InternerKit deferral block from BACKLOG into DESIGN, add
a usage subsection).

The DESIGN.md.tmpl edits are the load-bearing surface; per-crate
README.md.tmpl, BACKLOG.md.tmpl, and the kit DESIGN.md.tmpl get
small consistency edits to remove now-stale "deferred to v0.1"
language.

## CHANGE: api/DESIGN.md.tmpl gains `BuilderResource<T>` sealed-trait section

File: `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`
Insertion point: after the `### BuilderExtending<B> sealed trait`
section (line 261-288), before the `### Depth trait` section (line
290).

Section body declares the trait shape, names the
`builder_resource_sealed::Sealed<T>` private supertrait, gives the
engine impl skeleton, names the `with_resource(self, init: T) ->
Self::WithResource` method, and explains the use case (Kit impls
in providers crates that cannot import the engine). One sentence
points at hilavitkutin-providers's InternerKit as the first caller.

Verification: `grep -n "BuilderResource" mock/crates/hilavitkutin-api/DESIGN.md.tmpl`
returns at least one match; the trait name appears in section heading.

## CHANGE: api/DESIGN.md.tmpl mentions BuilderResource in module-overview list

File: `mock/crates/hilavitkutin-api/DESIGN.md.tmpl`

The api crate's module-level introduction (top of file, around lines
1 to 12) does not currently enumerate the builder-support traits. No
edit there. The new section is reachable by direct anchor link.

If a top-level builder-trait list exists elsewhere in the file, add
`BuilderResource<T>` to it. Otherwise, no edit.

Verification: `grep -c "BuilderResource" mock/crates/hilavitkutin-api/DESIGN.md.tmpl`
returns 1 or more (the section heading itself).

## CHANGE: providers/DESIGN.md.tmpl promotes InternerKit from BACKLOG note to DESIGN section

File: `mock/crates/hilavitkutin-providers/DESIGN.md.tmpl`

Edit 1: under the existing surface table (lines 14 to 22), add a row
for `InternerKit<BYTES, ENTRIES>` with purpose "Kit impl that
registers `Resource<StringInterner<MemoryArena<BYTES, ENTRIES>>>` via
`add_kit`.".

Edit 2: rewrite the section "What v0 deliberately does not ship"
(lines 86 to 103) to reflect that the InternerKit ships in v0.1 as
of this round. Drop the "deferred to v0.1" sentence on InternerKit;
keep the M-parameterised arena and ColumnStorage / Clock / Memory
deferrals (those are still future scope).

Edit 3: add a new subsection under the surface descriptions
explaining the `InternerKit<BYTES, ENTRIES>` shape:
  - struct shape (unit struct with two const generics)
  - `Kit<B>` impl bound (`B: BuilderResource<StringInterner<MemoryArena<BYTES, ENTRIES>>>`)
  - install body forwards to `builder.with_resource(default_interner::<...>())`
  - usage example: `Scheduler::builder().add_kit(InternerKit::<4096, 256>).build()`
  - cross-reference to api's `BuilderResource<T>` for the bridging contract.

Verification: `grep -n "InternerKit" mock/crates/hilavitkutin-providers/DESIGN.md.tmpl`
returns at least 3 matches (table row, subsection heading, cross-reference).

## CHANGE: providers/BACKLOG.md.tmpl removes InternerKit deferral note

File: `mock/crates/hilavitkutin-providers/BACKLOG.md.tmpl`
Section to remove: lines 7 to 19 (`## InternerKit Kit impl (v0.1)`
heading plus paragraph plus "Gated on" line).

Reason: the entry now ships; promoted to DESIGN. Per
`mock-workspace.md` rule "Promote items into DESIGN.md.tmpl when they
ship; don't duplicate."

The remaining sections (`## M-parameterised arena variant (v1)`,
`## Sync arena variant (v1)`, `## Future provider modules`) stay
untouched. They are still future scope.

Verification: `grep -c "InternerKit" mock/crates/hilavitkutin-providers/BACKLOG.md.tmpl`
returns 0.

## CHANGE: providers/README.md.tmpl drops "Future modules add..." Kit deferral

File: `mock/crates/hilavitkutin-providers/README.md.tmpl`
Lines 1 to 9 of current README:

```markdown
# `hilavitkutin-providers`

Default Resource-backed providers for the hilavitkutin scheduler.
Standalone ecosystem crate; consumed by the engine via Kit
installation, no reverse dependency. v0 ships the InternerKit
(an inline-storage MemoryArena wrapped by `StringInterner`).
Future modules add ColumnStorage, Clock, and Memory defaults as
consumer demand surfaces.
```

The README copy says "v0 ships the InternerKit" today; that is
inaccurate per the v0 scope-down. Round 202605041345 makes it
accurate. Edit: change "v0 ships the InternerKit" to "ships the
InternerKit (v0.1)" so the README reads honestly post-promotion.
Other lines unchanged. Stays under 10 lines (per
`.claude/rules/readmes.md`).

Verification: `grep -c "InternerKit" mock/crates/hilavitkutin-providers/README.md.tmpl`
returns 1.

## CHANGE: kit/DESIGN.md.tmpl drops the SchedulerBuilder-direct example or marks it engine-internal

File: `mock/crates/hilavitkutin-kit/DESIGN.md.tmpl`
Lines 49 to 71 currently show an InternerKit Kit impl that names
`SchedulerBuilder<MAX_UNITS, MAX_STORES, MAX_LANES, Wus, Stores>`
directly. This is the example a downstream-engine-aware crate would
write; it stays accurate as one shape Kit impls can take.

The kit/DESIGN gets one paragraph after this example pointing at the
new providers/api pattern: a Kit declared in a crate that cannot
import the engine generalises over `B: BuilderResource<T>` (or future
sibling traits) instead of naming `SchedulerBuilder` directly. Brief
enough to not duplicate the api section; long enough to direct the
reader to the right shape.

Verification: `grep -c "BuilderResource" mock/crates/hilavitkutin-kit/DESIGN.md.tmpl`
returns at least 1.

## What this changelist does NOT do

- Does not edit `mock/DESIGN.md.tmpl` (workspace-level). The new
  trait lives inside hilavitkutin-api; the crate-level DESIGN edits
  carry the design statement. Workspace-level DESIGN regenerates
  from `{{crate_summaries}}` by inserting the README.md.tmpl content;
  no manual edit is needed.
- Does not edit `mock/PRINCIPLES.md.tmpl`. No new principle is
  introduced.
- Does not edit `mock/mockspace.toml`. No lint-config change. The
  new trait is a normal sealed-trait addition; existing forbidden-
  imports rules already permit the cross-crate dep direction
  (engine -> api remains; providers -> api remains).
- Does not edit any agent-rule template (`mock/agent/...`). The new
  trait does not change agent guidance.

## Sketch evidence

No sketch is required for this round. The trait shape is a direct
mechanical lift of the existing `BuilderExtending<B>` sealed pattern
(read api/builder.rs lines 49 to 51 and lines 199 to 213 for the
template). No trait-solver-cycle risk, no generic-const-expr risk,
no rustc-feature gating beyond what api already uses. The single
non-obvious edge (the engine's `with_resource` impl forwards to
`.resource()` without changing return type) is the same pattern as
the existing `add_kit` -> `Kit::install` forward.

## Lock criteria

- All five files edited per the CHANGE blocks above.
- `cargo mock --lint-only --commit` passes.
- doc CL grep verifications all return expected counts.
