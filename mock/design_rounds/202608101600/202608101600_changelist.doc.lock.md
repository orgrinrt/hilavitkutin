# Doc changelist: the header says what the file is

**Date:** 2026-08-10
**Phase:** DOC
**Topic:** `202608101600_topic.a-locked-claim-that-was-never-executed.md`

## CHANGE: the overview's header, from a false claim to an accurate one

File: `docs/HILAVITKUTIN_SYM_OVERVIEW.md`

Replaces "there is no template behind this file, so nothing regenerates it",
both halves of which are false, with what the file actually is: a stale rendering
of `DESIGN.md.tmpl` under a filename the current generator no longer emits,
superseded by the template and awaiting the rename migration.

It also stops inviting hand edits, since a generator run deletes this path rather
than updating it.

Verification, run as a command against `git show HEAD` before this changelist
locks: `grep -c 'no template behind this file'` returns 0, and `grep -c 'stale
rendering'` returns 1.
