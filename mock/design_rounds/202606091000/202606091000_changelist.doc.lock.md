**Date:** 2026-06-09
**Phase:** DOC
**Round:** 202606091000 (d1e-runfiber-dispatch)

# Doc changelist: no doc changes (source aligns to existing design)

## Current state

`mock/crates/hilavitkutin/DESIGN.md.tmpl` already specifies this round's target end-state:

- Lines 130-145 name `RunFiber` as the sequential walk that constructs each unit's `EngineCtx` and runs `execute` over a value-carrying `WuCons` / `WuNil` unit list, projecting resources via `Project`, columns via `ColProject`, accumulators via `AccumProject`, with parallel witness lists inferred at the call site and no caller turbofish. It states "No unit dispatches through a stored function pointer, so a fiber's unit sequence monomorphises into one straight-line body that devirtualizes under fat LTO."
- Lines 155-157 state "There is no type-erased per-unit dispatch slot: the column-capable walk is the only dispatch path."
- Lines 181-182 already name "the per-fiber, or single-core whole-schedule, monomorphised body" (the flat Approach E body this round wires).

The shipped source contradicts the doc: `Scheduler::run` materialises a `FiberSlot` (type-erased instance pointer plus shim fn-pointer) per unit via `CollectFiber` and dispatches through a `topo_order`-indexed fn-pointer call (`live[order[k].0]`). That is the C2 slice-3 scaffold, written before the `RunFiber` walk existed; it is the drift, and the doc is the oracle.

## Changes proposed

None. This round makes no `*.md.tmpl` edits. The design already mandates the `RunFiber`-only dispatch path; the src changelist brings source into alignment by adding the `RunFiber` trait (the one design-named type currently absent from source) and removing the `FiberSlot` / `CollectFiber` scaffold (which appears in no design doc).

The `RunFiber` execution order in the doc is the RCM-reordered topological order. For the GATE-1 workloads (linear chains and single accumulator WUs) the carrier registration order is the unique topological order and coincides with the RCM order, so the carrier walk dispatches in that order without further machinery. For non-linear DAGs the registration order validated by D1d (`BuildError::NonTopologicalRegistration` names the RCM-recommended order) is the provisional interim per roadmap r2 op-decision (b); the auto-RCM guarded walk (sketch 202606090300) is the near-term follow-on per op-decision (a). The doc describes the target; the interim mechanism lives in source and the src changelist, not in the design oracle.

## Lock criteria

- No `*.md.tmpl` files are edited in this round.
- The src changelist adds `RunFiber` so every `RunFiber` reference in `DESIGN.md.tmpl` (lines 130, 153) resolves to a real source type, and removes `FiberSlot` / `CollectFiber`, which the design doc does not name, so no `design-doc-source-mismatch` violation is introduced.
