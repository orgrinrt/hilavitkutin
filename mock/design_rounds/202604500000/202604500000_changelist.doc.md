# Changelist (doc): extensions cop-out cleanup

**Date:** 2026-04-24
**Phase:** DOC
**Source topics:** `202604241645_topic.extensions-cop-out-cleanup.md`

## Doc-side changes

### `mock/crates/hilavitkutin-extensions/DESIGN.md.tmpl`

- Introduce `AbiVersion` in the "Descriptor module" section: `#[repr(transparent)] AbiVersion(pub u32)` semantic newtype over the host-side ABI version value.
- Update `HOST_ABI_VERSION` line: `HOST_ABI_VERSION: AbiVersion` (was `u32`).
- Update `DESCRIPTOR_SYMBOL` line: `DESCRIPTOR_SYMBOL: &CStr` (was `&[u8]`), describing the const unsafe construction via `CStr::from_bytes_with_nul_unchecked`.
- Update the `ExtensionError::AbiVersionMismatch` variant signature: `{ expected: AbiVersion, got: AbiVersion }` (was `u32, u32`).
- Note in the passage that the FFI-wire `ExtensionDescriptor.abi_version: u32` field is unchanged; host code wraps into `AbiVersion` explicitly before comparison.

### `mock/crates/hilavitkutin-extensions/BACKLOG.md.tmpl`

- Remove any "wrap ABI version / DESCRIPTOR_SYMBOL / error payload" items now shipping.

### What this changelist does not touch

- Per-round SRC work: the actual Rust edits in `mock/crates/hilavitkutin-extensions/src/descriptor.rs` and `src/error.rs`. Those land with the src changelist.
- FFI wire types: `ExtensionDescriptor.abi_version`, `ExtensionVersion.*`, `CapabilityId(pub u64)`, `ExtensionMeta::NAME`. Out of scope per topic; deferred to #206.
