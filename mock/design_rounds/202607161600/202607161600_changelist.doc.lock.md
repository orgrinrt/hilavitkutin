# Doc changelist: post-review fixes for the A3b round

**Date:** 2026-07-16
**Phase:** DOC (this changelist opens it; no doc-template edits in scope)
**Scope:** reviewer findings on PR #166 (round 202607161410): the stale step-9
rustdoc and the clamp-invariant guard. No `*.md.tmpl` edits; this changelist
walks the phase machine to IMPL.

## Changes proposed

None on doc templates. The source changelist carries: (1) the
`compute_fiber_morsel_windows` rustdoc rewritten to the domain-12 window
contract (the body was rewritten, the item docs still described the retired
partition placeholder), and (2) a `debug_assert!` on `min_morsel <= max_morsel`
plus a catalogued ignored test for the misconfigured-clamp and
non-4-aligned-floor edge, per the edge-cases-as-tests discipline.
