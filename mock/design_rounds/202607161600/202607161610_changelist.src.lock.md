# Src changelist: A3b post-review clarifications

**Date:** 2026-07-16
**Phase:** IMPL (this changelist opens it)
**Doc CL:** `202607161600_changelist.doc.lock.md`

## CHANGE: `compute_fiber_morsel_windows` rustdoc restated to the window contract
File: `mock/crates/hilavitkutin/src/plan/steps.rs`
The item docs state the domain-12 formula, the write-mask-union semantics, and
the zero-sum / unbudgeted fallbacks, replacing the retired partition prose.
Verification: `grep -A4 "Step 9" mock/crates/hilavitkutin/src/plan/steps.rs` shows the formula, no "remainder" wording remains on the item.

## CHANGE: clamp-invariant guard + catalogued misconfiguration edge
File: `mock/crates/hilavitkutin/src/plan/steps.rs` + `mock/crates/hilavitkutin/tests/r6_morsel_window_formula.rs`
A `debug_assert!` pins `min_morsel <= max_morsel` (a misconfigured consumer
const panics at plan time in std's clamp otherwise, with a worse message); the
non-4-aligned `MIN_MORSEL` floor interaction (`& !3` after the clamp can land
below the floor) is catalogued as an ignored test naming the intended
resolution.
Verification: `grep -c "debug_assert" mock/crates/hilavitkutin/src/plan/steps.rs` is at least 1; the ignored case is discoverable via `-- --ignored`.
