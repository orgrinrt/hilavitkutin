**Date:** 2026-05-10
**Phase:** TOPIC
**Round:** 202605102100 (PR #73 review fixups)

# Topic: address senior-review findings on round 202605091700

## Findings being addressed

1. **Em-dashes in `mock/crates/hilavitkutin-api/src/provider.rs`** module-level and trait-level doc comments. Replace with colons or rewrite. Workspace writing-style rule forbids em-dashes in any gated prose; `crates/**/*.rs` doc comments are gated.
2. **`hilavitkutin-extensions-macros/DESIGN.md.tmpl` not swept in the Capability → Provider rename**. Backticked references that no longer exist anywhere: `CapabilityEntry`, `CapabilityId`, `CapabilityExport`, `required_host_caps`, `CAPABILITIES`, `CAP_A`, `CAP_B`. The macro source already emits `ProviderEntry` etc. Sweep DESIGN to match.
3. **Internal generated identifiers in `extensions-macros/src/lib.rs`**: `__EXT_CAPABILITIES` and `__EXT_REQUIRED_CAPS` half-renamed. Flip to `__EXT_PROVIDERS` and `__EXT_REQUIRED_PROVIDERS`.
4. **BACKLOG entry on `hilavitkutin-api`**: `Resource::new(value)` drops the value at construction (stub until HILA-RUNTIME-C6 wires the persistence spine). Add a backlog item naming the obligation so the constructor is revisited when the data plane lands.

## Decisions

All four are mechanical fixes within the round-202605091700 design space. No design intent changes.

For the macro identifier rename: kept as `__EXT_PROVIDERS` (singular semantic was already plural-list). `__EXT_REQUIRED_PROVIDERS` for the required-host-providers slice.

For the BACKLOG entry: shape it as a "Wire `Resource::new` to scheduler-owned resource registry" item, cross-referencing HILA-RUNTIME-C6 and `mock/research/notes/202605091700_src_apply_decisions.md` (D1).
