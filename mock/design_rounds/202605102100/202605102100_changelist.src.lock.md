**Date:** 2026-05-10
**Phase:** SRC
**Round:** 202605102100 (PR #73 review fixups)
**Doc CL:** 202605102100_changelist.doc.lock.md (locked)

# Source CL — em-dashes + macro identifier polish

## CHANGE: em-dash punctuation removed from `mock/crates/hilavitkutin-api/src/provider.rs`

File: `mock/crates/hilavitkutin-api/src/provider.rs`

Replace em-dash characters in module-level `//!` and trait-level `///` doc comments with colons. Affected lines (per HEAD): 1, 84, 86, 87.

Verification: `! grep -n " — " mock/crates/hilavitkutin-api/src/provider.rs` returns zero matches.

## CHANGE: macro internal identifiers renamed in `mock/crates/hilavitkutin-extensions-macros/src/lib.rs`

File: `mock/crates/hilavitkutin-extensions-macros/src/lib.rs`

Rename internal generated `const` identifiers:

- `__EXT_CAPABILITIES` → `__EXT_PROVIDERS`
- `__EXT_REQUIRED_CAPS` → `__EXT_REQUIRED_PROVIDERS`

Both names appear in macro emit `quote!` blocks. Mechanical sed-rename.

Verification: `! grep -n "__EXT_CAPABILITIES\|__EXT_REQUIRED_CAPS" mock/crates/hilavitkutin-extensions-macros/src/lib.rs` returns zero matches; the macro tests in `lto_smoke` still pass post-rename.

## Lock criteria

`cargo check --workspace --lib` clean. `cargo mock --lint-only --commit` shows no `design-doc-source-mismatch` violations on the touched crates.
